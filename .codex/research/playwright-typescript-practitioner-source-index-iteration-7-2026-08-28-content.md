---
title: "Playwright + TypeScript practitioner source index — article content"
source: ".codex/research/playwright-typescript-practitioner-source-index-iteration-7-2026-08-28.md"
retrieved: "2026-08-29"
---

# Playwright + TypeScript practitioner source index

## Kolejność i pokrycie

The source index contains 95 HTTP/HTTPS Markdown links in the order of appearance. One canonical-URL duplicate was merged (`scrolltest.com/playwright-lighthouse-performance-audits/` appears both in the Michal Drajna section as a repost pointer and in the ScrollTest section; kept once at its first position), leaving 94 unique entries below in identical order. Local Markdown links were not fetched.

| # | Autor/serwis | Tytuł | URL | Status |
|---:|---|---|---|---|
| 1 | Anton Gulin | Review AI-generated tests with seven checks | https://www.anton.qa/blog/posts/review-ai-generated-tests-seven-checks | complete |
| 2 | Anton Gulin | Your Regression Suite Is a Museum | https://www.anton.qa/blog/posts/regression-suite-museum | complete |
| 3 | Michal Drajna | How to Create Custom Fixtures in Playwright TypeScript (repost) | https://www.linkedin.com/posts/michaldrajna-qa_how-to-create-custom-fixtures-in-playwright-activity-7499020460131766273-l642 | complete |
| 4 | Michal Drajna | Why Your Playwright Tests Are Lying to You (repost) | https://www.linkedin.com/posts/michaldrajna-qa_why-your-playwright-tests-are-lying-to-you-activity-7498658155678244865-tiNA | complete |
| 5 | Abdur Raffae Masood | What Actually Breaks When Your Playwright Suite Hits 10,000 Tests | https://medium.com/@ARaffaeSQA/what-actually-breaks-when-your-playwright-suite-hits-10-000-tests-6e7f15279369 | complete |
| 6 | Abdur Raffae Masood | What Actually Breaks at 10,000 Tests (DEV mirror) | https://dev.to/araffaesqa/what-actually-breaks-when-your-playwright-suite-hits-10000-tests-55g2 | unavailable |
| 7 | ScrollTest / Pramod Dutta | Lighthouse Performance Audits in Playwright | https://scrolltest.com/playwright-lighthouse-performance-audits/ | complete |
| 8 | Michal Drajna | What if learning Playwright felt like playing a video game? | https://www.linkedin.com/posts/michaldrajna-qa_what-if-learning-playwright-felt-like-playing-activity-7498295694269730817-WAL8 | complete |
| 9 | Michal Drajna | Playwright MCP gives an AI agent a browser (repost) | https://www.linkedin.com/posts/michaldrajna-qa_playwright-mcp-gives-an-ai-agent-a-browser-activity-7490356957200031744-CmT9 | complete |
| 10 | Viktor Konovalov | Playwright tip: snapshots aren't just for UI | https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-api-activity-7487397613378363392-7Egr | complete |
| 11 | Viktor Konovalov | Playwright tip: use `locator.evaluateAll()` in one browser call | https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7482327981252591616-q1yt | complete |
| 12 | Viktor Konovalov | Playwright tip: use CDPSession when public API is not enough | https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7489942200819015680-t7Pt | complete |
| 13 | Viktor Konovalov | Playwright tip: validate the accessibility tree, not the DOM | https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7472539768522940418-yMME | complete |
| 14 | Viktor Konovalov | Playwright tip: use `page.addInitScript()` before the app starts | https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7470361720436183040-N_HK | complete |
| 15 | Viktor Konovalov | Compare Playwright HTML, Allure and QAit reports | https://www.linkedin.com/posts/viktorkonovalovqa_day3reportcomparison-activity-7492837359609192448-K0mf | complete |
| 16 | Viktor Konovalov | Secrets-management experiment | https://www.linkedin.com/posts/viktorkonovalovqa_day4secrets-activity-7495385292724596736-ib3O | complete |
| 17 | Stefan Minchev | One login test plus `storageState` | https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7490425075612049409-pXAh | complete |
| 18 | Stefan Minchev | Automate the second factor instead of disabling it | https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7493686570198142978-GBRT | complete |
| 19 | Stefan Minchev | Seed the order through the request fixture | https://www.linkedin.com/posts/stefan-minchev-qa_seed-data-via-api-activity-7492961792541851648-oLlI | complete |
| 20 | Stefan Minchev | `isVisible()` is not a waiting assertion | https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7488613133301792768-wV3t | complete |
| 21 | Stefan Minchev | Switch environments with one typed variable | https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7487889010883801088-yhnc | complete |
| 22 | Ivan Davidov | I Made Stefan Minchev Defend Every Decision in Our Playwright Framework | https://dev.to/idavidov13/i-made-stefan-minchev-defend-every-decision-in-our-playwright-framework-2dfa | complete |
| 23 | Angela Zelaya | Claude Code + Playwright CLI (repost) | https://www.linkedin.com/posts/angela-zelaya-b185b218b_getting-started-with-claude-code-and-playwright-activity-7490742116910870528-06b9 | complete |
| 24 | Angela Zelaya | Playwright CLI UI Review and `generate-locator` (repost) | https://www.linkedin.com/posts/angela-zelaya-b185b218b_giving-ui-reviews-to-coding-agents-playwright-activity-7455966334925910016-Lt1E | complete |
| 25 | ScrollTest / Promode | Playwright Contract Testing with TypeScript: Day 46 | https://scrolltest.com/playwright-contract-testing-typescript-day-46/ | complete |
| 26 | ScrollTest / Promode | API Contract Testing: REST + UI in One Test | https://scrolltest.com/api-contract-testing-playwright-rest-ui-one-test/ | complete |
| 27 | ScrollTest / Promode | TypeScript for Playwright Testing | https://scrolltest.com/typescript-for-playwright-testing/ | complete |
| 28 | ScrollTest / Promode | Playwright Suite Lying? 3 Signs Your Tests Pass Wrong | https://scrolltest.com/playwright-suite-lying-tests/ | complete |
| 29 | ScrollTest / Promode | Playwright Retries and Flaky Tests: Day 42 | https://scrolltest.com/playwright-retries-flaky-tests-day-42/ | complete |
| 30 | ScrollTest / Promode | Playwright Flaky Test Audit Template | https://scrolltest.com/playwright-flaky-test-audit-template/ | complete |
| 31 | ScrollTest / Promode | Browser Contexts: Test Isolation in TypeScript: Day 52 | https://scrolltest.com/playwright-browser-contexts-typescript-day-52/ | complete |
| 32 | ScrollTest / Promode | Authentication: Day 10 | https://scrolltest.com/playwright-authentication-day-10/ | complete |
| 33 | ScrollTest / Promode | Performance Testing with TypeScript: Day 47 | https://scrolltest.com/playwright-performance-testing-typescript-day-47/ | complete |
| 34 | ScrollTest / Promode | iFrames in TypeScript: Day 50 | https://scrolltest.com/playwright-iframes-frames-typescript-day-50/ | complete |
| 35 | ScrollTest / Promode | Tags and Annotations: Day 48 | https://scrolltest.com/playwright-tags-annotations-typescript-day-48/ | complete |
| 36 | ScrollTest / Pramod Dutta | Network Interception: Day 8 (updated) | https://scrolltest.com/21-day-playwright-day-8-network-interception-mock-api/ | complete |
| 37 | ScrollTest / Promode | Mobile Testing and Device Emulation: Day 49 | https://scrolltest.com/playwright-mobile-testing-typescript-day-49/ | complete |
| 38 | ScrollTest / Pramod Dutta | Date Pickers and Calendars | https://scrolltest.com/playwright-date-picker-calendar-testing/ | complete |
| 39 | ScrollTest / Pramod Dutta | Toast Notifications and Timing | https://scrolltest.com/playwright-toast-notification-testing/ | complete |
| 40 | ScrollTest / Pramod Dutta | Keyboard Navigation and Accessibility | https://scrolltest.com/playwright-keyboard-navigation-accessibility/ | complete |
| 41 | ScrollTest / Pramod Dutta | Allure Reporting with Playwright: The Complete Guide | https://scrolltest.com/playwright-allure-reporting-complete-guide/ | complete |
| 42 | ScrollTest / Promode | Release Notes Checklist | https://scrolltest.com/playwright-release-notes-checklist/ | complete |
| 43 | ScrollTest / Promode | Playwright 1.62 Upgrade Checklist | https://scrolltest.com/playwright-1-62-upgrade-checklist/ | complete |
| 44 | ScrollTest / Promode | Playwright 1.62 Regression Checklist | https://scrolltest.com/playwright-1-62-regression-checklist/ | complete |
| 45 | ScrollTest / Promode | Release Smoke Suite for 1.62.1 Upgrades | https://scrolltest.com/playwright-release-smoke-suite-1621/ | complete |
| 46 | ScrollTest / Promode | Browser AI Testing: 1.62.1 Deep Dive | https://scrolltest.com/playwright-browser-ai-testing/ | complete |
| 47 | ScrollTest / Promode | Playwright vs Selenium in 2026 | https://scrolltest.com/playwright-vs-selenium-2026/ | complete |
| 48 | Currents.dev (Walter Galvão) | AI Skill: Playwright Best Practices | https://currents.dev/posts/playwright-best-practices-skill | complete |
| 49 | Currents | Agent Skill: Playwright Best Practices (docs) | https://docs.currents.dev/ai/agent-skill-playwright-best-practices | complete |
| 50 | currents-dev (GitHub) | playwright-best-practices-skill repository | https://github.com/currents-dev/playwright-best-practices-skill | complete |
| 51 | Currents.dev | Playwright's New `--last-failed-file` Flag | https://currents.dev/posts/playwright-re-run-only-failed-tests | complete |
| 52 | Currents.dev | Playwright HTML Reporter: Why It Breaks Down at Scale | https://currents.dev/posts/playwright-html-reporter-why-it-breaks-down-at-scale | complete |
| 53 | Currents.dev (Goodness Eboh) | State of Playwright AI Ecosystem in 2026 | https://currents.dev/posts/state-of-playwright-ai-ecosystem-in-2026 | complete |
| 54 | Currents.dev (Asjad Khan) | When Tests Should Run Headless vs Headed | https://currents.dev/posts/when-tests-should-run-headless-vs-headed-in-playwright | complete |
| 55 | Currents.dev | The Playwright Network Mocking Playbook | https://currents.dev/posts/the-playwright-network-mocking-playbook | complete |
| 56 | TestDino (Jashn Jain) | 17 Playwright Best Practices That Actually Matter | https://testdino.com/blog/playwright-best-practices | complete |
| 57 | TestDino (Savan Vaghani) | Playwright UI Mode: Visual Test Debugging | https://testdino.com/blog/playwright-ui-mode | complete |
| 58 | TestDino (Vishwas Tiwari) | Playwright Flaky Test Debugging | https://testdino.com/blog/playwright-flaky-test-debugging | complete |
| 59 | TestDino (Pratik Patel) | Headless vs Headed in Playwright | https://testdino.com/blog/headless-vs-headed | complete |
| 60 | TestDino (Dhruv Rai) | Playwright Trace Viewer | https://testdino.com/blog/playwright-trace-viewer | complete |
| 61 | TestDino (Vishwas Tiwari) | Why Playwright Tests Pass Locally but Fail in CI | https://testdino.com/blog/playwright-test-failure | complete |
| 62 | TestDino (Ayush Mania) | Performance Testing Using Playwright | https://testdino.com/blog/playwright-performance-testing | complete |
| 63 | Level Up Coding / Mohammad Faisal Khatri | Custom Fixtures in Playwright TypeScript | https://levelup.gitconnected.com/how-to-create-custom-fixtures-in-playwright-typescript-a-complete-practical-guide-4fa8b2fc2c82 | complete |
| 64 | Level Up Coding / Mohammad Faisal Khatri | Browser vs BrowserContext vs Page | https://levelup.gitconnected.com/playwright-browser-vs-browsercontext-vs-page-complete-guide-with-examples-b6c771c8d371 | complete |
| 65 | Level Up Coding / Mohammad Faisal Khatri | Data-driven API Testing: Part 1 | https://levelup.gitconnected.com/data-driven-api-testing-in-playwright-typescript-part-1-cc4f7deaf74a | complete |
| 66 | Level Up Coding / Mohammad Faisal Khatri | Data-driven API Testing: Part 2 | https://levelup.gitconnected.com/data-driven-api-testing-in-playwright-typescript-part-2-5d6e84f5da45 | complete |
| 67 | Level Up Coding / Mohammad Faisal Khatri | GET API Requests | https://levelup.gitconnected.com/how-to-test-get-api-requests-with-playwright-typescript-7caf7bf790b2 | complete |
| 68 | Level Up Coding / Mohammad Faisal Khatri | Global Setup for API Testing | https://levelup.gitconnected.com/global-setup-in-playwright-typescript-for-api-testing-ce7abe0173cf | complete |
| 69 | Level Up Coding / Mohammad Faisal Khatri | Verify Response Data | https://levelup.gitconnected.com/how-to-verify-response-data-in-playwright-typescript-4b288ad46616 | complete |
| 70 | Level Up Coding / Mohammad Faisal Khatri | Build a Local AI QA Engineer with Playwright MCP | https://levelup.gitconnected.com/build-your-own-local-ai-qa-engineer-with-docker-ollama-librechat-and-playwright-mcp-1a254fab91d0 | complete |
| 71 | Level Up Coding / Mohammad Faisal Khatri | Vibe Testing with Playwright MCP | https://levelup.gitconnected.com/vibe-testing-with-playwright-mcp-testing-ux-with-ai-agents-6b1be03b388a | complete |
| 72 | Vitali Haradkou | Angular-Aware Playwright Selectors (canonical) | https://vitalicset.hashnode.dev/angular-aware-playwright-selectors-query-components-by-input-signals-and-state | complete |
| 73 | Vitali Haradkou | Testing Angular Components by Properties (DEV mirror) | https://dev.to/vitalicset/testing-angular-components-by-properties-with-playwright-33jj | complete |
| 74 | Vitali Haradkou | Blog index (Vercel) | https://blog-vitaliharadkous-projects.vercel.app/blog | complete |
| 75 | Vitaliy Potapov | Posts index | https://vitalets.github.io/posts/ | complete |
| 76 | Sajith Dilshan | Playwright Auto-Waiting (LinkedIn promo) | https://www.linkedin.com/posts/sajith-dilshan_playwright-testautomation-qualityassurance-activity-7489870161777152000-hWVW | complete |
| 77 | Sajith Dilshan | Playwright Auto-Waiting (canonical Medium article) | https://medium.com/@sajith-dilshan/playwright-auto-waiting-the-secret-behind-stable-and-reliable-test-automation-bd3987a3156e | complete |
| 78 | Sajith Dilshan | TypeScript Module Systems Explained (LinkedIn promo) | https://www.linkedin.com/posts/sajith-dilshan_typescript-module-systems-explained-commonjs-activity-7474649871942307840-HVje | complete |
| 79 | Sajith Dilshan | Scalable Authentication beyond `globalSetup` (LinkedIn promo) | https://www.linkedin.com/posts/sajith-dilshan_playwright-testautomation-qaengineering-activity-7484796747332931584-bgzX | complete |
| 80 | Artem Bondar | Course refresh announcement | https://www.linkedin.com/posts/artem-bondar_hey-guys-just-fyi-i-recently-refreshed-activity-7490476134573412352-rkQH | complete |
| 81 | Artem Bondar / Bondar Academy | Blog index | https://bondaracademy.com/blog | complete |
| 82 | Joseph Ward | Blog index | https://josephward.tech/blog/ | complete |
| 83 | Yevhen Laichenkov | Posts index | https://elaichenkov.github.io/posts/ | complete |
| 84 | Yevhen Laichenkov | 17 Playwright Testing Mistakes You Should Avoid | https://elaichenkov.github.io/posts/17-playwright-testing-mistakes-you-should-avoid | complete |
| 85 | Yevhen Laichenkov | TIL: Playwright step decorator | https://elaichenkov.github.io/posts/til-playwright-step-decorator | complete |
| 86 | Butch Mayhew | Do not stop reading agent-written code | https://www.linkedin.com/posts/butchmayhew_softwaretesting-aiinqa-qa-activity-7490115981323935744-S_1o | complete |
| 87 | Butch Mayhew | Playwright and AI in QA: Beyond Test Generation | https://www.linkedin.com/posts/butchmayhew_orchestrating-ai-native-testing-with-playwright-activity-7478073686026207234-DD7I | complete |
| 88 | Butch Mayhew | Manual testing is still valuable | https://www.linkedin.com/posts/butchmayhew_playwright-testautomation-qaengineering-activity-7473717480301977600-X6nL | complete |
| 89 | Playwright | Best Practices (official docs) | https://playwright.dev/docs/best-practices | complete |
| 90 | Playwright | Fixtures (official docs) | https://playwright.dev/docs/test-fixtures | complete |
| 91 | Playwright | Assertions (official docs) | https://playwright.dev/docs/test-assertions | complete |
| 92 | Playwright | Configuration (official docs) | https://playwright.dev/docs/test-configuration | complete |
| 93 | Playwright | Test API: `test` (incl. `test.step`) | https://playwright.dev/docs/api/class-test#test-step | complete |
| 94 | Playwright | Authentication (official docs) | https://playwright.dev/docs/auth | complete |

## Artykuły

### 1. Anton Gulin — Review AI-generated tests with seven checks

- Source: https://www.anton.qa/blog/posts/review-ai-generated-tests-seven-checks
- Retrieved: 2026-08-29
- Exa status: complete

Published: August 12, 2026.

Review AI-generated tests with seven checks. Name the user risk. Break the product. Check the final result. Change the data. Read the failure. Repeat the run. Then decide whether the test could stop a bad release.

AI can write a clean test in seconds. The file may look finished. The names may sound correct. The test may even pass.

None of those facts prove value. A useful test catches a failure that matters to users. Your review must find that proof before the test joins your suite.

I use seven checks for that decision.

## 1. Name the user risk

Start with the person who could get hurt. Write the risk in one sentence.

For example: "A customer sees the wrong total and pays too much."

Avoid risks like "checkout may fail." That sentence does not name the damage. It also gives the test no clear target.

Ask two questions:

1. Who loses something?
2. What do they lose?

Money, time, access, and trust are clear answers. "The feature breaks" is not.

## 2. Break the product

A test should fail when its protected behavior breaks. Prove that before keeping it.

AI often writes a test that follows the correct steps. The test may never check the important result.

```typescript
test('customer can pay', async ({ page }) => {
  await page.goto('/checkout')
  await page.getByRole('button', { name: 'Pay' }).click()
  await expect(page.getByText('Success')).toBeVisible()
})
```

This test checks a message. It does not check the charged amount.

Remove the payment action or return the wrong total. The test must fail. A passing result means the test protects nothing useful.

## 3. Check the final result

Clicks are steps. Results are proof.

The test above clicks the right button. A stronger test checks the amount and payment record.

```typescript
await expect(page.getByTestId('order-total')).toHaveText('$120.00')
await expect(page.getByTestId('payment-status')).toHaveText('Paid')
```

An assertion means a result check. Playwright provides assertions that wait for results. The tool can wait. You still choose the right result.

Review every assertion. Ask what user outcome it proves. Rewrite assertions that only confirm page activity.

## 4. Change the data

AI tends to generate a clean example. Real users bring missing, wrong, and extreme values.

Test more than one amount. Include zero, a large value, and invalid text.

```typescript
for (const amount of ['0', '999999', 'wrong']) {
  await page.getByLabel('Amount').fill(amount)
  await page.getByRole('button', { name: 'Pay' }).click()
  await expect(page.getByRole('alert')).toBeVisible()
}
```

The exact values depend on your product. The review question stays simple. Can one neat example hide a serious failure?

## 5. Read the failure

Run the test against a broken result. Then read its message.

Another engineer should understand the problem without opening the whole file. Compare these messages:

```text
Expected: "$20.00"
Received: "$120.00"
```

```text
Timeout after 30000ms
```

The first message points to the product error. The second sends someone searching through logs.

Use clear result checks and useful test names. A failure should shorten the investigation.

## 6. Run it twice

Use the same input twice. You should get the same result.

Repeated runs catch shared data and timing problems. They also expose tests that depend on another test.

Do not accept a passing second run as proof. Compare both runs. Investigate any difference before keeping the test.

## 7. Make the release decision

Finish with one question: Would this test stop a bad release?

Name the release it could stop. For example: "This test blocks checkout when totals are wrong."

Keep the test when the answer is clear. Rewrite or delete it when the answer stays vague.

This step protects the review queue. Teams do not need every generated test. They need the small set that proves important behavior.

## A five-minute review example

Imagine that AI writes a checkout test. The test adds one product and completes payment.

### 2. Anton Gulin — Your Regression Suite Is a Museum: 5 Questions That Decide Delete vs Keep

- Source: https://www.anton.qa/blog/posts/regression-suite-museum
- Retrieved: 2026-08-29
- Exa status: complete

Published: August 19, 2026.

Every sprint adds tests. Almost no sprint deletes one.

That one-way flow has a predictable end. The suite gets slower. Flaky tests (tests that fail randomly) pile up. Engineers stop trusting red builds. Eventually someone suggests rewriting everything, and the cycle restarts.

There is a cheaper fix. Audit the suite with five questions. On the last suite the author inherited, they retired a third of the tests. Nothing those tests "guarded" ever broke.

## Question 1: When did this test last fail for a real reason?

Your CI history (the record of server test runs) already knows.

A test that has been green for a year has two possible explanations. Either the code it guards never changed, or the test cannot detect change. Both are worth knowing. Only one deserves compute on every commit.

Practical rule: pull the last 90 days of runs. Tag every test that failed only for environment reasons, or never failed at all. Those are audit candidates.

## Question 2: What user risk does it guard?

Write the risk in one sentence, naming a user.

"It tests the profile page" is not a risk. "A user loses saved work when the session expires" is a risk.

If nobody on the team can produce that sentence, the test is guarding an implementation detail, not a user. Details change on purpose all the time. Tests guarding them fail on purpose all the time. That is where flakiness lives.

## Question 3: Would anyone notice if it vanished?

Run the experiment instead of debating it:

```bash
# in a branch: skip the suspect test, run everything else
npx playwright test --grep-invert "@museum-candidate"
```

If coverage of the named risk survives through other tests, and no gap appears in the risk list from Question 2, the test was furniture.

Delete it in the branch. Keep the branch open a week. Merge with confidence.

## Question 4: Does it check results or steps?

A test that clicks through checkout and asserts the button was clickable is a tour, not a test.

```typescript
// a tour: asserts the step happened
await page.getByRole('button', { name: 'Pay' }).click();
await expect(page).toHaveURL(/confirmation/);

// a test: asserts the outcome is real
await expect(page.getByTestId('invoice-total')).toHaveText('$34.20');
await expect(page.getByTestId('invoice-number')).not.toBeEmpty();
```

The URL can change while the invoice ships blank. Assert on the thing the user came for.

## Question 5: Can it fail at all?

Flip the condition and run it.

```typescript
// original
await expect(status).toBe('paid');
// flipped: this MUST fail. If it passes, the test is dead.
await expect(status).not.toBe('paid');
```

A test that passes both ways asserts nothing. AI-written tests fail this check more than any other kind, because generators optimize for green.

Sixty seconds per suspicious test. The flip never lies.

## What you get back

Five questions, one afternoon, on the oldest third of your suite.

The suite you keep is faster, and every test in it can answer "what breaks if I go red?" That is what makes a red build mean something.

And when an AI agent starts writing tests into your suite, this audit is the contract you hold its work against. A generated test enters only if it survives the same five questions.

### 3. Michal Drajna — How to Create Custom Fixtures in Playwright TypeScript (repost)

- Source: https://www.linkedin.com/posts/michaldrajna-qa_how-to-create-custom-fixtures-in-playwright-activity-7499020460131766273-l642
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post by Michal Drajna, 2026-08-28. Repost pointing to the Level Up Coding guide (canonical article listed separately in this index).

Why custom fixtures make Playwright fundamentally superior to legacy test runners. 🛠️ Coming from frameworks where global hooks like beforeEach control state, Playwright's dependency-injected fixture model can feel like magic. But once you lock down the design pattern, you'll never go back. 🎭 Check out this comprehensive guide on Level Up Coding to see how custom TypeScript fixtures eliminate global state, prevent race conditions in parallel CI runs, and keep your test suite lightning-fast. 🧠 https://lnkd.in/dGjWKSMg

### 4. Michal Drajna — Why Your Playwright Tests Are Lying to You (repost)

- Source: https://www.linkedin.com/posts/michaldrajna-qa_why-your-playwright-tests-are-lying-to-you-activity-7498658155678244865-tiNA
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post by Michal Drajna, 2026-08-27. Repost of Abdur Raffae Masood's guide.

Playwright auto-waits. So why are your tests still flaking? 📊 Switching to Playwright eliminates many timing issues, but modern app complexity creates a whole new class of flakiness. 🎭 If your suite passes in isolation but breaks under parallel CI execution, the issue usually isn't the framework. It's environment drift, optimistic UI renders, and shared data dependencies. https://lnkd.in/dmupGPRY

### 5. Abdur Raffae Masood — What Actually Breaks When Your Playwright Suite Hits 10,000 Tests

- Source: https://medium.com/@ARaffaeSQA/what-actually-breaks-when-your-playwright-suite-hits-10-000-tests-6e7f15279369
- Retrieved: 2026-08-29
- Exa status: complete

Published: July 29, 2026 · 7 min read. Surfaced via Michal Drajna's section of the index; a DEV mirror exists (listed separately below).

Somewhere between 500 tests and 10,000, every Playwright suite hits the same wall — and it's rarely the thing teams expect. It's not that Playwright can't handle the volume. It's that the shortcuts which felt harmless at 500 tests — a shared test user, a flat folder, a five-second `waitForTimeout` copy-pasted into fifty files — start compounding into hours of CI time, mystery failures, and a team that quietly stops trusting the pipeline.

This is a practical look at the architecture decisions that separate a suite that scales gracefully from one that collapses under its own size — and, more usefully, what to fix first if you're already feeling the pain.

## Scale Isn't About Test Count — It's About Everything Test Count Multiplies

Adding tests doesn't just add tests. It adds browsers running concurrently, API calls hitting your backend, database writes, CI minutes, and — critically — surface area for tests to interfere with each other. A suite of 500 tests might finish in fifteen minutes. The same design, scaled naively to 10,000, doesn't take twenty times longer — it can take days, because the interference between tests grows faster than the test count itself.

The fix isn't more CI runners. It's a framework that was built to scale before it needed to.

## The Four Things That Actually Determine Whether You Scale

Every scaling problem traces back to a gap in one of these:

- Isolation — can a test run alone, first, last, or in parallel, and get the same result every time?
- Parallelism — is your suite actually structured to use multiple workers, or just configured to?
- Architecture — can a new engineer find and safely extend a test without reading the whole codebase first?
- Feedback speed — does a developer find out about a broken test in 3 minutes, or 3 hours?

Weakness in any one of these caps how far the other three can take you. You can add as many parallel workers as you want — if tests share state, parallelism makes failures more frequent, not less.

## Structure the Repo Around Ownership, Not File Type

The single most common anti-pattern in growing suites is a flat `tests/` folder with hundreds of files and no clear owner for any of them. Organize around what a team actually owns and how they'll navigate it:

```text
playwright-suite/
├── tests/
│   ├── checkout/
│   ├── authentication/
│   ├── inventory/
│   ├── payments/
│   └── accessibility/
├── page-objects/
├── fixtures/
├── api-clients/
├── test-data/
├── config/
└── playwright.config.ts
```

Naming test files `test1.spec.ts`, `test2.spec.ts` might feel fine for the first fifty files. By file three hundred, nobody can tell what's covered without opening each one. `checkout/apply-discount-code.spec.ts` tells a reviewer what broke before they even open the diff.

## Isolation Is the Real Bottleneck — Not Hardware

Here's the failure mode that kills scaling efforts: Test A creates a customer record, Test B modifies it, and everything works — as long as they run in that exact order, on the same worker, every single time. The moment you add parallel workers, that assumption breaks, and you get failures that only reproduce "sometimes," which is the worst kind of failure to debug.

```typescript
// Fragile: depends on execution order and shared state
test('creates a discount code', async ({ page }) => {
  await page.goto('/admin/discounts/new');
  await page.getByLabel('Code').fill('SUMMER25');
  await page.getByRole('button', { name: 'Create' }).click();
});
```

### 6. Abdur Raffae Masood — What Actually Breaks at 10,000 Tests (DEV mirror)

- Source: https://dev.to/araffaesqa/what-actually-breaks-when-your-playwright-suite-hits-10000-tests-55g2
- Retrieved: 2026-08-29
- Exa status: unavailable

- Exa error: `CRAWL_NOT_FOUND` (two attempts, second after a 30 s backoff marker).
- Fallback scrape attempt (dedicated scraper) returned HTTP 404: "404: Page Not Found — Looks like this page doesn't exist or may not be published."

The DEV mirror URL referenced in the source index is dead or unpublished. The canonical Medium article is entry 5 in this document; no content was substituted for this mirror.

### 7. ScrollTest / Pramod Dutta — Lighthouse Performance Audits in Playwright

- Source: https://scrolltest.com/playwright-lighthouse-performance-audits/
- Retrieved: 2026-08-29
- Exa status: complete

By Pramod Dutta, August 5, 2026. This URL appears twice in the source index (once as the repost Michal Drajna pointed to, once in the ScrollTest section); the duplicate was merged into this single entry.

Your end-to-end suite proves the app works, but does it prove the app is fast? A page can pass every functional test and still ship a 4-second Largest Contentful Paint that quietly bleeds conversions. In this guide you'll learn how to wire Playwright Lighthouse performance audits into your TypeScript suite so Core Web Vitals become a hard gate in CI — failing a build when performance regresses, exactly like a broken assertion would.

## Why run Lighthouse from Playwright?

Lighthouse is Google's open-source auditing engine for performance, accessibility, SEO, and best practices. On its own it runs against a URL via the CLI or Chrome DevTools, but those runs are one-off and detached from your test flow. The interesting cases — a logged-in dashboard, a multi-step checkout, a page that only renders after you dismiss a cookie banner — require a real browser session with cookies, storage, and navigation already in place. That is precisely what Playwright gives you.

By bridging the two, you run a genuine Lighthouse audit against the same Chromium instance your test already drove. You reuse Playwright's authentication state, its network mocking, and its navigation, then hand the live browser to Lighthouse over the Chrome DevTools Protocol (CDP). The result: performance scores for the exact authenticated, interacted-with state your users see — not an anonymous cold load of the marketing homepage.

| Approach | Auth / interaction state | CI integration | Best for |
| --- | --- | --- | --- |
| Lighthouse CLI | None (cold anonymous load) | Separate step | Quick public-URL checks |
| Lighthouse CI (LHCI) | Limited (scripted) | First-class, with budgets server | Tracking trends over time |
| Playwright + Lighthouse | Full (reuses Playwright session) | Inside your existing suite | Auditing authenticated, post-interaction pages |

## Setting up the toolchain

The cleanest way to glue Playwright and Lighthouse together is the community package `playwright-lighthouse`, which wraps the Lighthouse Node API and accepts a Playwright `page` directly. You also need `lighthouse` itself as a peer dependency:

```bash
// Terminal
npm i -D @playwright/test playwright-lighthouse lighthouse
npx playwright install chromium
```

The single most important setup detail is the remote debugging port. Lighthouse attaches to Chrome over CDP, so the browser must be launched with a fixed `--remote-debugging-port`. Playwright does not expose that flag through fixtures by default, so the reliable pattern is to launch the browser yourself inside the test (or a fixture) with the port pinned:

```typescript
import { test } from '@playwright/test';
import { chromium } from 'playwright';
import { playAudit } from 'playwright-lighthouse';

test('homepage meets the performance budget', async () => {
  // Launch Chromium with a fixed CDP port Lighthouse can attach to.
  const browser = await chromium.launch({
    args: ['--remote-debugging-port=9222'],
  });
  const page = await browser.newPage();
  await page.goto('https://example.com');

  await playAudit({
    page,
    port: 9222,
    thresholds: {
      performance: 80,
      accessibility: 90,
      'best-practices': 90,
      seo: 90,
    },
  });

  await browser.close();
});
```

When any category score falls below its threshold, `playAudit` throws — and a thrown error fails the Playwright test, which fails the build. That single behaviour is what turns a vanity metric into an enforced contract.

## Auditing an authenticated page

The real payoff of running Playwright Lighthouse performance audits is auditing pages behind a login. Because the audit runs against the same browser you control, you can sign in first — or, better, reuse a saved `storageState` so the audit starts from the authenticated state.

### 8. Michal Drajna — What if learning Playwright felt like playing a video game?

- Source: https://www.linkedin.com/posts/michaldrajna-qa_what-if-learning-playwright-felt-like-playing-activity-7498295694269730817-WAL8
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post by Michal Drajna, 2026-08-26.

What if learning Playwright felt like playing a video game? 🎮 Reading dry documentation and sifting through GitHub gists can make learning a new testing framework feel like a chore. 🎭 Enter Playwright Quest — an interactive online game that guides you through modern E2E automation step-by-step. Instead of passive watching, you solve real-world coding puzzles, tackle tricky selectors, and level up your test automation skills directly in your browser. 🚀 https://lnkd.in/dNcuiGUc

Engagement: 11 comments; commenters joke about flaky-suite fixing being the final boss and share their own quizzes.

### 9. Michal Drajna — Playwright MCP gives an AI agent a browser (repost)

- Source: https://www.linkedin.com/posts/michaldrajna-qa_playwright-mcp-gives-an-ai-agent-a-browser-activity-7490356957200031744-CmT9
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post by Michal Drajna, 2026-08-04. Repost of Daniel Dahlinn's blueprint; authorship of the underlying material is external.

Beyond the MCP Hype: How to build AI test generation that actually scales. 📈
The Model Context Protocol gives AI agents direct access to browser automation frameworks like Playwright. 🎭
But without strict boundaries, agents quickly produce duplicate Page Objects, fragile selectors, and unmaintainable test logic.
Daniel Dahlinn presents a blueprint for shifting from raw agentic crawling to Model-Governed Test Generation. Discover how encoding application domain rules before the agent writes code preserves code quality, prevents drift, and makes agent output truly production-ready. ⚡

https://lnkd.in/dhAaiHCb

Engagement: 21 reactions; a commenter notes that without a governance layer, review becomes the actual bottleneck.

### 10. Viktor Konovalov — Playwright tip: snapshots aren't just for UI

- Source: https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-api-activity-7487397613378363392-7Egr
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-07-27.

When people hear "snapshot testing", they usually think about screenshots.

But snapshots are just as useful for API testing.

They're especially useful when you need to verify that stable parts of an API response never change unexpectedly.

Instead of writing dozens of assertions for large JSON payloads, you can compare the entire response against a stored snapshot.

Before creating the snapshot, remove dynamic fields such as IDs, timestamps, or tokens. This keeps the comparison focused on the data that should remain stable.

Snapshots work especially well for:

- configuration endpoints
- feature flags
- large API payloads
- API contract verification

The goal isn't to replace assertions.

It's to detect unexpected changes in fields that are expected to remain unchanged, while keeping your tests simple and easy to maintain.

Engagement: commenters add that schema validation and targeted assertions are layers, not alternatives — AJV fits OpenAPI-spec teams, Zod fits TypeScript-first teams.

### 11. Viktor Konovalov — Playwright tip: use `locator.evaluateAll()` to process elements in one browser call

- Source: https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7482327981252591616-q1yt
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-07-13.

Need to read data from many elements?

Many tests do something like this:

- get all locators
- loop through them
- call `textContent()` for each one

It works.

But every `textContent()` is a separate round trip between Node.js and the browser.

`locator.evaluateAll()` executes your code inside the browser and returns the final result in a single call.

This is especially useful when extracting:

- table values
- lists of links
- product names
- IDs
- attributes
- any transformed data

Less browser communication. Cleaner and often faster tests.

Engagement: 23 reactions.

### 12. Viktor Konovalov — Playwright tip: use CDPSession when public API is not enough

- Source: https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7489942200819015680-t7Pt
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-03.

Playwright has a clean and powerful public API.

But sometimes you need access to browser-level features that are not exposed directly.

That's where CDPSession can help.

It lets you talk to Chrome DevTools Protocol directly and use lower-level browser capabilities.

For example, you can:

- emulate network conditions
- inspect performance metrics
- control browser permissions
- collect runtime data
- work with browser internals

This is useful when the public Playwright API is not enough, but the browser itself already supports what you need.

CDPSession is not something you should use everywhere.

For most tests, Playwright's built-in API is better, cleaner, and more stable. But when you need deeper browser control, CDPSession gives you a way in.

Engagement: 11 reactions; a commenter notes that CDP is Chrome's internal debugging protocol and a CDPSession detaches the moment its target closes — it must be treated as scoped to a single page lifecycle.

### 13. Viktor Konovalov — Playwright tip: validate the accessibility tree, not the DOM

- Source: https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7472539768522940418-yMME
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-06-16.

Many UI tests are tightly coupled to implementation details: CSS classes, element IDs, and page structure. The problem is that users never interact with those things.

Instead of asking "Does this div exist?", ask "Can a user find and use this button?"

Using accessibility-based locators like `getByRole()` and `getByLabel()` gives a few advantages:

- tests focus on user behavior instead of implementation details
- selectors become more stable during UI refactoring
- accessibility issues are often discovered earlier
- test code becomes easier to read

Good tests verify what users can do, not how the page is built.

Engagement: a commenter argues for a centralized CSS-constant locator strategy; the counterpoint (per the index synthesis) is that CSS/test ID remain a fallback when the app lacks stable semantics.

### 14. Viktor Konovalov — Playwright tip: use `page.addInitScript()` before the app starts

- Source: https://www.linkedin.com/posts/viktorkonovalovqa_playwright-typescript-qa-activity-7470361720436183040-N_HK
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-06-10.

Sometimes you need to control browser state before your application loads.

For example:

- feature flags
- localStorage values
- session data
- `Math.random()`
- browser APIs

Many teams set these values after `page.goto()`.

The problem is that the application may have already read them during startup.

`page.addInitScript()` runs before any page scripts execute.

This makes tests more predictable and avoids race conditions during initialization.

Good test setup starts before the application starts.

Engagement: 32 reactions.

### 15. Viktor Konovalov — Compare Playwright HTML, Allure and QAit reports

- Source: https://www.linkedin.com/posts/viktorkonovalovqa_day3reportcomparison-activity-7492837359609192448-K0mf
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-11. Part of the author's practical experiment series; commercial disclosure included.

Here is the next practical part of the experiment. This time, I focused on reporting. I compared the QAit report with Playwright HTML Report and Allure Report.

The comparison focused on five areas:

- readability
- debugging value
- release visibility
- historical analytics
- how much setup is needed to get useful reporting

All three reports solve the same basic problem — showing test results. But they approach it very differently.

Some are stronger for engineering investigation. Some provide better long-term analytics. And some require much more configuration before the report becomes truly useful. So I also looked at how quickly each option can be set up and how much tuning is needed to get accurate, meaningful results.

The interesting question is not which report has more features. It is which one gives the most useful information with the least reporting overhead.

Disclosure: QAit provided access to the platform and a referral link; the testing process and conclusions are the author's own.

Engagement: 19 reactions.

### 16. Viktor Konovalov — Secrets-management experiment

- Source: https://www.linkedin.com/posts/viktorkonovalovqa_day4secrets-activity-7495385292724596736-ib3O
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-18. Part of the same experiment series; commercial disclosure included.

Here is the next practical part of the experiment. This time, I focused on secrets management.

Test automation often needs credentials, API keys, tokens, and other sensitive data. The question is where those values should live — and how much protection the platform gives you by default.

I compared a few common approaches:

- secrets in the repository
- CI/CD secrets
- dedicated secret managers
- QAit built-in Secrets

What stood out in QAit is that secret handling is integrated directly into the testing workflow:

- encrypted at rest
- never returned to the UI in plaintext
- injected at runtime
- masked in logs, traces, and websocket frames
- audit and version history available
- reusable across tests

At the same time, storage is only one part of the security model. Access control and key management still matter.

Disclosure: QAit provided access to the platform and a referral link; the technical evaluation remains independent.

Engagement: 7 reactions.

### 17. Stefan Minchev — One login test plus `storageState`

- Source: https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7490425075612049409-pXAh
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-04.

Your login form deserves exactly one test, and it is not the one you are writing.

Most suites sign in inside `beforeEach`. Every spec pays for the same form, and not one of them is testing login.

A setup project pays once. It drives the real form, then `page.context().storageState({ path })` writes the cookies and localStorage to a file.

The config does the rest. `testMatch: /.*\.setup\.ts/` marks the setup file, `dependencies: ['setup']` makes every other project wait for it, and `storageState` points those projects at the saved file.

Two rules keep it honest. `playwright/.auth` goes in `.gitignore`, and one real UI test still drives the login form, because nothing else covers it anymore.

Engagement: 14 comments; commenters flag shared accounts and expired sessions under parallel runs as the tricky part.

### 18. Stefan Minchev — Automate the second factor instead of disabling it

- Source: https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7493686570198142978-GBRT
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-13.

Every login-once tutorial quietly assumes your app has no second factor.

`storageState` saves a logged in session to a file, but a 6 digit code stands between you and that session.

A TOTP code is math over a shared secret and the clock. Your authenticator app holds that secret, and your setup project can hold a test account copy.

🔸 `await generate({ secret: process.env.TOTP_SECRET })` builds the current code
🔸 Fill it, land on the dashboard, save `storageState`
🔸 Every other project starts logged in

🚩 A hardcoded code dies after 30 seconds
🚩 A human typing the code mid run is not automation
🚩 2FA turned off on staging means you never test the real login

Two limits worth stating. The secret must be issued for a test account and stored as a CI secret, never a real person's. SMS and email codes need a mailbox API instead.

Sequel to the author's post on logging in once: that one saves the session, this one gets past the wall standing in front of it.

### 19. Stefan Minchev — Seed the order through the request fixture

- Source: https://www.linkedin.com/posts/stefan-minchev-qa_seed-data-via-api-activity-7492961792541851648-oLlI
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-11.

The test for the order page walks the whole checkout first. It does not have to.

UI setup is the slowest way to reach a page. Many clicks, few navigations and a payment step, all before the first assertion about the order details.

It also spreads the blame. A broken discount field in checkout turns the order page test red, and now you debug a feature this test was never meant to cover.

The built-in `request` fixture creates the same order over HTTP. One POST, an ID back, and `page.goto` lands straight on the page under test.

🔸 Setup runs in milliseconds instead of a full browser flow
🔸 A red test points at the order page, not at checkout
🔸 Cleanup runs after `use()` in the same fixture

Keep one real UI checkout test. That flow deserves coverage. It just does not deserve to run again as setup for every other test.

### 20. Stefan Minchev — `isVisible()` is not a waiting assertion

- Source: https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7488613133301792768-wV3t
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-07-30.

Two lines, same locator. One waits up to 5 seconds, the other answers in 2 milliseconds.

`await expect(banner).toBeVisible()` retries until the 5 second assertion timeout, so a banner that renders late still passes.

`if (await banner.isVisible())` reads the page as it looks right now. The author ran it against a banner that renders 800 ms after the click. It returned false in 2 ms.

The `if` body is skipped. Nothing throws, the test goes green, the bug ships.

It also ignores its own timeout option. Passing `{ timeout: 5000 }` changed nothing in the run.

Careful with the rule though. `isEnabled()`, `isChecked()` and `textContent()` do wait for the element to exist, they just answer about that one instant afterwards. `isVisible()` and `isHidden()` are the pair that never waits at all.

So assert when the element must be there. Read a boolean only when the state is already settled.

Engagement: 27 comments.

### 21. Stefan Minchev — Switch environments with one typed variable

- Source: https://www.linkedin.com/posts/stefan-minchev-qa_qa-playwright-softwaretesting-activity-7487889010883801088-yhnc
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-07-28.

One variable switches my whole suite from localhost to staging. No file editing.

If `BASE_URL` is the only value that changes and you switch twice a month, edit one `.env` by hand. That really is simpler.

It earns its place when the list grows. Staging wants its own URL, its own user, its own token, so every switch becomes a three-line edit you have to remember to undo.

Split the values by environment and keep the keys identical. `.env` carries everything shared, `.env.staging` only what differs.

Point dotenv at a list of files. The first wins, later files fill only the gaps:

```javascript
const env = process.env.TEST_ENV ?? 'local'
dotenv.config({ path: ['.env.' + env, '.env'] })
```

`.env.staging` supplies `BASE_URL`, `TEST_USER` falls through to `.env`.

Switching is `TEST_ENV=staging npx playwright test`, or `$env:TEST_ENV = "staging"` on PowerShell.

Wrap it in one script per environment and you never type it again. Follow-up comment details the catch: `"test:staging": "cross-env TEST_ENV=staging playwright test"` — `cross-env` is not decoration, because npm runs scripts through `cmd` on Windows where a bare `TEST_ENV=staging` prefix is not a command. Array paths need dotenv 16.4.1 or newer, and only `.env.example` ever gets committed.

### 22. Ivan Davidov — I Made Stefan Minchev Defend Every Decision in Our Playwright Framework

- Source: https://dev.to/idavidov13/i-made-stefan-minchev-defend-every-decision-in-our-playwright-framework-2dfa
- Retrieved: 2026-08-29
- Exa status: complete

By Ivan Davidov, August 24, 2026. Interview series "ArchQA, Unscripted".

"If you wipe the test folder tomorrow, the architecture is still sitting right there. The tests are just the cherry on top."

That's Stefan Minchev, four minutes into the first ArchQA, Unscripted episode ever recorded, calmly walking out of the trap set for him. A year ago, when the author had to design a Playwright framework that five QAs across five different teams would use every day on a trading platform, Stefan was the person he designed it with. We built the same thing. Time to defend it, decision by decision.

The conversation is split into four episodes.

## Round 1: What Is Test Architecture

Stefan's claim: your tests are the tip of the iceberg, maybe the last 10 to 15 percent of the work. Architecture is every decision you make before you ever open a spec file. The folder tree, the fixtures, the data isolation, the rules that stop bad code from being committed.

We landed fast on the real enemy, tribal knowledge. All the conventions that live only in senior heads, so a new engineer discovers them by breaking them in a pull request. Then Stefan said the quiet part about documentation:

> "Within a few months, your readme is just lying to everyone."

The episode walks a ladder from the weakest way to store your rules all the way to the strongest, including the rung called the best context you can give an LLM.

## Round 2: The Cost of Bad Architecture

Stefan's claim: skipping architecture doesn't save the work. It converts it into maintenance, and the bill lands somewhere between month one and month six. Green checkmarks start lying, half the suite turns red after one UI change, and eventually someone says the most dangerous phrase in software engineering, "nobody touches that file."

To the objection that this is just normal tech debt every codebase has:

> "There's a massive difference between the tech debt you choose and the tech debt you inherited."

Chosen debt is a tracked trade-off. Inherited debt compiles silently in the background because nobody ever set a boundary to violate.

He also prices out the classic offenders one by one — duplication, the God object, magic strings, weak assertions — and picks the single most expensive one to fix first if your suite is already a mess.

## Round 3: Architect Your Own Framework

The screen-share round, in which the framework decisions are defended live. The episode continues with further rounds on fixtures, data isolation, lint rules and conventions as architecture that exists before specs.

### 23. Angela Zelaya — Claude Code + Playwright CLI (repost)

- Source: https://www.linkedin.com/posts/angela-zelaya-b185b218b_getting-started-with-claude-code-and-playwright-activity-7490742116910870528-06b9
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post by Angela Zelaya, 2026-08-05. Reshare of a post by Michal Drajna pointing to Aswani Kumar's guide; Angela's own comment: "So good!"

> Stop burning millions of tokens streaming raw HTML into your AI agent context. 💸
>
> When using terminal-based AI agents to drive browser automation, sending massive DOM trees on every single step destroys your context window and spikes API costs. 🎭
> In his latest step-by-step guide, Aswani Kumar breaks down how to pair Claude Code with Microsoft's Playwright CLI. By storing DOM state locally as YAML snapshots and using reference-based interactions, you keep your agent sessions cheap, fast, and remarkably long-running. 🚀
>
> https://lnkd.in/diYY9qec

Engagement: 1 reaction.

### 24. Angela Zelaya — Playwright CLI UI Review and `generate-locator` (repost)

- Source: https://www.linkedin.com/posts/angela-zelaya-b185b218b_giving-ui-reviews-to-coding-agents-playwright-activity-7455966334925910016-Lt1E
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post by Angela Zelaya, 2026-05-01. Reshare of an official Playwright announcement.

> 🛠️ playwright-cli v0.1.9 — sharper tools for coding agents:
>
> 🎯 UI Review + highlight for visual confirmation
> 📦 drop files & clipboard data
> 🧭 generate-locator for stable refs
> 🧱 snapshot --boxes with bounding boxes
> 📬 --raw and --json everywhere
>
> New Video shows the UI Review in action: https://lnkd.in/e8RiV8as

Engagement: 10 reactions.

### 25. ScrollTest / Promode — Playwright Contract Testing with TypeScript: Day 46

- Source: https://scrolltest.com/playwright-contract-testing-typescript-day-46/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 8, 2026.

Playwright contract testing with TypeScript is the safety net to add when a UI suite keeps breaking because the backend response shape changed quietly. In this Day 46 tutorial, you build contract checks around Playwright API tests, validate JSON with Zod and AJV, connect those checks to UI flows, and avoid turning contract testing into another noisy pipeline.

## Why Playwright contract testing with TypeScript matters

Most Playwright tutorials focus on clicking buttons and checking visible text. That is useful, but it misses a painful class of failures: the UI receives a technically valid HTTP 200 response, but the JSON shape no longer matches what the frontend expects. The page may show a blank price, hide a button, display "undefined", or fail only for one account type.

Contract testing catches that earlier. A contract is an agreement about request fields, response fields, types, required values, status codes, and sometimes business invariants.

### Contract tests vs API tests vs E2E tests

Teams often use these names loosely. The difference kept simple:

**API test** — checks whether an endpoint behaves correctly for a request. It may assert status code, response body, database side effect, headers, and error handling. Example: create an order, then verify the API returns the new order with status `201`.

**Contract test** — checks whether the provider response still matches the consumer's expected shape and rules. Example: the frontend expects `order.id` as a string, `order.total.amount` as a number, and `order.status` as one of `CREATED`, `PAID`, or `CANCELLED`. If the backend changes `amount` to a string, the contract test fails before the UI silently formats it wrong.

**E2E test** — checks a user workflow through the browser. High confidence but costs more time and is harder to debug when the failure is buried behind five screens.

A practical split:

1. Use API tests for endpoint behavior and backend workflow checks.
2. Use contract tests for response shape, enum values, nullability, and backwards compatibility.
3. Use E2E tests for the few flows where the browser experience is the product risk.

## Project setup for contract checks

Start with a normal Playwright TypeScript project. Add Zod for runtime validation and AJV for JSON Schema validation. TypeScript gives two wins: test helpers become easier to read because response objects have declared shapes, and tools like Zod can infer TypeScript types from runtime schemas, so the same contract can validate real JSON and guide test code completion.

For scale context, the npm downloads API reported 208,655,630 last-month downloads for `@playwright/test`.

### 26. ScrollTest / Promode — API Contract Testing: REST + UI in One Test

- Source: https://scrolltest.com/api-contract-testing-playwright-rest-ui-one-test/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, May 14, 2026.

Most QA teams still treat API testing and UI testing as separate kingdoms. The API team owns the contract suite in Postman or REST Assured. The automation team owns the browser suite in Playwright or Selenium. They run on different schedules, report to different channels, and rarely share state. The result is slow feedback, duplicated setup code, and bugs that slip through the gap between a green API test and a broken user interface.

The author stopped accepting this separation two years ago. At Tekion, they now run API contract testing with Playwright as a single hybrid suite. One test creates a user via REST, validates the response against an OpenAPI schema, logs in through the browser using shared cookies, and confirms the dashboard renders the correct data. The test runs in 4 seconds. A pure UI version of the same flow takes 18 seconds. The hybrid version is also more reliable because it validates the contract before the browser ever opens.

## What Is API Contract Testing and Why It Matters

A contract test verifies that an API response matches a predefined schema. It does not test business logic. It tests structure. If the `/user` endpoint promises to return a field called `email` as a string, the contract test fails the moment that field becomes a number or disappears entirely.

This matters because frontend code depends on that structure. A missing `email` field does not just break an API test. It breaks the React component that renders the profile page, the TypeScript interface the component imports, and the form validation that expects a string.

### The Cost of Breaking Contracts in Production

Postman's 2025 State of the API Report found that 62% of organizations experienced an API-breaking change in production at least once per quarter. The average time to detect and rollback was 3.4 hours.

### Contract Testing vs Integration Testing

Many teams confuse the two. Integration testing asks: "Does the checkout flow work end to end?" Contract testing asks: "Does the `/checkout` response still contain `orderId`, `total`, and `status` with the correct types?" The questions are complementary. You need both. But contract tests are faster, more deterministic, and should run at every pull request.

- Integration tests: 200-2000ms per API call, validate logic and state.
- Contract tests: 20-80ms per response, validate schema and types.
- UI tests: 3000-8000ms per flow, validate user experience.

## Why Playwright Is the Right Tool for Hybrid API + UI Testing

Playwright is not just a browser automation framework. It ships with a full-featured `APIRequestContext` that can send HTTP requests, handle cookies, manage headers, and reuse authentication state with the browser context. This is not an afterthought; it was designed specifically for the hybrid pattern.

### 27. ScrollTest / Promode — TypeScript for Playwright Testing

- Source: https://scrolltest.com/typescript-for-playwright-testing/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, May 17, 2026.

## What Is TypeScript and Why Does It Matter for Test Automation?

TypeScript is JavaScript with a type system. That sounds simple, but the impact on test automation is massive. In JavaScript, this code runs fine until it explodes at runtime:

```javascript
const userName = 42; // accidentally a number
console.log(userName.toUpperCase()); // TypeError at runtime
```

In TypeScript, the same code fails before you even run the test:

```typescript
const userName: string = 42;
// Error: Type 'number' is not assignable to type 'string'
```

For QA engineers, this matters because test suites are not throwaway scripts. A mature Playwright project at a product company can easily contain 800-1,200 test cases, 40+ page object models, and multiple environment configurations. When a test fails at 2 AM in CI, you want to know it is a real product bug — not a typo in your selector string or a missing await.

The adoption numbers back this up. JetBrains' State of Developer Ecosystem 2024 report found that TypeScript adoption surged from 12% in 2017 to 35% in 2024.

### Static Types Are Living Documentation

When onboarding a new SDET, point them to the `page-objects/` folder. Because everything is written in TypeScript, the interfaces tell the story. A new hire can read `interface LoginPage` and immediately know which methods accept a `string`, which return a `Locator`, and which throw. No guesswork. No stale documentation.

## The Data: How TypeScript Catches Bugs Before They Ship

In 2017, researchers Zheng Gao, Christian Bird, and Earl T. Barr published an ICSE paper titled "To Type or Not to Type: Quantifying Detectable Bugs in JavaScript". They analyzed 400 public bugs across real JavaScript projects and manually annotated them with TypeScript and Flow types.

Their central finding: TypeScript 2.0 successfully detected 15% of public bugs — bugs that had already survived code review and manual testing. The researchers called this a conservative estimate.

Now apply that to test automation. If 15% of your test bugs — flaky selectors, wrong return types, missing awaits — never reach CI because TypeScript caught them at compile time, the author's experience is roughly 3-4 hours per week saved on a 12-person QA team.

### 28. ScrollTest / Promode — Playwright Suite Lying? 3 Signs Your Tests Pass Wrong

- Source: https://scrolltest.com/playwright-suite-lying-tests/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 11, 2026.

Your Playwright suite lying is not a dramatic phrase. It is what happens when a green CI run hides weak assertions, fake data, and missing error-state coverage. Playwright 1.62.1 shipped on July 30, 2026, and the tool is mature enough that most false confidence now comes from how teams write tests, not from the framework itself.

The `@playwright/test` 1.62.1 package crossed 204,135,164 npm downloads in the last month for the period ending August 9, 2026. The Microsoft Playwright GitHub repository also shows 94,316 stars and 6,262 forks at the time of checking. That adoption is a signal: teams trust the tool. But trust in a tool is not the same thing as trust in your test design.

## Why Green Playwright Tests Still Lie

A green build means only one thing: the executed checks passed under the conditions you gave them. It does not prove the user journey works, the backend contract is stable, or the failure state is usable.

### Playwright is not the problem

The official Playwright assertions documentation gives auto-retrying assertions such as `toBeVisible()`, `toHaveText()`, `toHaveURL()`, and `toHaveCount()`. These are strong primitives. The problem starts when a suite uses those primitives to check only that something exists, without checking whether the right business outcome happened.

### False confidence is expensive

False confidence wastes more time than a red build. A red build blocks the team quickly. A fake green build waits until staging, production, or a customer support ticket. By then, the SDET team is not debugging one test. They are debugging trust.

That is why one blunt question during test review helps: if this assertion passes, what user or business risk did we actually reduce? If nobody can answer in 10 seconds, the test is probably checking implementation noise.

### The 3 signs

- Sign 1: The test asserts presence, not outcome.
- Sign 2: The mock is so complete that the real product disappears.
- Sign 3: The suite never checks network failures, timeouts, empty states, or 500 responses.

## Sign 1: Weak Assertions Create Fake Confidence

The test has steps, locators, and a final expect. But the expect is too shallow to catch the bug that matters:

```typescript
import { test, expect } from '@playwright/test';

test('user can complete payment', async ({ page }) => {
  await page.goto('/checkout');
  await page.getByLabel('Card number').fill('4242424242424242');
  await page.getByRole('button', { name: 'Pay now' }).click();

  // Weak: a banner can appear even when the order was not created.
  await expect(page.getByText('Success')).toBeVisible();
});
```

This test can pass even if the order ID is missing, the amount is wrong, the receipt email is not queued, or the UI displays the wrong state.

### 29. ScrollTest / Promode — Playwright Retries and Flaky Tests: Day 42

- Source: https://scrolltest.com/playwright-retries-flaky-tests-day-42/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 3, 2026.

Playwright retries are useful, but they become dangerous when teams use them to hide broken test design. Day 42 of the Playwright + TypeScript series is about a better pattern: retry once, collect evidence, classify the failure, and quarantine only the tests that deserve quarantine.

Many QA teams add `retries: 2` in `playwright.config.ts` and call the problem solved. The build becomes green for a week, then the same product area starts failing at 2 AM during release night. Retries should buy debugging signal, not silence.

## Why Playwright retries exist

The official Playwright retries documentation describes retries as automatic re-runs for tests that fail intermittently. That wording matters. A retry is for intermittent failure investigation, not for a selector that is permanently wrong or a test that asserts the wrong business rule.

Playwright classifies outcomes in a way that helps managers read the build. A test that passes on the first run is passed. A test that fails on the first run and passes on retry is flaky. A test that fails on the original run and all retries is failed. That distinction gives you a clean triage queue.

### Retries are not a quality strategy

A quality strategy answers questions like these:

- Which user journeys must block a release?
- Which failures need immediate engineering ownership?
- Which tests are noisy because of environment, data, or timing?
- Which tests should move to API, component, or unit level?

Playwright retries answer only one smaller question: if the test gets one more clean run, does it still fail? That is useful evidence, but it is not a fix.

### The signal to want from a retry

When a test passes after retry, three things should be knowable quickly:

1. Did the app eventually behave correctly, or did the assertion become weaker?
2. Did Playwright capture a trace, screenshot, video, or console log from the failed attempt?
3. Can this failure be grouped with other failures by feature, owner, or root cause?

This is why retries work best with a trace-first setup. If a retry gives a green build but no evidence from the failing attempt, the pain only moved to tomorrow.

## A practical retry policy for TypeScript projects

The default policy is boring: no retries locally, one retry in CI, and trace capture on the first retry.

```typescript
// playwright.config.ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  timeout: 30_000,
  expect: {
    timeout: 7_000,
  },
  retries: process.env.CI ? 1 : 0,
  workers: process.env.CI ? 4 : undefined,
  reporter: [
    ['list'],
    ['html', { outputFolder: 'playwright-report', open: 'never' }],
    ['json', { outputFile: 'test-results/results.json' }],
  ],
  use: {
    baseURL: process.env.BASE_URL ?? 'http://localhost:3000',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
});
```

A trace is not only a screenshot. It shows actions, DOM snapshots, network calls, console messages, and timing — exactly what is needed when the first attempt fails and the retry passes.

### Use project-level retry overrides when needed

Some teams test against multiple projects: Chromium, Firefox, WebKit, mobile viewport, staging, and production smoke. Not every project gets the same retry budget. A production smoke suite should be stricter. A full staging regression suite may allow one retry while the team is stabilizing infrastructure.

### 30. ScrollTest / Promode — Playwright Flaky Test Audit Template

- Source: https://scrolltest.com/playwright-flaky-test-audit-template/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 9, 2026.

A Playwright flaky test audit is the fastest way to find the tests that pass locally, fail in CI, and waste the team's trust. Playwright 1.62.1 shipped as a bug-fix release on 30 July 2026, and that is exactly the type of moment to inspect traces, selectors, retries, and network evidence instead of blindly bumping a package.

This guide gives a working audit template, TypeScript examples, and a CI checklist to copy into a real QA repo. Flakiness is treated not as bad luck but as a signal that the test is missing an explicit contract.

## Why a Playwright flaky test audit matters now

The dirty secret with modern browser automation is simple: a green Playwright report can still hide a weak test. Teams celebrate a pass rate without asking whether the test actually observed the business risk. That is how retries become a painkiller instead of a diagnosis.

The adoption numbers make this more important. The npm downloads API reports more than 208 million downloads for `@playwright/test` in the last-month window ending 6 August 2026, and the Microsoft Playwright GitHub repository shows more than 94,000 stars.

A Playwright flaky test audit is not a rewrite project. It is a focused inspection of evidence. Pick the tests that failed or retried, open the trace, inspect what the test actually waited for, and classify the cause. The goal is to make the next run more trustworthy, not to add another dashboard nobody reads.

### False confidence costs more than red builds

A red build is annoying, but at least it asks for attention. A flaky green build is worse because it trains the team to ignore risk. If the checkout test passes because the button happened to be visible, but never verifies the order API response, the test is not protecting revenue.

This is why a small audit spreadsheet beats vague labels like "unstable test". Each row must name the failure mode, owner, evidence link, and fix. If there is no evidence, the first action is not a fix. The first action is better instrumentation.

### Where to start

- Audit the tests that retried, not the tests people complain about most.
- Keep the evidence link beside the defect or pull request.
- Separate product bugs, test bugs, data bugs, and infrastructure bugs.
- Do not raise retry count until classification is complete.

## What Playwright 1.62.1 tells QA teams

The Playwright 1.62.1 release notes list bug fixes around TypeScript project resolution, accessibility snapshots, branded primitive type checking in `page.evaluate()`, and image-type actionable elements in snapshots. A serious SDET reads a release note as a test input: if the release mentions snapshots, check snapshot assertions; if it mentions TypeScript resolution, check monorepo projects.

### 31. ScrollTest / Promode — Browser Contexts: Test Isolation in TypeScript: Day 52

- Source: https://scrolltest.com/playwright-browser-contexts-typescript-day-52/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 16, 2026.

A browser context is an isolated, incognito-style profile — its own cookies, localStorage, sessionStorage, cache, and viewport — and Playwright spins one up fresh for every test by default. This tutorial shows how contexts work in TypeScript, how to build multi-user tests, share login state, emulate devices, and the five traps that quietly leak state between tests.

## What Is a Browser Context?

A browser context in Playwright is the closest thing to opening a fresh incognito window — but it is an API, not a click. The official docs define it this way: "BrowserContexts provide a way to operate multiple independent browser sessions." Every context has its own cookies, local storage, session storage, and cache, and Playwright guarantees they never bleed into each other, even when they share a single underlying browser process.

The mental model: a browser is a heavyweight process. A context is a lightweight sandbox inside that process. A page is a single tab inside a context. When a test receives `page` from the Playwright test fixture, it is actually getting a page that lives inside a brand-new context created just for that test.

```typescript
import { test, expect } from '@playwright/test';

test('example test', async ({ page, context }) => {
  // "context" is an isolated BrowserContext, created for this specific test.
  // "page" belongs to this context.
  await page.goto('https://example.com');
  await expect(page).toHaveTitle(/Example Domain/);
});

test('another test', async ({ page, context }) => {
  // "context" and "page" in this second test are completely
  // isolated from the first test.
  await page.goto('https://example.com');
  await expect(page).toHaveTitle(/Example Domain/);
});
```

Two tests, two contexts, zero shared state. If the first test sets a cookie, the second test never sees it.

## Why Isolation Is a Superpower

Teams have fought test-order dependencies for years in Selenium, running "cleanup" methods after every test to delete cookies — only to discover you cannot clean up visited-link state, it is baked into the browser's history. Playwright's answer is simpler and harsher: start from scratch every time instead of trying to clean up in between.

Isolation buys three concrete things:

- No failure carry-over. If one test fails, it does not drag the next test down with it.
- Easy debugging. A single failing test can be rerun a hundred times and gets the same clean slate every time.
- Order-independence under parallelism. When sharding across workers or machines, test 3 and test 47 can run at the same time because they share nothing.

The cost is near zero. Contexts reuse an existing browser process, so spinning up a context costs milliseconds, not the seconds a full browser launch takes.

### 32. ScrollTest / Promode — Authentication: Day 10

- Source: https://scrolltest.com/playwright-authentication-day-10/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, June 18, 2026.

Playwright authentication is where a beginner suite usually becomes a real automation suite. Login is slow, login is fragile, and login is the first thing that breaks when every spec repeats the same UI steps.

Day 10 fixes that: create authenticated state once, save it with `storageState`, reuse it safely, and avoid the traps that make logged-in tests flaky in CI.

## Why Playwright authentication matters

The official Playwright authentication guide says Playwright runs tests in isolated browser contexts and can load an existing authenticated state to avoid signing in for every test. That one sentence explains the whole strategy. Isolation gives clean tests. Saved state gives speed.

This matters more as the suite grows. If 80 tests each spend 8 seconds on login, the suite wastes more than 10 minutes before it checks the product. Worse, the login page becomes a single point of failure. One OTP delay, one captcha rule, one slow identity provider, and the entire run looks broken.

### What changes after Day 10

- Stop putting login steps in every test.
- Separate authentication setup from product behavior checks.
- Keep admin, manager, and customer sessions isolated.
- Make CI runs faster without sharing unsafe state between workers.

Playwright is popular enough that this pattern is not niche: the npm downloads API reported 162,011,279 downloads for `@playwright/test` from 2026-05-18 to 2026-06-16, and the GitHub API showed 91,163 stars for `microsoft/playwright` during this run. Treat authentication as a first-class framework feature.

## The mental model: browser context plus storage state

Playwright authentication works because a browser context stores cookies, local storage, and related browser session data. When you call `context.storageState({ path })`, Playwright writes that state into a JSON file. Later, a test project can load that file through `use.storageState`.

Do not think of this as "skip login forever." Think of it as "login once per run, then reuse the same realistic browser state for tests that do not care about login behavior." If the login flow itself is the feature under test, write a normal login spec. If the dashboard is the feature under test, start from an already logged-in state.

### What storage state usually contains

- Session cookies for the app domain.
- Local storage values used by the frontend.
- Origin-specific state for the browser context.

It does not magically store everything. If the application uses server-side sessions, refresh tokens, short-lived cookies, or a custom WebAuthn flow, a setup flow that creates valid state is still needed. The saved file is a snapshot. When the backend invalidates that snapshot, the setup must run again.

### 33. ScrollTest / Promode — Performance Testing with TypeScript: Day 47

- Source: https://scrolltest.com/playwright-performance-testing-typescript-day-47/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 9, 2026.

Playwright performance testing is not a replacement for k6, JMeter, Lighthouse, or a full observability stack. It is the fast feedback layer to have inside every Playwright + TypeScript suite: page budgets, API timing, trace evidence, and CI gates that catch obvious slowness before it reaches staging sign-off.

Day 47 is about building that layer without turning end-to-end tests into fake load tests: measure the right browser signals, store evidence, fail only on stable budgets, and keep the suite useful for QA teams that already run Playwright daily.

## Why performance budgets belong in Playwright

A common mistake in automation teams is treating performance as a separate ceremony that happens late in the release. A QA engineer validates flows with Playwright. Another team runs load tests once a sprint. Nobody catches the login page that quietly moved from 1.8 seconds to 5.6 seconds after a new analytics script.

That gap is where Playwright performance testing helps. It does not prove that the platform handles 20,000 concurrent users. It proves that the single-user journey did not become slower, heavier, or noisier than the budget the team agreed to.

### What Playwright can measure well

- Navigation time for important pages like login, search, checkout, and reports.
- API response time for calls made during a user journey.
- Resource count and size for JavaScript, CSS, images, fonts, and third-party scripts.
- Basic web-vital-style signals collected from the browser.
- Regression evidence through trace files and screenshots.

### What Playwright should not pretend to measure

Do not sell this as load testing. A Playwright browser session is expensive compared with protocol-level load tools. Running 500 browsers to simulate traffic is usually wasteful, unstable, and hard to analyze. If sustained load is needed, use a load tool and feed the result into the release gate separately.

The clean model: Playwright checks the customer journey budget. Load tools check system behavior under volume. Observability checks production and staging telemetry. These three layers support each other.

## Setup

Use a separate performance project in `playwright.config.ts` so the checks do not slow every PR run:

```bash
mkdir -p tests/performance
npm init playwright@latest
npm install -D @playwright/test
```

Playwright's official timeout documentation gives a default test timeout of 30,000 ms in Playwright Test — useful for avoiding stuck tests and relevant for stable timing checks.

### 34. ScrollTest / Promode — iFrames in TypeScript: Day 50

- Source: https://scrolltest.com/playwright-iframes-frames-typescript-day-50/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 14, 2026.

QA engineers still lose entire afternoons to a single error: `waiting for getByPlaceholder('Card number') to be visible` on a payment page that loads fine in the browser. The culprit is almost always an iframe. Playwright treats iframes as separate documents, and a locator will never pierce that boundary unless explicitly told to.

## Why iFrames Break Your Test Automation

An iframe is a full HTML document embedded inside another HTML document. When you open a checkout page, the browser actually loads two (or more) separate documents: the parent page, and each iframe the parent references. The Stripe card field, the chat widget, the embedded video player — each is its own mini-page.

This matters for automation because of how the DOM and the JavaScript context are isolated. A locator like `page.getByPlaceholder('Card number')` searches only the top-level document. If that input lives inside an iframe, the locator returns nothing, Playwright keeps retrying until the timeout expires, and the test fails with an "element is not visible" or "element not found" error even though the element clearly rendered on screen.

This is the single most common reason a "working" selector suddenly fails in CI. The selector was never wrong. It was pointing at a document boundary the test did not cross. The worst part is that the failure looks like a normal timeout, so engineers waste time re-checking the selector, re-installing browsers, and blaming the environment before anyone looks at the iframe.

### The Frame Tree, Explained

Every page in Playwright exposes a frame tree. The top-level document is the main frame, and every iframe (and every iframe inside an iframe) is a child frame. Playwright gives two ways to reach these frames:

- `page.frames()` returns the full list of `Frame` objects, including the main frame.
- `page.frameLocator()` returns a `FrameLocator` that scopes every subsequent locator to a specific frame.

`frameLocator()` will almost always be the right choice. It composes cleanly with the locators you already know and keeps the test readable.

## How Playwright Sees Frames

In Selenium you switch into a frame with `driver.switchTo().frame()` and then remember to switch back. In Playwright there is no switching state to manage. A `FrameLocator` is a scoped locator: you create it once, and every call chained onto it stays inside that frame automatically.

That is a huge deal for readability and for avoiding the classic Selenium bug where a test forgets `switchTo().defaultContent()` and every later step runs inside the wrong context.

### frameLocator() vs page.locator()

The difference is scope, not syntax.

### 35. ScrollTest / Promode — Tags and Annotations: Day 48

- Source: https://scrolltest.com/playwright-tags-annotations-typescript-day-48/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 11, 2026.

Playwright tags and annotations look small until your suite crosses 200 tests. Then tags decide which checks run on every pull request, which tests block a release, which known bugs stay visible, and which flaky tests need owner action instead of silent retries.

This Day 48 tutorial shows how to structure Playwright tags, built-in annotations, custom annotations, grep filters, and CI commands in a TypeScript framework. The taxonomy is kept boring on purpose, because boring labels are easier to trust at 2 AM when a release is waiting.

## Why Playwright Tags and Annotations Matter

A useful suite answers a release question: what should run now, why did it run, who owns the failure, and should the pipeline stop?

The official Playwright annotations documentation says Playwright supports tags and annotations that appear in test reports. In practice, it is a design hook for the whole automation operating model.

### Tags are selection, annotations are explanation

- Tags decide which tests are selected or excluded.
- Built-in annotations control test behavior such as skip, fail, slow, and fixme.
- Custom annotations add context for humans and reports.
- Projects decide browser, device, environment, and dependency shape.

If you mix these ideas, the framework becomes hard to reason about. For example, `@smoke` is a selection label. `test.skip()` is a behavior decision. `owner: checkout-team` is reporting context. They should not compete with each other.

### The release question

Good tags make three questions cheap:

1. What is the smallest set that protects this pull request? Usually `@smoke` and a few feature tags.
2. What blocks production? Usually `@release`, `@critical`, and selected API checks.
3. What needs analysis but should not block yet? Usually `@quarantine`, known defects, and unstable experiments.

## Built-in Annotations to Use Carefully

Playwright documents built-in annotations such as `test.skip()`, `test.fail()`, `test.fixme()`, and `test.slow()`. They keep intent close to the test instead of hiding policy in a spreadsheet.

Use `skip` for impossible execution, not for uncomfortable failures: a feature may not exist in Firefox yet, a payment sandbox may not support a region, a mobile-only test may not make sense on desktop.

### 36. ScrollTest / Pramod Dutta — Network Interception: Day 8 (updated)

- Source: https://scrolltest.com/21-day-playwright-day-8-network-interception-mock-api/
- Retrieved: 2026-08-29
- Exa status: complete

By Pramod Dutta, August 17, 2026 (series update). Day 8 of the 21-Day Playwright with TypeScript Challenge.

`page.route()` intercepts every network request the app makes. Mock APIs, block analytics, simulate server errors — all without changing application code.

## Mock API Responses

```typescript
test('mock user API', async ({ page }) => {
  await page.route('**/api/users', route => {
    route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify([{ id: 1, name: 'Mock User' }])
    });
  });
  await page.goto('/users');
  await expect(page.getByText('Mock User')).toBeVisible();
});
```

## Simulate Server Errors

```typescript
test('handle 500 error gracefully', async ({ page }) => {
  await page.route('**/api/orders', route => {
    route.fulfill({ status: 500, body: 'Internal Server Error' });
  });
  await page.goto('/orders');
  await expect(page.getByText('Something went wrong')).toBeVisible();
  await expect(page.getByRole('button', { name: 'Retry' })).toBeVisible();
});
```

## Block Resources for Speed

```typescript
test('fast page load without images', async ({ page }) => {
  await page.route('**/*.{png,jpg,gif,svg}', route => route.abort());
  await page.route('**/analytics/**', route => route.abort());
  await page.route('**google-analytics**', route => route.abort());
  await page.goto('/dashboard'); // 3x faster
});
```

## Wait for Specific Responses

```typescript
test('wait for search results', async ({ page }) => {
  const responsePromise = page.waitForResponse('**/api/search**');
  await page.goto('/search');
  await page.getByPlaceholder('Search').fill('playwright');
  await page.getByRole('button', { name: 'Search' }).click();
  const response = await responsePromise;
  expect(response.status()).toBe(200);
});
```

### 37. ScrollTest / Promode — Mobile Testing and Device Emulation: Day 49

- Source: https://scrolltest.com/playwright-mobile-testing-typescript-day-49/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 13, 2026.

Playwright mobile testing is the fastest way to catch mobile-only bugs without buying a single device. When a checkout flow on an iPhone 13 profile breaks because a button renders off-screen, that is a bug a desktop run would never find. This Day 49 tutorial walks through device emulation in TypeScript: the built-in device registry, viewports, geolocation, permissions, touch gestures, and the honest limits of emulation versus a real phone.

## What Is Playwright Mobile Testing?

Playwright mobile testing means running the same Chromium, WebKit, or Firefox engine with a mobile profile attached, so the page renders and behaves the way it does on a phone. The engine is still a desktop browser under the hood. Playwright swaps four things at the context level: the user agent string, the viewport size, the device scale factor, and two flags, `isMobile` and `hasTouch`. Together those five fields convince the page that it is running on a phone, so responsive CSS breakpoints, touch-only handlers, and mobile-specific redirects all fire correctly.

Teams often conflate two ideas and it costs them. Device emulation changes how the page sees the device. A viewport resize only changes the window width. You can set `viewport: { width: 390, height: 844 }` and still miss the bug, because the page never receives a mobile user agent, so the server serves the desktop layout. If you want the real mobile experience in a test, use a device descriptor, not just a width.

## The Device Registry: iPhone, Pixel, and Galaxy Profiles

Playwright ships a built-in registry of device descriptors. You do not hand-write user agent strings or guess viewport sizes. You import `devices` and reference a profile by name:

```typescript
import { devices } from '@playwright/test';

console.log(devices['iPhone 13']);
// {
//   userAgent: 'Mozilla/5.0 (iPhone; CPU iPhone OS 15_0 like Mac OS X) ...',
//   viewport: { width: 390, height: 844 },
//   deviceScaleFactor: 3,
//   isMobile: true,
//   hasTouch: true,
//   defaultBrowserType: 'webkit'
// }
```

### Reading a device descriptor

Each descriptor has six fields to know cold:

- userAgent: the exact UA string the device sends. The server uses this to serve mobile markup.
- viewport: the CSS pixel width and height. iPhone 13 is 390×844, Pixel 5 is 393×851.
- deviceScaleFactor: the DPR. iPhone 13 reports 3, which matters for canvas and image rendering tests.
- isMobile: flips the `navigator.userAgentData` and mobile media-query behavior.
- hasTouch: enables touch event dispatch and the `page.touchscreen` API.
- defaultBrowserType: which engine the device normally uses. iPhones map to WebKit, Pixels to Chromium.

### Which devices ship out of the box

The registry covers the phones and tablets users actually carry: iPhone 12 and 13 families, Pixel 5 and 7, Galaxy S8 through S20, several iPad generations including iPad Pro 11, and desktop profiles like Desktop Chrome and Desktop Safari for parity runs.

### 38. ScrollTest / Pramod Dutta — Date Pickers and Calendars

- Source: https://scrolltest.com/playwright-date-picker-calendar-testing/
- Retrieved: 2026-08-29
- Exa status: complete

By Pramod Dutta, July 11, 2026.

Date pickers and calendar widgets are quietly one of the flakiest things to automate. They render dates dynamically based on "today", they hide the actual input behind a styled popup, and the cell you want to click might live in a different month than the one currently shown. This guide covers reliable patterns for Playwright date picker testing in TypeScript: typing into native inputs, navigating month-by-month, freezing "today" with the clock API, and writing helpers that never break when the calendar advances to a new month.

## Why Date Pickers Break Automated Tests

Most calendar bugs in test suites come from three sources. First, the widget renders relative to the system clock, so a test that hardcodes "click June 15" passes in June and fails in July. Second, the visible date grid only shows one month at a time, so blindly clicking a day cell fails when the target date is in a future or past month. Third, many UI libraries (React-DatePicker, MUI, Ant Design, Flatpickr) render the popup in a portal at the end of the body, so the calendar cells are not DOM children of the input you clicked.

The fix is to treat the calendar like any other stateful component: control the inputs you can, navigate deterministically, and freeze the clock so "today" is always the same.

## Strategy 1: Skip the Calendar and Fill the Native Input

The single most underused technique is to ignore the calendar popup entirely. Native HTML date fields, and many custom widgets, accept a typed value directly. This is the fastest and least flaky approach because it skips all the month navigation:

```typescript
import { test, expect } from '@playwright/test';

test('fill a native date input directly', async ({ page }) => {
  await page.goto('https://example.com/booking');

  const dateInput = page.getByLabel('Check-in date');

  // Native <input type="date"> accepts an ISO yyyy-mm-dd string.
  await dateInput.fill('2026-07-15');

  // Assert the committed value rather than the rendered popup.
  await expect(dateInput).toHaveValue('2026-07-15');
});
```

If the widget is a custom one backed by a text input that ignores `fill()` (some libraries only react to real keystrokes), use `pressSequentially()` to type character by character and trigger the library's keydown handlers, then commit with Enter:

```typescript
test('type into a custom text-based date field', async ({ page }) => {
  const input = page.getByPlaceholder('MM/DD/YYYY');

  await input.click();
  await input.pressSequentially('07/15/2026', { delay: 50 });
  await input.press('Enter');

  await expect(input).toHaveValue('07/15/2026');
});
```

Always prefer this path when the application accepts it. Reserve clicking through the calendar grid for cases where the input is truly read-only and the popup is the only way to pick a date.

## Strategy 2: Navigate the Calendar Month by Month

When you must use the popup, never assume the target month is visible. Read the calendar's current month/year header, compare it to your target, and click the "next" or "previous" arrow until they match:

```typescript
import { type Page, expect } from '@playwright/test';

const MONTHS = [
  'January', 'February', 'March', 'April', 'May', 'June',
  'July', 'August', 'September', 'October', 'November', 'December',
];

async function pickDate(page: Page, target: Date) {
  await page.getByLabel('Departure date').click(); // open the popup

  const header = page.locator('.calendar__header-title');
  const wantLabel = `${MONTHS[target.getMonth()]} ${target.getFullYear()}`;

  // Navigate until the header matches the target month/year.
  for (let i = 0; i < 36; i++) {
    const current = await header.innerText();
    // ...compare and click next/previous until they match
  }
}
```

### 39. ScrollTest / Pramod Dutta — Toast Notifications and Timing

- Source: https://scrolltest.com/playwright-toast-notification-testing/
- Retrieved: 2026-08-29
- Exa status: complete

By Pramod Dutta, July 23, 2026.

Toast notifications are some of the hardest UI elements to test reliably: they pop in for two or three seconds, animate, and vanish before the assertion even runs. A test that passes on a fast laptop fails on a loaded CI runner because the toast disappeared while Playwright was still scrolling. This guide covers a robust approach to Playwright toast notification testing, including how to catch short-lived toasts, assert their text and ARIA role, freeze auto-dismiss timers with the Clock API, and stack multiple toasts without flakiness.

## Why toasts are so flaky to test

A toast (also called a snackbar, flash message, or notification) is a small, transient message that confirms an action, like "Profile saved" or "Item added to cart." The properties that make toasts good UX are exactly what make them hard to automate:

- They auto-dismiss. Most toasts disappear after 2,000 to 5,000 milliseconds, so there is a tight race between the toast appearing and the assertion running.
- They animate. Slide-in and fade-out transitions mean the element exists in the DOM before it is visible and lingers after it is functionally gone.
- They are removed from the DOM. Unlike a modal, the toast node is usually destroyed on dismiss, so a stale locator throws errors.
- They stack and replace. Fire three actions quickly and you may get three toasts, one merged toast, or a queue, depending on the library.

The naive fix, sprinkling `page.waitForTimeout(3000)` everywhere, is the single biggest cause of flaky toast tests. It is slow, and it still races: if the runner stalls for 200ms, the toast is gone before the check.

## Capture the toast with auto-retrying web-first assertions

The first rule is to use web-first assertions like `expect(locator).toBeVisible()` instead of manual waits. These assertions auto-retry until the condition is met or the timeout expires, so they latch onto the toast the instant it appears.

The most stable way to locate a toast is by its ARIA role. Accessible toast libraries render the container with `role="status"` (polite) or `role="alert"` (assertive), which is also what screen readers announce. Targeting the role means the test does not break when someone changes a CSS class:

```typescript
import { test, expect } from '@playwright/test';

test('shows a success toast after saving', async ({ page }) => {
  await page.goto('/settings/profile');
  await page.getByLabel('Display name').fill('Ada Lovelace');
  await page.getByRole('button', { name: 'Save' }).click();

  // Prefer the ARIA role: status (polite) or alert (assertive).
  const toast = page.getByRole('status').filter({ hasText: 'Profile saved' });

  // Auto-retrying assertion: latches on the moment the toast appears.
  await expect(toast).toBeVisible();
  await expect(toast).toHaveText(/Profile saved/);

  // Optionally confirm it auto-dismisses afterwards.
  await expect(toast).toBeHidden({ timeout: 6000 });
});
```

Notice there is no `waitForTimeout`. The `toBeVisible()` assertion polls quickly (roughly every 100ms) until the toast renders, and `toBeHidden()` confirms the auto-dismiss behavior with an explicit timeout that comfortably exceeds the toast's lifetime. If the toast library does not set an ARIA role, fall back to a stable test id such as `page.getByTestId('toast')` rather than a brittle class selector.

For toasts that auto-dismiss in 1,000ms or less, the article presents three reliable strategies in increasing order of power (including Clock API timer freezing).

### 40. ScrollTest / Pramod Dutta — Keyboard Navigation and Accessibility

- Source: https://scrolltest.com/playwright-keyboard-navigation-accessibility/
- Retrieved: 2026-08-29
- Exa status: complete

By Pramod Dutta, August 13, 2026.

Most teams ship UIs that work flawlessly with a mouse and silently break the moment a user reaches for the `Tab` key. Broken focus order, invisible focus rings, and modals that trap nobody are exactly the defects automated suites miss and real users hit first. This guide covers Playwright keyboard navigation accessibility testing end to end: driving the keyboard, asserting focus, verifying ARIA roles and the accessibility tree, and wiring in axe-core so a11y regressions fail CI instead of customers.

## Why Keyboard and Accessibility Testing Belongs in Playwright

Keyboard operability is the backbone of accessibility. If every interactive control can be reached and activated with `Tab`, `Shift+Tab`, `Enter`, `Space`, and arrow keys, a large slice of WCAG 2.2 Level A and AA is already satisfied. Screen readers, switch devices, and power users all ride on the same keyboard plumbing. Playwright is unusually well suited to this because it drives a real browser, exposes a true keyboard device, and ships first-class role and accessible-name locators that mirror how assistive technology perceives the page.

The three pillars: simulating keyboard input precisely, asserting that focus lands where it should, and validating the semantic accessibility tree, including an automated axe-core scan.

## Driving the Keyboard with page.keyboard and locator.press

Playwright gives two layers of keyboard control. `page.keyboard` dispatches keys at the page level (whatever currently has focus receives them), while `locator.press()` focuses a specific element first and then sends the key:

```typescript
import { test, expect } from '@playwright/test';

test('user can reach the submit button using only the keyboard', async ({ page }) => {
  await page.goto('https://example.com/signup');

  // Start focus on the first field, then Tab through the form.
  await page.getByLabel('Email').focus();
  await page.keyboard.press('Tab');   // -> Password
  await page.keyboard.press('Tab');   // -> Remember me checkbox
  await page.keyboard.press('Space'); // toggle the checkbox
  await page.keyboard.press('Tab');   // -> Submit button

  // The button should now hold focus and respond to Enter.
  const submit = page.getByRole('button', { name: 'Create account' });
  await expect(submit).toBeFocused();
  await page.keyboard.press('Enter');

  await expect(page.getByText('Welcome aboard')).toBeVisible();
});
```

A few real APIs worth knowing: `page.keyboard.press('Shift+Tab')` moves focus backward. `page.keyboard.down('Shift')` and `page.keyboard.up('Shift')` hold a modifier across several presses, useful for range selection. For typing visible text use `locator.pressSequentially()` (the modern replacement for the old `type()` method) when per-character `keydown`/`keyup` events are specifically needed; otherwise prefer `locator.fill()`.

### 41. ScrollTest / Pramod Dutta — Allure Reporting with Playwright: The Complete Guide

- Source: https://scrolltest.com/playwright-allure-reporting-complete-guide/
- Retrieved: 2026-08-29
- Exa status: complete

By Pramod Dutta, August 10, 2026.

The built-in Playwright HTML report is great for local debugging, but the moment you need rich dashboards, historical trends, severity grouping, and BDD-style steps that non-engineers can actually read, it starts to feel thin. Playwright Allure reporting fills that gap by turning test runs into an interactive report with attachments, categories, and trend graphs.

## Why use Allure instead of the default Playwright report?

The answer is audience and history. The default report is a per-run snapshot optimized for developers chasing a flaky test. Allure is optimized for teams and stakeholders who want to see how quality is trending over weeks, slice failures by feature or severity, and read a clean step-by-step narrative without opening a trace file.

| Capability | Default Playwright HTML | Allure |
| --- | --- | --- |
| Per-run results | Yes | Yes |
| Trace + video attachments | Yes | Yes |
| Historical trends across runs | No | Yes |
| Severity / feature grouping | No | Yes |
| BDD-style nested steps | Limited | Rich |
| Failure categories (flaky vs product bug) | No | Yes |
| Requirement / TMS links | No | Yes |

In practice many teams keep both: the Playwright report for fast local triage and Allure as the shareable system of record published to CI. They are not mutually exclusive, and Playwright lets you register multiple reporters at once.

## Installing and configuring the Allure Playwright reporter

Allure has two moving parts. The `allure-playwright` npm package is the reporter that runs inside Playwright and writes raw result files (one JSON per test) into an `allure-results` folder. The Allure command line tool then converts those raw results into the browsable HTML report.

```bash
# Reporter that plugs into Playwright
npm install --save-dev allure-playwright

# Allure CLI to generate and open the report
# (Java 8+ must be on PATH; the CLI is a Java app)
npm install --save-dev allure-commandline
```

Register the reporter in `playwright.config.ts`:

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  retries: process.env.CI ? 2 : 0,
  reporter: [
    ['line'],
    ['allure-playwright', {
      detail: true,
      resultsDir: 'allure-results',
      suiteTitle: true,
      environmentInfo: {
        framework: 'Playwright',
        node_version: process.version,
        os: process.platform,
      },
    }],
  ],
  use: {
    baseURL: 'https://playwright.dev',
    trace: 'on-first-retry',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
    { name: 'firefox', use: { ...devices['Desktop Firefox'] } },
  ],
});
```

Allure automatically picks up the artifacts Playwright produces and attaches them to the corresponding test in the report, so a failing test ships with its trace, screenshot, and video without any extra wiring.

### 42. ScrollTest / Promode — Release Notes Checklist

- Source: https://scrolltest.com/playwright-release-notes-checklist/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, June 27, 2026.

Playwright release notes look harmless until one line turns into a broken CI run. Teams upgrade Playwright, skip the release-note review, and then spend the next standup explaining why screenshots, traces, UI mode, or browser installs suddenly behave differently.

This guide gives a practical release-note review checklist for QA teams, built around Playwright's recent releases, real commands, and the exact questions an SDET should answer before the upgrade reaches the main branch.

## Why Playwright Release Notes Break Test Suites

If a team uses Playwright, those notes are not marketing copy. They are risk signals.

Playwright moves fast. The official GitHub releases page shows recent versions such as v1.61.1 on 23 June 2026, v1.61.0 on 15 June 2026, v1.60.0 on 11 May 2026, and v1.59.0 on 1 April 2026. It is also not a small tool anymore: about 91,700 GitHub stars and 168,373,938 npm downloads for `@playwright/test` in the last-month window ending 24 June 2026.

### Release notes are executable risk

A release note becomes real when it touches one of these areas:

- Test runner behavior such as fixtures, retries, sharding, or reporters.
- Browser behavior such as Chromium, Firefox, WebKit, permissions, or storage.
- Debugging behavior such as trace viewer, UI mode, screenshots, and video.
- Node.js runtime behavior such as ESM loaders and TypeScript resolution.
- New APIs that invite teams to rewrite existing helpers too early.

The danger is not only a breaking change. Many painful upgrades are caused by bug fixes: a fix can expose an assumption in the framework that has been wrong for six months.

### Patch releases deserve respect

Teams often treat patch versions as safe. A patch can still touch the runner, trace viewer, loader, browser process spawning, or assertion behavior. For example, the v1.61.1 release notes list fixes related to expect matcher overrides, UI mode API request reporting, trace viewer websocket message timing, and a Node 22.15 loader regression. Those sit directly in the test feedback loop.

### Minor releases create adoption pressure

Minor releases are where the shiny features arrive: v1.61.0 introduced a Credentials virtual authenticator for WebAuthn passkey testing; v1.60.0 added `tracing.startHar()`/`tracing.stopHar()`; v1.59.0 introduced `page.screencast`. Each feature also creates a decision: adopt now, wrap it in the framework, or wait for one more patch.

## The 5-Minute Release-Notes Triage

Classify the upgrade type first:

1. Patch: v1.61.0 to v1.61.1. Expect fixes, but inspect runner-affecting items.

### 43. ScrollTest / Promode — Playwright 1.62 Upgrade Checklist

- Source: https://scrolltest.com/playwright-1-62-upgrade-checklist/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 1, 2026.

The Playwright 1.62 upgrade checklist is not just a version-bump note for package.json. This release changes component testing, retry behavior, screenshot formats, cancellation control, reporter hooks, browser versions, MCP packaging, and a few CI assumptions that can quietly break a stable test suite.

The author treats every Playwright upgrade like a small release project: the blast radius covers browser binaries, traces, visual baselines, fixtures, Docker images, and the habits of every SDET who debugs failures at 2 AM.

## Why Playwright 1.62 Matters

Microsoft published Playwright v1.62.0 on GitHub on 24 July 2026. The npm downloads API reported 199,962,685 downloads for `@playwright/test` in the 2026-07-01 to 2026-07-30 window. The GitHub API also shows the Microsoft Playwright repository with 93,776 stars and 6,186 forks at the time of checking. A release this widely adopted gets copied into enterprise frameworks very quickly.

### What changed in plain English

The official release notes list five items that matter most to a QA lead: new component testing stories and galleries, AbortSignal support for most operations and assertions, WebP screenshot support, reporter preprocessing, and isolated retries. Browser versions: Chromium 151.0.7922.34, Firefox 153.0, WebKit 26.5, plus stable Chrome 151 and Edge 151.

That is enough surface area to justify a controlled rollout. If the suite only runs login and checkout, the bump can happen faster. If the team owns visual regression, component testing, CI sharding, mobile emulation, or trace review, do not merge the bump on Friday evening.

## The 5-Check Playwright 1.62 Upgrade Checklist

One rule: test the behavior that changed, not the marketing headline. Version numbers do not fail builds. Mismatched browser binaries, different retry timing, new screenshot encodings, and stale component-test helpers fail builds.

The short version:

1. Component fixtures: run every component spec that uses `mount()`, providers, mocks, or story-like data.
2. Abort behavior: test long waits, flaky API waits, and custom timeout wrappers with `AbortController`.
3. Visual baselines: decide whether WebP belongs in the snapshot strategy before it leaks into PRs.
4. Retries: compare `retryStrategy: 'immediate'` and `retryStrategy: 'isolated'` on known flaky tests.
5. CI image: confirm browser versions, Docker base image, Debian 11 usage, reporters, and MCP commands.

Preflight command set:

```bash
git checkout -b chore/playwright-1-62-upgrade
npm view @playwright/test version
npm install -D @playwright/test@1.62.0
npx playwright install --with-deps
npx playwright --version
npx playwright test --list
```

Start with a clean branch and make the update visible in one commit, so rollback is easy when a browser binary or snapshot format surprises the team.

### 44. ScrollTest / Promode — Playwright 1.62 Regression Checklist

- Source: https://scrolltest.com/playwright-1-62-regression-checklist/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 1, 2026.

The Playwright 1.62 regression checklist is the upgrade guardrail to run before bumping `@playwright/test` in a real CI pipeline. Playwright upgrades are usually smooth, but browser binaries, trace behavior, fixtures, projects, and retries can expose weak assumptions that older suites quietly carried for months.

## Why this regression checklist matters

Playwright is not a tiny test helper anymore: more than 93,000 GitHub stars, and 199,962,685 npm downloads for `@playwright/test` during July 2026. Many teams now treat Playwright upgrades like a minor dependency bump when they should treat them like a controlled browser-platform release.

### What can break even when your code compiles?

Most Playwright upgrade issues are not TypeScript compiler errors. They are runtime mismatches:

- Browser binary versions can change screenshots, timing, media permissions, and download behavior.
- Reporter output can move or become too large for the artifact retention policy.
- Retries can hide a real regression if the team only checks the final green status.
- Project dependencies can mask setup failures when the config is too clever.
- CI cache keys can reuse old browsers with new test code.

A test waits for a network call that now finishes earlier. A fixture that was safe in serial mode starts leaking state in parallel. A trace is generated but not attached to the CI artifact. A browser install works on one runner image but fails on the older Linux image used by another team.

## Lock the upgrade baseline before touching tests

Do not upgrade from a messy baseline. If the suite is already flaky, slow, or full of skipped tests, 1.62 becomes the scapegoat for problems that existed last week. Freeze the current state so before and after can be compared with evidence.

Capture the current version and browser state on the same CI image that executes the production suite:

```bash
node --version
npm ls @playwright/test
npx playwright --version
npx playwright install --dry-run || true
npx playwright test --list > /tmp/pw-before-tests.txt
```

Commit a tiny upgrade note in the repo (e.g. `docs/qa/playwright-upgrade-1.62.md`) with the previous version, target version, CI image name, node version, and links to the before/after reports.

### Make skipped tests visible

Skipped tests are dangerous during framework upgrades because they create false confidence. Add skipped-test counting to the upgrade gate:

```bash
npx playwright test --reporter=json > results-before.json
node scripts/count-playwright-statuses.js results-before.json
```

```javascript
// scripts/count-playwright-statuses.js
const fs = require('fs');
const report = JSON.parse(fs.readFileSync(process.argv[2], 'utf8'));
const counts = { passed: 0, failed: 0, skipped: 0, timedOut: 0, interrupted: 0 };
```

### 45. ScrollTest / Promode — Release Smoke Suite for 1.62.1 Upgrades

- Source: https://scrolltest.com/playwright-release-smoke-suite-1621/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 3, 2026.

The author treats every Playwright upgrade like a release candidate, not a package bump. A Playwright release smoke suite gives QA teams a small, repeatable gate for Playwright 1.62.1 upgrades before the full regression suite burns CI minutes.

The point is simple: validate the risk areas the release touched, keep the suite under ten minutes, and collect enough trace evidence to make the upgrade decision clear.

## Why a Playwright Release Smoke Suite Matters

Most teams upgrade Playwright in one of two bad ways. They either pin the package for months and then panic when a browser change forces an upgrade, or they let Dependabot open a pull request and hope the normal regression suite catches everything.

Playwright is stable, but it sits close to browsers, TypeScript, selectors, accessibility snapshots, tracing, and CI images. A release can be technically correct and still expose weak spots in the test framework.

### The upgrade risk is not only browser behavior

Playwright 1.62.1 is a patch release with concrete bug fixes. The official GitHub release notes list regressions around TypeScript project reference resolution, bare specifier handling for tsconfig extends, branded primitive type checking in page.evaluate arguments, and accessibility snapshot behavior.

Those are not exotic edge cases. Monorepos use tsconfig project references. Design systems often wrap accessible names inside nested spans and SVG icons. Senior SDETs pass typed fixtures into page.evaluate.

### A smoke suite protects upgrade confidence

A good release smoke suite answers four questions fast:

- Can the Playwright test runner start in the same CI image used daily?
- Do the core fixtures, auth setup, and project configuration still load?
- Do selectors, accessibility checks, and trace collection behave as expected?
- Can a failure be explained in less than 15 minutes?

This is different from a product smoke suite. Product smoke tests ask, "Can users still perform critical flows?" A Playwright release smoke suite asks, "Can our automation platform still be trusted after this tool upgrade?"

### Why the suite should stay small

The target is not full coverage. The target is fast signal. If the release gate takes 90 minutes, people will skip it during sprint pressure. The author prefers 12 to 18 tests, three browser projects, trace on first retry, and a hard timeout that makes failure visible.

### Release note items worth testing (1.62.1)

1. tsconfig extends resolution: bare specifiers should resolve through node_modules walk-up like tsc.
2. TypeScript project references: directory-form project references should resolve correctly.
3. Accessibility snapshot names: button names should not disappear when text is nested inside spans with aria-hidden SVG.
4. page.evaluate typing of branded primitives.

### 46. ScrollTest / Promode — Browser AI Testing: 1.62.1 Deep Dive

- Source: https://scrolltest.com/playwright-browser-ai-testing/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 15, 2026.

Playwright browser AI testing stopped being a side project the week Microsoft bundled an MCP server into the framework. Version 1.62.1, released on July 30, 2026, ships `npx playwright mcp`, a WebP-based visual regression path, and a Chromium 151 engine bump in a single release. For QA teams running browser tests, this is the release that turns spec files into something an AI agent can actually drive.

## What Shipped in Playwright 1.62.1

Playwright 1.62.0 landed on July 24, 2026, and 1.62.1 followed six days later as a small patch release fixing five regressions, mostly around TypeScript config resolution and accessibility snapshots. The 1.62.0 release contains:

- A new component testing model built around stories and galleries
- An `AbortSignal` option for cancelling actions, navigations, waits, and assertions
- WebP screenshot support in `toHaveScreenshot()` and `page.screenshot()`
- A `Reporter.preprocess()` hook for filtering tests before the run starts
- An `isolated` retry strategy that runs retries at the end of a suite
- WebAuthn credential persistence in storage state
- A bundled MCP server and CLI, runnable via `npx playwright mcp` and `npx playwright cli`

That last bullet is the signal about where browser testing is heading.

## The Engine Bump: Chromium 151, Firefox 153, WebKit 26.5

The bundled browsers are now Chromium 151.0.7922.34, Firefox 153.0, and WebKit 26.5, with the release also tested against stable Chrome 151 and Edge 151.

Why does an engine bump matter to a tester? A large share of flaky browser tests trace back to browser bugs, not test bugs. Running CI against an old Chromium build while users are on Chrome 151 means testing a browser nobody ships. Keeping the engine current closes that gap.

Two practical things to do on every Playwright upgrade:

1. Diff the `playwright.config.ts` channel setting against the bundled version. If pinned to `channel: 'chrome'`, use the stable Chrome the release tested against.
2. Re-run visual regression baselines. An engine bump shifts font rendering and anti-aliasing at the margins, so golden screenshots drift even when the app did not change.

One breaking change worth flagging: Debian 11 is no longer supported. Teams on Debian 11 (bullseye) runners must upgrade to Debian 12 or move to a newer container image.

The engine bump matters twice as much once AI agents enter the picture: an agent that drives the browser through MCP is only as accurate as the engine it controls.

### 47. ScrollTest / Promode — Playwright vs Selenium in 2026

- Source: https://scrolltest.com/playwright-vs-selenium-2026/
- Retrieved: 2026-08-29
- Exa status: complete

By Promode, August 13, 2026.

The Playwright vs Selenium question is the one the author gets most from testers starting out in 2026, and it is also the question where most answers online are pure opinion with zero numbers attached. The short version: Playwright has passed Selenium on almost every adoption metric that is publicly measurable, and Selenium is still the right answer for a specific kind of team.

## The Data That Settles the Debate

Numbers pulled directly from the GitHub API and the npm registry in August 2026:

On GitHub, Microsoft's Playwright repository sits at roughly 94,400 stars. Selenium sits at about 34,400 stars — a 2.7x gap.

The npm data is where the gap gets impossible to ignore. In the last month, the playwright package recorded around 300.9 million downloads. The selenium-webdriver package recorded about 8.0 million — a 37x gap in monthly downloads.

The trend matters more than the snapshot. Year over year, from July 2025 to July 2026:

- `@playwright/test` grew from about 46.0 million to 212.4 million monthly downloads — a 4.6x jump, roughly a 362% increase.
- `playwright` (the core library) grew from 93.3 million to 301.0 million — a 3.2x jump.
- `selenium-webdriver` went from 8.38 million to 8.12 million. Flat, and slightly down.

Playwright's test runner grew more than four times in a single year while Selenium's npm package stayed flat. This does not mean Selenium is dead: Selenium 4.47.0 shipped on August 10, 2026, and both projects are actively maintained. The center of gravity for new browser automation has moved.

## What Selenium Is (and Where It Still Wins)

Selenium WebDriver is the older of the two by a wide margin. The project started in 2004, and WebDriver became a W3C standard in 2018. Being a standard means the protocol is stable, vendor-neutral, and implemented the same way across every browser vendor.

Selenium has been battle-tested across every industry: banks, airlines, telecoms, government systems. When a company has a 15-year-old automation suite that must keep working, that suite is almost always Selenium, often written in Java. The switching cost is measured in years, not sprints.

Selenium officially supports Java, Python, C#, Ruby, JavaScript, and Kotlin.

### 48. Currents.dev (Walter Galvão) — AI Skill: Playwright Best Practices

- Source: https://currents.dev/posts/playwright-best-practices-skill
- Retrieved: 2026-08-29
- Exa status: complete

Walter Galvão, February 3, 2026.

Empower your AI Agents to write, debug, and maintain Playwright tests with expert knowledge.

AI agents are becoming a core part of how developers build software. They write code, debug issues, and ship features alongside us. But when it comes to testing, generic AI assistance often falls short—agents rely on outdated patterns, miss framework-specific nuances, and produce tests that are flaky by design.

To get the most out of agents, you need to provide them with expertise.

Today, we're releasing the Playwright Best Practices Skill—a skill that gives AI agents the specialized knowledge they need to help you write, debug, and maintain production-ready Playwright tests.

```bash
npx skills add https://github.com/currents-dev/playwright-best-practices-skill
```

## What are Agent Skills?

Agent Skills are a new open standard for providing expertise to agents without bloating the context window. Created by Anthropic, skills are now available in all major AI development tools, including Claude Code, Cursor, VS Code, Google Gemini, and more.

At its simplest, a skill is a directory containing a `SKILL.md` file with metadata and expert knowledge that tells an agent how to perform a task in an opinionated way:

```text
playwright-best-practices-skill/
├── SKILL.md          # Instructions + metadata
└── references/       # Topic-specific documentation
    ├── locators.md
    ├── assertions-waiting.md
    ├── debugging.md
    └── ...
```

Skills are progressively disclosed to preserve context. When an agent starts, only the skill's name and description are loaded. When a task matches the skill's purpose, the agent reads the full instructions and pulls in relevant references as needed.

## Introducing the Playwright Best Practices Skill

The skill gives AI agents specialized guidance for writing, debugging, and maintaining Playwright tests in TypeScript. It's designed for any repo where you work with Playwright — E2E, component, API, visual regression, accessibility, security, Electron, or browser extension tests.

What it covers:

- Writing tests — structure, locators, assertions, waiting strategies, Page Object Model, fixtures, test data, annotations
- Debugging & troubleshooting — trace viewer, flaky tests, selectors, timeouts, race conditions, console errors
- Specialized testing — accessibility (axe-core), mobile/responsive, component testing, iframes, canvas/WebGL, service workers/PWA, i18n/localization, Electron apps, browser extensions
- Browser APIs & real-time — WebSockets, geolocation, permissions, clipboard, camera/microphone, multi-tab/popup flows, OAuth
- Error & edge cases — error boundaries, offline mode, network failures, form validation
- Multi-user scenarios — collaboration testing, role-based access, concurrent actions
- Security & performance — XSS, CSRF, auth security, Web Vitals, Lighthouse, performance budgets
- Infrastructure — project config, CI/CD, parallel execution, sharding, global setup/teardown, test coverage
- Advanced patterns — GraphQL mocking, HAR recording, third-party services (payments, email/SMS)

The skill is activity-based: the AI is directed to the right reference depending on what you're doing, so you get focused advice without loading everything into context.

Core testing references include `debugging.md` (trace viewer, inspector, common issues), `flaky-tests.md` (detection, diagnosis, fixing, quarantine), `test-organization.md` (structure, config, E2E/component/API/visual tests), and `locators.md` (selectors, robustness).

### 49. Currents — Agent Skill: Playwright Best Practices (docs)

- Source: https://docs.currents.dev/ai/agent-skill-playwright-best-practices
- Retrieved: 2026-08-29
- Exa status: complete

Documentation page for the Playwright Best Practices agent skill.

## Installation

```bash
npx skills add https://github.com/currents-dev/playwright-best-practices-skill
```

After installing, the AI will automatically use the skill when tasks involve Playwright. No additional configuration is required.

## What is an Agent Skill

Agent Skills are a new open standard for **providing expertise to agents without bloating the context window**. Created by Anthropic, skills are now available in all major AI development tools, including Claude Code, Cursor, VS Code, Google Gemini, and more.

At its simplest, a skill is a directory containing a `SKILL.md` file with metadata and expert knowledge that tells an agent how to perform a task in an opinionated way:

```text
playwright-best-practices-skill/
├── SKILL.md          # Instructions + metadata
└── references/       # Topic-specific documentation
    ├── locators.md
    ├── assertions-waiting.md
    ├── debugging.md
    └── ...
```

Skills are **progressively disclosed** to preserve context. When an agent starts, only the skill's name and description are loaded. When a task matches the skill's purpose, the agent reads the full instructions and pulls in relevant references as needed.

## How is it used?

The skill triggers automatically when the AI infers a Playwright-related task. Example prompts:

- "Fix this flaky login test" → the agent pulls in debugging and assertions guidance
- "Add a test for the checkout flow" → test organization and locator best practices
- "Refactor these tests to use Page Object Model" → POM patterns and structure
- "Why is this test timing out in CI?" → debugging and CI/CD references
- "Set up parallel execution for our test suite" → performance and CI/CD guidance
- "Add accessibility tests for the dashboard" → axe-core and keyboard navigation guidance
- "Test the mobile layout and touch gestures" → device emulation and touch patterns
- "Mock the payment gateway in tests" → third-party service mocking patterns
- "Test the real-time collaboration feature" → multi-user and WebSocket testing

### The skill covers:

- Writing new E2E, component, API, visual regression, or accessibility tests
- Testing mobile/responsive layouts, touch gestures, or device emulation
- Implementing file uploads/downloads, date/time mocking, or WebSocket testing
- Handling OAuth popups, geolocation, permissions, or multi-tab flows
- Testing iframes, canvas/WebGL, service workers, or PWA features
- Testing Electron desktop apps or browser extensions
- Internationalization (i18n), locales, RTL layouts, or date/number formats
- Testing error states, offline mode, or network failure scenarios
- Security testing (XSS, CSRF, authentication, authorization)
- Performance testing with Web Vitals or Lighthouse
- Reviewing or refactoring Playwright test code; fixing flaky tests or debugging failures
- Setting up CI/CD, test coverage, or global setup/teardown
- Configuring projects, dependencies, parallel runs, or sharding

### 50. currents-dev — playwright-best-practices-skill (GitHub repository)

- Source: https://github.com/currents-dev/playwright-best-practices-skill
- Retrieved: 2026-08-29
- Exa status: complete

Repository: "AI Skill for Playwright Best Practices — made by Currents.dev". 368 stars, 63 forks, MIT License, created 2026-01-27. Topics: ai, ai-skill, ai-skills, playwright, playwright-typescript.

A skill that gives the AI specialized guidance for writing, debugging, and maintaining **Playwright** tests in **TypeScript**. Use it in any repo where you work with Playwright so the assistant follows best practices for E2E, component, API, visual regression, accessibility, security, i18n, Electron, and browser extension testing.

## Installation

```bash
npx skills add https://github.com/currents-dev/playwright-best-practices-skill
```

The skill is activity-based: the AI is directed to the right reference depending on what you're doing, so you get focused advice without loading everything at once.

## When the Skill Is Used

The skill triggers when the AI infers tasks such as:

- Writing new E2E, component, API, visual regression, or accessibility tests
- Testing mobile/responsive layouts, touch gestures, or device emulation
- Implementing file uploads/downloads, date/time mocking, or WebSocket testing
- Handling OAuth popups, geolocation, permissions, or multi-tab flows
- Testing iframes, canvas/WebGL, service workers, or PWA features
- Testing Electron desktop apps or browser extensions
- Internationalization (i18n), locales, RTL layouts, or date/number formats
- Testing error states, offline mode, or network failure scenarios
- Security testing (XSS, CSRF, authentication, authorization)
- Performance testing with Web Vitals or Lighthouse
- Reviewing or refactoring Playwright test code
- Fixing flaky tests or debugging failures
- Setting up CI/CD, test coverage, or global setup/teardown
- Configuring projects, dependencies, parallel runs, or sharding

## What's Inside

**57 reference documents** organized into 8 categories.

Core (`core/`):

| Topic | Reference | Use for |
| --- | --- | --- |
| Test structure | `test-suite-structure.md` | Structure, config, E2E/component/API/visual tests |
| Locators | `locators.md` | Selectors, robustness, avoiding brittle locators |
| Assertions & waiting | `assertions-waiting.md` | Expect APIs, auto-waiting, polling |
| Page Object Model | `page-object-model.md` | POM structure and patterns |
| Fixtures & hooks | `fixtures-hooks.md` | Setup, teardown, auth, custom fixtures |
| Test data | `test-data.md` | Factories, Faker, data-driven testing |
| Annotations | `annotations.md` | skip, fixme, slow, test steps |
| Configuration | `configuration.md` | playwright.config.ts options |
| Global setup | `global-setup.md` | globalSetup/Teardown, DB migrations |
| Projects | `projects-dependencies.md` | Project config, dependencies, filtering |

Debugging (`debugging/`) includes `debugging.md` (trace viewer, inspector) and further references; the remaining categories cover specialized testing, browser APIs, error cases, multi-user scenarios, security/performance, and infrastructure.

### 51. Currents.dev — Playwright's New `--last-failed-file` Flag

- Source: https://currents.dev/posts/playwright-re-run-only-failed-tests
- Retrieved: 2026-08-29
- Exa status: complete

Currents Team, June 22, 2026.

Playwright v1.61 adds `--last-failed-file`, a flag that makes caching `--last-failed` in CI simpler and more reliable. If you use sharded Playwright tests in GitHub Actions and have ever wanted to wire up "Re-run failed jobs" without reaching into Playwright's internals, this is for you.

## When This Is the Right Tool

Playwright's retries config already handles the common case: flaky tests that need a couple of attempts to pass. If a test is occasionally unreliable, retries are the better fit — they handle it automatically without human intervention.

`--last-failed` solves a different problem. Think about a release that gets blocked because a CI environment variable wasn't set correctly, or a feature flag wasn't enabled in the environment under test. The tests that failed aren't broken — the environment was. Once you've fixed the config, you want to re-run only the affected tests immediately, without touching test code or waiting for a full shard replay across the entire suite.

That's the scenario where "Re-run failed jobs" with `--last-failed` is the fastest path to unblocking a release.

## Why `--last-failed` Was Awkward in CI

Playwright has had `--last-failed` for a while. Locally, it works great. In CI, wiring it up required some internal knowledge. The `.last-run.json` file that powers `--last-failed` lived inside the first project's outputDir. Since that path is defined in your Playwright config, your CI workflow had to hardcode it — leaking project internals into the pipeline just to cache a file.

Without that setup, clicking "Re-run failed jobs" in GitHub Actions meant Playwright re-ran every test assigned to that shard. Not just the ones that failed. All of them. Minutes wasted, CI budget burned on work that already passed.

## The New Flag

The result is a new flag in Playwright v1.61: `--last-failed-file`. You can also set it via the `PLAYWRIGHT_LAST_RUN_OUTPUT_FILE` environment variable.

It does exactly what the name suggests: tells Playwright where to write and read the `.last-run.json` file. You can place it anywhere — outside `outputDir`, outside `test-results/`, in a dedicated cache directory. The file no longer gets wiped on test start, and you don't need to know which project's output directory Playwright chose internally.

One flag. But it removes the need to know Playwright's internal directory layout, making the caching setup straightforward.

## Using It with GitHub Actions Retries

The idea: cache the `.last-run.json` file per shard, so that when a failed job is retried, Playwright can read it and skip the tests that already passed.

### Cache Key Design

The cache key needs three properties:

1. It must be scoped to the current workflow run (`run_id`), so stale caches from previous runs don't interfere.
2. It must include the shard index, so each shard caches its own file.
3. It must include `run_attempt`, because GitHub Actions caches are immutable.

Restore and save steps should run separately with `if: always()`.

### 52. Currents.dev — Playwright HTML Reporter: Why It Breaks Down at Scale

- Source: https://currents.dev/posts/playwright-html-reporter-why-it-breaks-down-at-scale
- Retrieved: 2026-08-29
- Exa status: complete

Currents Team, April 24, 2026.

When Playwright suites grow, pass/fail reports stop being enough. Your suite starts at a few dozen tests. Six months later, it's three hundred. Somewhere in there, the HTML reporter stopped being useful. Tests fail, you retry, something passes, you move on. No one on the team can tell you whether the suite is getting more reliable or less.

At that scale, a pass/fail report isn't enough. You need to know whether the suite is getting healthier or degrading, how tests behave across multiple runs, which failures are correlated, and where to invest engineering effort. The focus shifts from collecting results to reading them.

## Playwright's HTML Reporter Was Built For A Different Problem

The native reporter is a debugging UI. It was designed for developer-friendly inspection of test results during local runs with `npx playwright test` or in smaller CI executions.

It works well for:

- Single-run debugging. When a handful of tests fail, you get the stack trace, the video, and the trace viewer per failure. That's the whole loop for fixing a broken test.
- Small suites. In our experience, you can eyeball 20 to 50 results per run without losing track of which is which.
- Sequential execution. With a single worker, results appear in test order.

Once you introduce multiple workers running in parallel, the limitations show up. Results from separate worker processes are aggregated into one report, but the HTML reporter does not preserve the execution context behind them.

For cross-shard runs, Playwright provides the blob reporter and the `merge-reports` command to combine sharded runs into a single report. That still doesn't show how failures cluster across workers or how tests were distributed during execution. The reporter isn't built for structured analysis across parallel runs, flakiness detection at scale, or multi-run trend analysis.

The report can show: "Two tests out of ten failed, and here is what went wrong."

It cannot show: "Your CI failure rate is 15 percent across the last 10 runs. Reliability is degrading, and the failures correlate with the payment integration tests."

### How the HTML Reporter Actually Works Under the Hood

Playwright's built-in reporter is event-driven and operates entirely in memory. It treats every test run as a clean slate and does not retain information about previous executions.

The reporter listens for lifecycle events such as:

- `onBegin(config, suite)`, which receives the resolved `FullConfig` and the root `Suite` containing all projects, files, and test cases.
- `onTestBegin(test, result)`, which fires when each test starts.
- `onTestEnd(test, result)`, which fires when the test finishes with the final `TestResult`. Note that `TestInfo` is only available inside test bodies, not to the reporter.
- `onEnd(result)`, which runs after all tests finish.

The reporter acts as an observer in the test runner pipeline, receiving structured events — and keeping none of the history.

### 53. Currents.dev (Goodness Eboh) — State of Playwright AI Ecosystem in 2026

- Source: https://currents.dev/posts/state-of-playwright-ai-ecosystem-in-2026
- Retrieved: 2026-08-29
- Exa status: complete

Goodness Eboh, March 2, 2026.

Why has AI adoption accelerated in Playwright? Why does its ecosystem seem to absorb new capabilities so naturally, from autonomously planning test flows to self-healing failing steps?

AI didn't become interesting in Playwright because "tests can be generated." It became interesting because Playwright is now turning testing workflows into programmable building blocks: plans, snapshots, traces, videos, reports, and repeatable execution surfaces that agents can drive and humans can audit.

The ecosystem is moving from "LLM writes some code" to "LLM participates in the workflow": exploring an app, producing a plan, generating tests, collecting proof artifacts, and iterating when things break. That shift is powered by three converging pieces:

- Constrained execution layers (MCP and tool-based automation),
- Agent specialization (planner → generator → healer),
- Artifact-first debugging (traces, videos, reports, dashboards).

## The Current Playwright AI Landscape

#### 1. MCP: The "Tools Layer" That Gives Models Superpowers

The Playwright MCP server provides a structured execution layer between AI systems and Playwright. Rather than letting an AI model infer what it can or cannot do, MCP exposes a fixed set of over 20 tool capabilities that the model can invoke — from `browser_click` and `browser_navigate` to `browser_snapshot` and `browser_console_messages` — with tightly scoped access to the browser, filesystem, and runtime environment.

A key architectural decision: Playwright MCP operates on the accessibility tree, not screenshots. The server returns structured accessibility snapshots that represent the page as a hierarchy of roles, names, and states — the same data assistive technologies consume. This means AI models don't need vision capabilities to interact with pages, and the resulting automation is deterministic and fast.

This structure significantly reduces action-level hallucinations. Instead of guessing selectors, APIs, or execution paths, an AI system operates only within the constraints defined by the engineer.

That constraint is what makes MCP valuable. It shifts AI interaction from free-form instruction following to constrained test execution that is reproducible and auditable. For example, you can instruct an AI, through MCP, to navigate between pages, interact with specific elements, analyze page state, and generate tests. MCP supplies the execution surface while preventing the AI from stepping outside those boundaries.

However, MCP's safety is defined by what teams choose to expose. Filesystem access, environment variables, and CI secrets remain common sources of risk if permissions are not carefully scoped. MCP is not test strategy, it's the execution toolbox. Strategy still comes from your prompts, your standards, and your architecture.

MCP limitations:

- It has no inherent reasoning or strategy for test design and relies entirely on user instructions and model behavior.
- It is only as powerful as the tools and permissions exposed through it.
- It can be token-expensive compared with CLI workflows.

### 54. Currents.dev (Asjad Khan) — When Tests Should Run Headless vs Headed

- Source: https://currents.dev/posts/when-tests-should-run-headless-vs-headed-in-playwright
- Retrieved: 2026-08-29
- Exa status: complete

Asjad Khan, February 25, 2026.

Your test suite works smoothly on your device. All of the tests have passed. You pushed the branch, kicked off CI, and a few tests failed with timeouts. You check the logs, but find nothing. So you try running those exact tests locally in headless mode to match what CI does. They pass.

Now, you feel stuck. The failures only occur in CI's headless environment, but they cannot be reproduced. You toggle between modes a few times, maybe adjust some timeouts, and eventually the tests pass. Problem solved, right?

Not really. You just masked the issue. Headless and headed aren't just "browser window on vs off." By default, Playwright actually uses two different Chromium binaries: a lightweight chromium headless shell for headless mode and the full Chromium browser for headed mode. They're separate implementations with different rendering behavior, GPU handling, and font rendering. What appears to be a flaky test is often a real behavioral difference between those two binaries.

## When to use each mode

- Use headless when: running tests in CI/CD pipelines; scaling parallel test execution; web scraping or routine health checks; faster execution for regression suites.
- Use headed when: debugging failed tests (especially timing issues); working with animations or CSS transitions; developing new test automation; troubleshooting hover states or drag interactions.

The workflow that works: write and debug in headed mode locally. Validate and run at scale in headless mode in CI. Don't switch modes randomly just to fix a flaky test.

## Headless Shell vs New Headless

Playwright ships two separate Chromium binaries:

**Chromium Headless Shell (default for headless):** a stripped-down browser built on Chromium's `//content` module. It doesn't depend on X11/Wayland or D-Bus, and it has a smaller footprint. But it's a different implementation than what you see in headed mode. Think of it as a purpose-built scraping and automation binary, not "Chrome minus the window."

**Full Chromium (used for headed mode):** the real browser, with the complete rendering pipeline, GPU integration, and all the platform features you'd expect.

This means that when a test passes headed but fails headless, you're not just dealing with a missing window — you're running a different binary with different rendering behavior, different GPU handling, and different font rendering.

Since Chrome 112, there's a third option: the new headless mode. It runs the full Chrome browser without a visible window. Same code, same rendering, same behavior as headed mode. You can opt into it in Playwright by setting `channel: 'chromium'`:

```typescript
// playwright.config.ts
import { defineConfig, devices } from "@playwright/test";

export default defineConfig({
  projects: [
    {
      name: "chromium",
      use: { ...devices["Desktop Chrome"], channel: "chromium" },
    },
  ],
});
```

### 55. Currents.dev — The Playwright Network Mocking Playbook

- Source: https://currents.dev/posts/the-playwright-network-mocking-playbook
- Retrieved: 2026-08-29
- Exa status: complete

Currents Team, April 1, 2026.

Playwright mocking is easy to start but hard to scale. Network mocking in Playwright is deceptively easy to get started with and deceptively hard to maintain. The mechanics — `page.route()`, `route.fulfill()`, `routeFromHAR()` — are well-documented and quick to pick up. Most teams get a working mock strategy within the first few weeks of adopting Playwright. The problem is that a strategy that passes tests today can become a liability over time, and that gap tends to stay invisible until something breaks in production.

The pattern usually goes like this: a team mocks external API calls to speed up CI, the suite runs fast and reliably, and confidence builds. Then, the real APIs start changing. A payment provider adds a required field. An auth provider changes how token errors are returned. An idempotency policy gets enforced that wasn't before. The mocks don't follow any of this; they stay frozen at the moment they were written. By the time the gap surfaces, usually through a customer-facing incident rather than a test failure, the mock debt has been accumulating for months.

What most teams are missing is a decision framework: one that covers when to mock, which flows need to be tested against real systems, and how to keep the two from drifting apart silently.

## The Mocking Spectrum (Not Binary)

The first thing to get right is the mental model. Mocking is not a switch you flip; it sits on a spectrum, and every position on it carries a different trade-off between speed and fidelity.

Full mocking gets you speed and determinism. Real integrations get you accuracy. Neither extreme is correct across the board.

### Level 0: No Mocking (Real Integration)

At this level, tests send real requests to external services — no interception or route handling, just the actual network call hitting a sandbox or test environment maintained by the provider:

```typescript
test("completes payment with Stripe test credentials", async ({ page }) => {
  await page.goto("/checkout");
  await page.getByTestId("card-number").fill("4242424242424242");
  await page.getByTestId("card-expiry").fill("12/26");
  await page.getByTestId("card-cvc").fill("123");
  await page.getByTestId("submit-payment").click();
  await expect(page.getByText("Payment confirmed")).toBeVisible();
});
```

Nothing is intercepted here. Stripe's test API receives the request, processes it against real validation logic, and returns a real response.

A caveat from the article: this example fills card fields directly on the page, which works for simplified checkout implementations. In many production Stripe integrations, sensitive card fields are hosted in Stripe-managed iframes or tokenized via Stripe.js before the form submits, meaning the actual test shape involves backend-mediated payment intent creation followed by webhook confirmation. The principle is the same; the test mechanics differ based on how the integration is built.

The fidelity is as high as it gets. You catch real contract issues, credential misconfiguration, redirect behavior, and edge cases in provider logic that a mock will never reproduce. But there are tradeoffs: network latency, dependency on external service uptime, rate limits, and the requirement of sandbox account management. The article continues through the remaining levels of the spectrum (static routes, partial mocking, HAR replay) and how to keep mock debt in check.

### 56. TestDino (Jashn Jain) — 17 Playwright Best Practices That Actually Matter

- Source: https://testdino.com/blog/playwright-best-practices
- Retrieved: 2026-08-29
- Exa status: complete

Updated August 12, 2026.

Playwright best practices are the patterns that keep an end-to-end suite fast, readable, and stable as it grows. In short: test what users see, lock onto role-based locators, let assertions auto-wait, isolate every test, seed data through the API, and watch flaky tests with real reporting in CI.

The 17 practices below each come with a runnable code example and the specific failure it prevents. The code snippets target Playwright 1.60.0.

What this guide covers:

1. Define your test coverage goals
2. Test what users see, not how it's built
3. Use stable locators
4. Name tests and steps for the failure
5. Keep tests focused and isolated
6. Write assertions that wait automatically
7. Assert the accessibility tree, not the DOM
8. Use APIs to seed test data
9. Reset database state between runs
10. Mock external dependencies with `page.route()`
11. Structure your project for scale
12. Master Playwright's debugging tools
13. Abort tests early when a precondition fails
14. Parallelize and shard across CI
15. Eliminate flaky tests
16. Use Playwright AI agents to generate and heal tests
17. Centralize reporting for CI

## Define your test coverage goals

End-to-end tests are slow and expensive to maintain, so spend them where a failure costs the most. Cover the paths that make money and the ones that page someone at night: sign-up, login, checkout, and the core action the product exists to do. Push everything else down to unit and integration tests.

Aim for roughly 30% of the total suite as E2E. A test that asserts a button is blue belongs in a unit test. A test that proves a guest can pay belongs here.

```typescript
// checkout-coverage.spec.ts
// E2E: prove the path works end to end
test('user can complete checkout', async ({ page, request }) => {
  const userId = await request.post('/api/test/user/create').then(r => r.json());
  await page.goto('https://storedemo.cms.testdino.com/');
  await page.getByText('Apple iPad Air').first().click();
  await page.getByRole('button', { name: /add to cart/i }).click();
  await expect(page.getByText('Cart')).toBeVisible();
});
```

Tip: use analytics data quarterly to revisit coverage priorities. What users do today might change in 6 months.

## Test what users see, not how it's built

A user does not know what the component is called. They see a button that says "Pay". When you assert against internal class names or state attributes, the test breaks every time a developer refactors markup that the user never notices.

```typescript
// Brittle: breaks on any markup refactor
await page.locator(".btn.btn--primary.checkout-cta").click()

// Resilient: survives refactors, mirrors the user
await page.getByRole("button", { name: "Pay" }).click()
```

Rule of thumb: if you delete the element's class attribute and the user experience stays the same, the test shouldn't break either. This affects everything downstream: locator stability, assertion reliability, and maintenance cost.

## Use stable locators

Playwright ranks locators by how closely they match what a user perceives. Reach for the top of the list first (roles, labels) and drop down to test IDs only when you have to.

### 57. TestDino (Savan Vaghani) — Playwright UI Mode: The Complete Guide to Visual Test Debugging

- Source: https://testdino.com/blog/playwright-ui-mode
- Retrieved: 2026-08-29
- Exa status: complete

Published July 16, 2026, updated August 3, 2026.

Tired of debugging E2E tests in a headless terminal? Playwright UI mode offers visual time-travel debugging to identify root causes and fix flaky tests in seconds.

Software teams are shipping updates faster than ever right now, and ensuring nothing breaks is a daily struggle. When an end-to-end test fails in a CI pipeline, figuring out exactly what went wrong in a sea of headless text logs can waste hours of precious engineering time.

If you are tired of staring at cryptic TimeoutError messages and wondering why a button was not clicked, you need a better approach.

## What is Playwright UI mode?

Playwright UI mode is an interactive desktop interface (introduced in v1.32) that lets developers visually explore, run, and debug automation scripts. It replaces standard terminal output with a time-travel workspace containing a live DOM viewer, timeline scrubber, and network intercept logs.

When engineers ask what Playwright UI mode is, they are usually looking for a better way to visualize test execution without relying on console logs. Think of it as a complete command center for the entire test automation workflow. It allows you to observe every single action the test takes inside a real browser window, capturing the exact state of the DOM at every millisecond.

Instead of reading a long stack trace to figure out why an element was hidden, you get to see exactly what the user would see. You can inspect the Document Object Model at the exact moment a failure occurs, dramatically reducing the time spent guessing why a specific element was not found.

Many teams struggle with flaky tests because they cannot see the network state during a run. This interactive runner captures all network requests and console logs automatically. You do not need to write extra code or complex configurations to enable these features — it hooks directly into the Chrome DevTools Protocol (CDP) to gather debugging data with almost zero performance penalty.

## Why engineers need Playwright UI mode

Relying solely on terminal outputs for E2E testing is a massive bottleneck. A faster way is needed to understand why a dynamic React component didn't render or why a GraphQL mutation timed out.

One major benefit is the ability to isolate specific test files instantly. You can filter the test tree and only execute the exact describe block you are actively working on. This targeted approach saves computing resources and tightens the feedback loop.

Tip: use the built-in search bar in the test explorer to filter by @tags. If tests are tagged with @auth or @checkout, typing @auth instantly scopes the UI runner down to just those critical paths.

Another huge advantage is the live Watch Mode. When you edit TypeScript or JavaScript test files, the runner instantly detects the change and triggers a rerun automatically.

Context switching is a known productivity killer. By keeping the code execution, the browser rendering, and the network logs in one unified dashboard, cognitive load is heavily reduced. It also simplifies onboarding for junior developers: visual tools are universally easier to grasp than dense command line outputs.

### 58. TestDino (Vishwas Tiwari) — Playwright Flaky Test Debugging: How to Fix Unstable CI Pipelines

- Source: https://testdino.com/blog/playwright-flaky-test-debugging
- Retrieved: 2026-08-29
- Exa status: complete

Updated July 20, 2026. The first Exa fetch returned only the page shell ("Loading blog post"); a retry with a rendering scraper after a 30 s backoff marker recovered the full article.

Stop chasing random CI failures. Learn advanced Playwright flaky test debugging techniques to trace, isolate, and permanently fix unstable tests.

Non-deterministic test failures undermine the core value of continuous integration. When test suites randomly fail in CI pipelines but pass locally, engineering teams rapidly lose trust in their automation. This leads to ignored test results, delayed deployments, and significant engineering hours wasted investigating false positives.

A reliable automation suite requires moving beyond temporary retries and addressing the underlying architectural flaws that cause test instability.

## Common causes requiring Flaky Test Debugging

Identifying the root cause of an unstable test is half the battle.

| Root Cause | Impact on Suite | Recommended Fix |
| --- | --- | --- |
| Network Races | High failure rate | Use web-first assertions |
| Hard Timeouts | Random CI failures | Remove static waits |
| Shared State | Cascading failures | Isolate test data |

**Flaky Test Debugging** is the systematic process of identifying, isolating, and resolving automated tests that produce inconsistent pass and fail results without any underlying code changes.

### Asynchronous rendering and network races

Modern single-page applications load data asynchronously. DOM elements frequently appear on the screen at vastly different times depending on server response latency. If a test attempts to interact with a button before the associated JavaScript event listener is fully attached, the test will fail unpredictably.

This timing mismatch is the primary reason teams struggle with stability. Relying on fixed timers will inevitably cause problems on slower CI machines. Instead, synchronize tests with the application's true state.

### Weak assertions and rigid timeouts

Using static timeouts is a dangerous anti-pattern. Waiting exactly five seconds guarantees a failure when a database query takes six seconds under heavy load. Instead of arbitrary pauses, modern automation requires asserting against the final expected state. Playwright offers built-in auto-waiting mechanisms designed specifically for this.

### Test data contamination and shared state

Tests that share a single staging environment often corrupt each other's data. If Test A deletes a user account that Test B relies on, Test B will fail randomly depending on execution order. Parallel execution acts as a magnifying glass for these collisions.

Every test should create its own unique data payload. Using API setup blocks to generate fresh records before the UI interaction begins ensures complete isolation.

### Third-party dependency failures

Modern applications rely heavily on external services, such as payment gateways or third-party authenticators. These external dependencies can experience downtime, rate limiting, or sandbox resets. Mocking these external services is the most effective defense mechanism: by intercepting network requests and providing mocked responses, the test runs identically every single time.

## Step-by-step Flaky Test Debugging in Playwright

### Isolating the unreliable test locally

Separate the unstable test from the rest of the suite to rule out global state pollution. Run it in isolation using the `.only` annotation:

```typescript
// test-isolation.spec.ts
import { test, expect } from '@playwright/test';
test.describe('Dashboard feature', () => {
  test.only('loads widgets correctly', async ({ page }) => {
    await page.goto('/dashboard');
    await expect(page.locator('.widget')).toBeVisible();
  });
});
```

### Disabling automatic retries for clarity

When investigating locally, disable automatic retries. Retries mask the underlying problem. Use `--retries=0` combined with `--repeat-each` to force the failure to happen:

```bash
# Force the flaky test to reveal itself by looping it
npx playwright test --retries=0 --repeat-each=20
```

### Capturing trace data for visual analysis

Trace files record every network request, console log, and DOM snapshot during execution. Enabling tracing in the configuration is a non-negotiable requirement for serious automation teams.

### Leveraging the interactive Playwright inspector

The Playwright Inspector allows pausing execution live. Insert a pause command right before the suspected breaking point; the browser halts, allowing new locators to be tested directly in the console:

```typescript
// inspector-pause.spec.ts
test('debug complex interaction', async ({ page }) => {
  await page.goto('/checkout');

  await page.pause(); // Execution halts here for live debugging
  await page.getByRole('button', { name: 'Submit' }).click();
});
```

## Best practices to prevent test instability

### Adopting resilient web-first assertions

Web-first assertions automatically wait for conditions to be met, retrying the check until a timeout is reached. This is exponentially safer than checking a state only once:

```typescript
// assertion-example.spec.ts
// Bad: checking state once
// expect(await page.locator('.submit-btn').isVisible()).toBeTruthy();

// Good: auto-retrying web-first assertion
await expect(page.locator('.submit-btn')).toBeVisible({ timeout: 10000 });
```

### Using semantic and user-facing locators

CSS and XPath locators break easily when developers refactor layouts. They are coupled to the DOM structure, not the user experience. Roles, labels, and text content represent what the user actually sees.

### Managing browser state independently

Never rely on a previous test to log you in. Each test block must handle its own authentication state. Playwright allows saving and reusing authentication cookies and tokens efficiently via `storageState`. This speeds up execution while maintaining complete test isolation.

### Mocking unpredictable backend services

Frontend tests should not fail because the staging database is undergoing maintenance. Use Playwright's network interception API to fulfill requests with static JSON:

```typescript
// api-mocking.spec.ts
test('renders user profile safely', async ({ page }) => {
  await page.route('**/api/users/me', route => {
    route.fulfill({ json: { id: 1, name: 'TestDino Admin', role: 'admin' } });
  });

  await page.goto('/profile');
  await expect(page.getByText('TestDino Admin')).toBeVisible();
});
```

## Advanced tools for analyzing test failures

### Utilizing built-in HTML reporters

The default HTML reporter highlights unstable tests clearly, marking tests that failed initially but passed on a retry.

### Customizing the CI retry configuration

Retries are a safety net, not a permanent solution. Running retries only in the CI environment is a standard industry practice. Keep local runs strict:

```typescript
// playwright.config.ts
import { defineConfig } from '@playwright/test';
export default defineConfig({
  // 2 retries on CI, 0 retries locally
  retries: process.env.CI ? 2 : 0,
  use: {
    trace: 'retain-on-failure',
  }
});
```

### Integrating visual regression checks

Sometimes tests pass functionally but fail visually — a button might be clickable but hidden behind a broken CSS modal:

```typescript
// visual-regression.spec.ts
test('dashboard layout remains intact', async ({ page }) => {
  await page.goto('/dashboard');
  await expect(page).toHaveScreenshot('dashboard-baseline.png', { maxDiffPixels: 100 });
});
```

### Extracting logs from the browser console

JavaScript errors in the browser console often cause silent test failures where the UI simply freezes:

```typescript
// console-logs.spec.ts
test('catches console errors', async ({ page }) => {
  const errors: string[] = [];
  page.on('console', msg => {
    if (msg.type() === 'error') errors.push(msg.text());
  });
  await page.goto('/app');
  expect(errors.length).toBe(0);
});
```

## How CI environments impact test stability

### Resource constraints and CPU throttling

CI runners have limited CPU allocations. A test that takes two seconds on an M3 Mac might take five seconds in a GitHub Actions runner. Tip: use the Chrome DevTools protocol to throttle local CPU and simulate the slower CI environment.

### Network latency and timing variations

Network speed in CI pipelines is highly unpredictable. Intercepting and explicitly waiting for network requests stabilizes these variations:

```typescript
// network-wait.spec.ts
test('waits for search results', async ({ page }) => {
  const searchPromise = page.waitForResponse('**/api/search?q=*');
  await page.getByPlaceholder('Search').fill('TestDino');

  await page.keyboard.press('Enter');
  const response = await searchPromise;
  expect(response.status()).toBe(200);
});
```

### Headless mode rendering differences

Running tests in headless mode saves resources but behaves differently. Fonts may render smaller, triggering unexpected layout shifts. Always run final local checks in headless mode to mirror the cloud environment.

### Parallel execution memory limits

Running tests in parallel speeds up the suite but consumes RAM. If the CI runner hits an out-of-memory exception, browser processes crash randomly. Tune the worker count based on the specific CI machine specs rather than defaulting to the maximum available.

## Building a long-term stability strategy

### Continuous monitoring and proactive quarantining

When a test becomes unstable, quarantine it immediately to protect the pipeline. A quarantined test runs in the background to gather data but does not fail the build. This gives the team time to fix the root cause without blocking deployments.

Note: consistency is the best weapon — review test failures daily before they accumulate into a massive backlog.

### Tracking failure metrics over time

Track stability metrics to secure engineering resources: how many tests fail randomly per week, how much time is wasted investigating them.

### Fostering a quality-first engineering culture

Quality cannot be the sole responsibility of the QA team. Developers must own the stability of the tests they write. Document internal best practices in a central wiki and run monthly workshops.

## Conclusion

While you cannot eliminate every unstable test overnight, applying web-first assertions, strict data isolation, and deep trace analysis yields immediate improvements. Make stability a core engineering metric, not just a QA chore. Start using the trace viewer today, quarantine the bad actors, and slowly rebuild confidence.

FAQ highlights: the fastest method is enabling the Trace Viewer (`trace: 'retain-on-failure'`); flaky debugging is difficult because failures are highly non-deterministic; fix locally with `--retries=0` + `--repeat-each=20` + `.only` + `page.pause()`; do not delete unstable tests immediately — quarantine first, then delete only if the test provides no business value; the built-in HTML reporter flags flaky tests automatically with a yellow warning icon.

### 59. TestDino (Pratik Patel) — Headless vs Headed in Playwright

- Source: https://testdino.com/blog/headless-vs-headed
- Retrieved: 2026-08-29
- Exa status: complete

Published March 18, 2026, updated June 8, 2026.

Most teams configure headless vs headed mode in Playwright once and never think about it again, until a test starts failing in CI that works perfectly on their machine.

The confusion comes down to one thing: teams pick a mode without understanding what each one actually does under the hood, and they pay for it with debugging sessions that take twice as long as they should.

TL;DR

- Headless = no visible window, headed = visible window. Headless is Playwright's default.
- Headless is faster because it skips painting frames to a display. Large suites see a 30%–50% pipeline-time drop.
- Since Playwright 1.57, the two modes use different binaries by default: headed runs chrome, headless runs chrome-headless-shell. That is why a test can pass headed and fail headless.
- The fix for cross-mode rendering gaps is the new headless mode: set `channel: 'chromium'` so both modes use the same full browser.
- Workflow that works: headed locally for debugging, headless in CI for speed.

## What is the difference between headless and headed mode in Playwright?

Headless mode runs the browser without opening a visible window. Headed mode opens a full browser window you can see and interact with. That single difference drives speed, resource usage, and how easily you can debug.

In headless mode, the browser engine still loads pages, runs JavaScript, and builds the DOM. It just skips painting pixels to a display. In headed mode, a native window opens and every frame renders on screen.

| Aspect | Headless mode | Headed mode |
| --- | --- | --- |
| Browser window | Not visible | Visible on screen |
| Speed | Faster | Slower |
| CPU / memory usage | Lower | Higher |
| Visual debugging | Not real-time (use Trace Viewer) | Full real-time visibility |
| Default Chromium binary | chrome-headless-shell | chrome (full browser) |
| Best for | CI/CD, regression suites, parallel runs | Debugging, test development, visual checks |

## The chrome-headless-shell gotcha: two browser binaries since 1.57

Here is the part most guides skip, and it is the root cause behind "passes headed, fails headless."

Since Playwright 1.57, Playwright runs on Chrome for Testing builds, and the two modes use different binaries by default. Headed mode launches chrome. Headless mode launches chrome-headless-shell, a separate, lighter binary built for automation.

That means headless and headed are not the same browser with one feature toggled off. By default they are two different executables with different rendering paths. Fonts can rasterize differently. GPU and canvas handling can differ. Most of the time it does not matter. When you compare pixel-level screenshots, it does.

The mental model that fixes this: headless vs headed is not a display switch, it is a binary switch.

### 60. TestDino (Dhruv Rai) — Playwright Trace Viewer: Debug Tests with Screenshots & Videos

- Source: https://testdino.com/blog/playwright-trace-viewer
- Retrieved: 2026-08-29
- Exa status: complete

Published February 13, 2026, updated June 3, 2026.

Your Playwright test passes locally but fails in CI with a cryptic error message. No context, screenshots, or network logs. You're debugging blind, adding console.log statements and increasing timeouts, hoping to catch the failure locally.

Playwright Trace Viewer eliminates this guesswork. It records every action, screenshot, network call, and DOM mutation during test execution, creating a complete forensic record that you can replay step by step. This transforms debugging from hours of trial and error into minutes of targeted analysis.

The guide covers setup, CI/CD configuration, debugging with screenshots and videos, trace file sizing, and the newest trace features through Playwright 1.60, including the first-class HAR recording API, the trace CLI, and the retain-on-failure-and-retries mode.

## Why is the Playwright trace viewer essential for debugging tests

Test failures without context waste valuable development time. When a Playwright test breaks in CI, you get a generic "element not found" error with zero visibility into what actually happened. The application state is gone. Network requests are invisible. DOM snapshots have vanished.

Playwright Trace Viewer captures 5 critical data layers:

1. Action Timeline — every Playwright action with duration, status, and locator details. Identifies slow operations, unexpected retries, and the exact step that triggered failures.
2. DOM Snapshots — complete page state before and after each action. Inspect elements, verify selectors, and debug DOM structure changes that break tests.
3. Network Activity — all HTTP requests with headers, payloads, status codes, and timing. Failed API calls that prevent buttons from loading become immediately visible.
4. Console Messages — browser console output plus Playwright's internal logs. JavaScript errors and auto-wait explanations help determine if failures stem from application bugs or test logic.
5. Screenshots & Videos — visual confirmation of page state at each step. Spot overlapping UI elements, rendering delays, and layout shifts invisible in text logs.

Playwright 1.59 also introduced the `page.screencast` API for recording video with action annotations and chapter markers, giving richer video evidence than the older `recordVideo` option.

This visibility eliminates the "works on my machine" problem by showing the exact browser state during CI execution. You see timing issues like elements appearing and then disappearing before interaction. Backend errors that cause UI elements to never load become traceable.

Without Trace Viewer, debugging flaky Playwright tests means recreating failures locally and guessing at root causes. With Trace Viewer, you open the trace file, navigate to the failed action, and see the exact browser state that caused the problem.

## How to use Playwright trace viewer to analyze screenshots and videos

Playwright trace files are .zip archives containing screenshots, network logs, DOM snapshots, and action metadata. Traces can be opened locally via the Playwright CLI or in a browser using trace.playwright.dev.

### 61. TestDino (Vishwas Tiwari) — Why Playwright Tests Pass Locally but Fail in CI

- Source: https://testdino.com/blog/playwright-test-failure
- Retrieved: 2026-08-29
- Exa status: complete

Published February 18, 2026, updated May 21, 2026.

The problem today is that most teams debug failures one at a time, without a system to understand why tests break.

This article combines data from peer-reviewed studies, open-source project analyses, and real-world failure classification patterns from Playwright teams to build a breakdown of test failures.

The goal: show where debugging time goes, and how to cut it in half.

## Finding 1: Almost Half of All Failures Trace Back to Async Wait Issues

The single biggest category of E2E test failures is async wait, and it's not close.

Romano et al. found that 45% of flaky UI tests fail because tests don't properly wait for asynchronous operations to complete. Luo et al.'s foundational study confirmed the same pattern: nearly half of flaky-test-fixing commits addressed async wait as the root cause.

In Playwright terms, this shows up as:

- Clicking before an element is interactive: the element exists in the DOM but isn't clickable yet
- Asserting before data loads: API responses haven't returned, so the UI shows a stale or empty state
- Racing against animations/transitions: the page is mid-render when the test acts

| Root Cause Category | % of UI Test Failures | Playwright Equivalent |
| --- | --- | --- |
| Async Wait | 45% | Missing waitFor, premature assertions |
| Concurrency / Race Conditions | ~24% | Parallel tab/context conflicts, shared state |
| Platform / Environment | ~12% | Browser-specific rendering, OS differences |
| Network | ~9% | API timeouts, failed backend requests |
| Test Order Dependency | ~5% | Shared database state between tests |
| Other (IO, Randomness, Time) | ~5% | Timezone-dependent logic, random test data |

Sources: Romano et al., 2021; Luo et al., 2014; Parry et al., 2022

Tip: Playwright's built-in auto-waiting covers many async cases automatically. But auto-wait only applies to actions like `click()` and `fill()`. Assertions like `expect(locator).toHaveText()` need explicit `await`, and custom logic that checks DOM state manually bypasses auto-wait entirely. That's where most async failures hide.

Start by auditing assertions. Replace manual DOM checks with Playwright's web-first assertions (`expect(locator).toBeVisible()` instead of checking element properties directly). This alone can address nearly half the failure backlog.

## Finding 2: Timeouts Are the Silent Killer

Timeouts sit in a gray zone between real failures and infrastructure noise. The SAP HANA study found that system tests suffer from timeout issues in 18% of cases, compared to just 7% for unit tests. The more complex the test, the more likely a timeout becomes the failure mode.

In Playwright, timeouts show up in three flavors:

- Action timeouts (default 30s): `page.click()` waiting for an element that never appears
- Navigation timeouts (default 30s): `page.goto()` on a slow or unresponsive server
- Test timeouts (default 30s per test): the entire test exceeds its time budget

Eck et al.'s study of 200 flaky tests at Mozilla found that test case timeouts and test suite timeouts were frequent enough to warrant their own categories, separate from async wait.

Tip: don't just increase timeout values when tests fail. That masks the problem. Check whether the timeout is caused by a slow backend (network issue), a missing element (locator issue), or a legitimately slow operation.

## Finding 3: Resource and Environment Flakiness

Resource and environment differences account for a large share of remaining failures — CI runners are slower and more constrained than local machines, so timing, rendering, and resource limits diverge.

### 62. TestDino (Ayush Mania) — Performance Testing Using Playwright

- Source: https://testdino.com/blog/playwright-performance-testing
- Retrieved: 2026-08-29
- Exa status: complete

Published May 19, 2026, updated May 23, 2026.

Catch slow pages before users do. This guide covers Playwright performance testing with Core Web Vitals, Lighthouse, tracing, and CI/CD gates.

Teams ship code multiple times a day now. Google uses Core Web Vitals as a ranking signal. Users leave a page if it takes more than three seconds to load. Performance is no longer optional.

The problem is that most teams catch performance issues after deployment. A slow API, a heavy image, or a render-blocking script sneaks into production and tanks the user experience before anyone notices. Setting up automated browser performance testing is the single most effective way to catch these regressions early.

## What is performance testing using Playwright?

Performance testing checks how fast your application responds to user actions. It covers page load speed, API response times, rendering performance, and resource download behavior.

Playwright is an open-source test automation framework built by Microsoft. It controls Chromium, Firefox, and WebKit browsers through a single API. While Playwright is primarily an end-to-end testing tool, it gives you deep access to browser internals that make it excellent for capturing performance data during real user flows.

Performance testing using Playwright means collecting browser-level timing data (navigation timings, Core Web Vitals, resource load durations) during automated test runs to detect speed regressions before they reach production.

There is an important distinction to make here. Playwright is not a load testing tool like k6, JMeter, or Artillery. Those tools simulate thousands of concurrent users hitting your server.

Playwright runs a single real browser instance and measures how that browser experiences your application. Think of it as a performance gatekeeper, not a load generator.

Here is what Playwright can measure:

- Navigation timings like Time to First Byte (TTFB), DOMContentLoaded, and full page load
- Core Web Vitals like Largest Contentful Paint (LCP), Cumulative Layout Shift (CLS), and Interaction to Next Paint (INP)
- Resource timings for individual CSS, JS, image, and API requests
- Network activity through HAR file recording
- Custom user flow durations using `performance.mark()` and `performance.measure()`

The real value is that you can embed these checks directly into the existing Playwright test automation suite. No separate tool, no separate pipeline. You write a functional test and add performance assertions on top of it.

## How Playwright captures performance metrics

Playwright gives you three main channels for collecting performance data. Each one serves a different purpose and depth level.

### Navigation Timing API

The browser records timestamps for every phase of a page load. Playwright lets you pull these out using `page.evaluate()` to access the Navigation Timing API. If you want to measure Playwright page load time, this is the simplest way to start:

```typescript
// perf-navigation.spec.ts
import { test, expect } from '@playwright/test';

test('measure page load timings', async ({ page }) => {
  await page.goto('https://your-app.com');
  // read navigation timing entries via page.evaluate()
});
```

You will need Node.js 18+ and Playwright 1.40+ to run all the examples in the guide.

### 63. Level Up Coding / Mohammad Faisal Khatri — Custom Fixtures in Playwright TypeScript

- Source: https://levelup.gitconnected.com/how-to-create-custom-fixtures-in-playwright-typescript-a-complete-practical-guide-4fa8b2fc2c82
- Retrieved: 2026-08-29
- Exa status: complete

July 24, 2026 · 12 min read.

Learn how to create custom fixtures for using page objects and test data in Playwright tests with a practical step-by-step guide.

If you've been writing Playwright tests for a while, you've probably noticed the same setup code appearing in multiple test files.

You create page objects, load test data, initialize helper classes, or configure users before every test. Initially, this seems manageable, but as the automation test suite grows, maintaining this duplicated setup becomes increasingly difficult.

This is exactly where custom fixtures in Playwright are especially helpful.

Fixtures are one of Playwright's most powerful features. They allow us to create reusable, maintainable, and scalable test setups that are automatically available to every test.

The tutorial covers: why custom fixtures are needed, what problems they solve, when to use a custom fixture, how to create custom fixtures for test data and page objects, pros and limitations of using custom fixtures, and best practices for building maintainable automation frameworks.

## What Problem Do Custom Fixtures Solve?

Suppose we're working on a large enterprise application, such as an online banking platform. Over the years, the automation test suite has grown to more than 300 test cases covering login, fund transfers, account management, bill payments, and transaction history.

At the beginning of almost every test, we need the same setup:

```typescript
const loginPage = new LoginPage(page);
const dashboardPage = new DashboardPage(page);
const user = users.standardUser;
await loginPage.login(user.username, user.password);
```

At first, this may not seem like a problem. But as the project evolves, requirements change:

- The LoginPage class is renamed to AuthenticationPage
- Test users are no longer stored in a local file; they are retrieved from a centralized test data service
- End-to-end tests need a logged-in user to complete the flow
- The application redirects users to a different landing page after login.

If this setup code has been duplicated across hundreds of test files, every one of those tests must be updated. Even a small architectural change can turn into hours of repetitive maintenance.

This is a classic example of violating one of the most important software engineering principles: Don't Repeat Yourself (DRY).

With custom fixtures:

- Common setup logic is maintained in a single location.
- Changes are implemented once rather than in hundreds of test files.
- Tests become shorter, cleaner, and focused solely on validating business functionality.
- The overall automation framework becomes easier to maintain as the application grows.

Instead of requiring every test to create page objects, load test data, and perform common setup steps, we define this logic once in a custom fixture. Playwright then automatically injects the required objects into every test that needs them.

In other words, custom fixtures allow us to write tests that focus on what needs to be tested, while Playwright handles how the required objects and data are prepared.

### 64. Level Up Coding / Mohammad Faisal Khatri — Browser vs BrowserContext vs Page

- Source: https://levelup.gitconnected.com/playwright-browser-vs-browsercontext-vs-page-complete-guide-with-examples-b6c771c8d371
- Retrieved: 2026-08-29
- Exa status: complete

July 22, 2026.

Automation testing in Playwright revolves around three core objects: Browser, BrowserContext, and Page. Whether we're writing our first Playwright test or designing an automation framework, understanding these concepts is essential.

Many beginners assume these objects are interchangeable because they all seem to be involved in opening a website. However, each has a distinct responsibility, and using the right one can make the tests faster and easier to maintain.

The tutorial covers: what Browser, BrowserContext, and Page are, how they relate, when to use each object, the built-in Playwright fixtures, the drawbacks of manually launching browsers, and best practices from real-world projects.

## Understanding the Relationship

```text
Browser
├── BrowserContext (User 1)
│   ├── Page (Tab 1)
│   ├── Page (Tab 2)
│   └── Page (Tab 3)
├── BrowserContext (User 2)
│   ├── Page (Tab 1)
│   └── Page (Tab 2)
└── BrowserContext (User 3)
    └── Page (Tab 1)
```

Think of it like Google Chrome on a computer:

- Browser → the Chrome browser
- BrowserContext → an individual user profile or Incognito window
- Page → a browser tab

## What is a Browser in Playwright?

A Browser represents the actual browser process, such as Chromium, Firefox, or WebKit.

Launching a browser starts the browser application, but it does not automatically create a tab or a user session.

Creating a browser — this test manually launches a new Chromium browser instance using `chromium.launch()` instead of using Playwright's built-in browser fixture:

```typescript
test("browser example", async ({}) => {
  const browser = await chromium.launch({ headless: false });
  await browser.close();
});
```

This approach gives full control over browser creation, but it bypasses Playwright's project configuration and automatic browser management.

### Browser Responsibilities

A Browser object is responsible for launching a browser such as Chromium, Firefox, or WebKit, creating one or more BrowserContext instances, managing the browser's lifecycle during test execution, and closing the browser when it is no longer needed.

### When to Use the Browser Object?

The Browser object is useful when complete control over the browser lifecycle is required: creating multiple browser contexts for cross-user testing, applying browser-level customizations, performing performance benchmarking, and developing reusable Playwright libraries or utilities.

For most functional test automation scenarios, however, there is rarely a need to create a Browser instance manually. Playwright Test automatically provides a managed browser instance through its built-in fixtures.

## What is a BrowserContext in Playwright?

A BrowserContext represents an isolated browser session within a browser instance. Each context maintains its own cookies, local storage, session storage, and cache.

### 65. Level Up Coding / Mohammad Faisal Khatri — Data-driven API Testing: Part 1

- Source: https://levelup.gitconnected.com/data-driven-api-testing-in-playwright-typescript-part-1-cc4f7deaf74a
- Retrieved: 2026-08-29
- Exa status: complete

May 23, 2026.

Modern API automation is not just about validating endpoints. It is about writing tests that are reusable and easy to maintain. One of the best ways to achieve this is through Data Driven Testing.

In real-world projects, APIs are rarely tested with a single payload. An order API, for example, may need to validate different products, quantities, tax values, and user combinations. Writing separate tests for every payload quickly becomes repetitive and difficult to manage.

## What is Data Driven Testing?

Data-driven testing is a testing technique where the same test logic is executed multiple times using different sets of input data.

Instead of creating separate tests for every scenario, the test is written once, and different datasets are supplied to it. This makes the automation framework smaller, cleaner, and easier to maintain.

Consider testing an e-commerce Create Order API. The API may need to validate multiple order details. If separate tests are written for every scenario, the framework will eventually contain a lot of duplicate code.

With data-driven testing, we can keep the test logic common while only changing the input data. This improves maintainability and significantly increases test coverage with minimal effort.

Another major advantage is readability. When a new automation engineer joins the team, it becomes much easier to understand a reusable test compared to hundreds of nearly identical test cases.

## How to write API data-driven tests using Playwright TypeScript?

Assume we are testing a Create Order API that accepts order details such as user information, product details, quantity, tax amount, and total order amount.

The first step is to create a TypeScript type for the order payload. This helps maintain a proper structure for the data and ensures that invalid fields or incorrect data types are caught early:

```typescript
type Order = {
  user_id: string;
  product_id: string;
  product_name: string;
  product_amount: number;
  qty: number;
  tax_amt: number;
  total_amt: number;
};
```

Once the type is created, we can define the test data using a constant variable. Since the data is static, keeping it directly inside the test file makes the implementation simple and readable.

### 66. Level Up Coding / Mohammad Faisal Khatri — Data-driven API Testing: Part 2

- Source: https://levelup.gitconnected.com/data-driven-api-testing-in-playwright-typescript-part-2-5d6e84f5da45
- Retrieved: 2026-08-29
- Exa status: complete

June 2, 2026.

API testing becomes much more flexible when we separate test data from the test code. Instead of hardcoding values in tests, we can store them in external files and reuse the same test logic with different datasets.

This tutorial shows how to use a JSON file as a data source in Playwright with TypeScript to test APIs efficiently. By the end, you will be able to create reusable utilities, build a model for request data, write data-driven tests, validate API responses using assertions, and learn best practices for data-driven testing with a JSON file.

## Why Use a JSON File for Test Data?

JSON is one of the most commonly used data formats in API testing, as modern APIs primarily exchange request and response data through JSON payloads.

Advantages of using JSON files for test data:

- Easy to Read: JSON files are simple and human-readable. Even non-technical team members can easily read the structure and update the file.
- Separates test data and test logic: if new test data needs to be added, the JSON file can be updated without modifying the test code.
- Reusability: the same JSON file can be reused across different tests.
- Works smoothly with APIs: since REST APIs commonly use JSON request and response bodies, JSON-based test data feels very natural in API automation.

## How to perform Data-Driven API Testing with JSON File using Playwright TypeScript

### Application Under Test

The demo uses the RESTful E-Commerce application available on GitHub, a sample application providing REST APIs for CRUD operations. The POST /addOrder API is used for demonstration. Sample request:

```json
[
  {
    "user_id": "101",
    "product_id": "P1001",
    "product_name": "Laptop",
    "product_amount": 50000,
    "qty": 1,
    "tax_amt": 5000,
    "total_amt": 55000
  }
]
```

### 67. Level Up Coding / Mohammad Faisal Khatri — GET API Requests

- Source: https://levelup.gitconnected.com/how-to-test-get-api-requests-with-playwright-typescript-7caf7bf790b2
- Retrieved: 2026-08-29
- Exa status: complete

January 8, 2026.

Playwright is a widely used open-source test automation framework developed by Microsoft. It allows developers and test automation engineers to reliably automate web applications across multiple browsers and platforms. One of its standout features is built-in API automation testing, which gives it a strong advantage over many traditional web automation frameworks.

## Installing Playwright with TypeScript

```bash
npm init playwright@latest
```

After running the above command, make sure you select "TypeScript" as the programming language.

## Application Under Test

The demo uses free, publicly available RESTful e-commerce APIs from a demo application hosted on GitHub. The project can be run locally using Node.js or Docker and provides order management APIs, including creating, updating, retrieving, and deleting orders.

## How to test GET API requests with Playwright TypeScript

Playwright provides a request API that lets us create and manage HTTP request contexts.

### Send a GET API request and verify the status code

```typescript
import { test, expect } from "@playwright/test";

test("Get Order details API test with status code check", async ({ request }) => {
  const response = await request.get("http://localhost:3004/getOrder/", {
    params: {
      user_id: "1",
    },
  });
  expect(response.status()).toBe(200);
});
```

Code walkthrough:

- `test(…)`: defines a Playwright test case; the string is the test name shown in the Playwright report.
- `async ({ request })`: uses Playwright's built-in request fixture, which injects an APIRequestContext and allows HTTP calls.
- Sending the GET request: the `request.get()` line sends an HTTP GET request to the `/getOrder/` endpoint with a `user_id` query parameter; the assertion verifies the response status is 200.

### 68. Level Up Coding / Mohammad Faisal Khatri — Global Setup for API Testing

- Source: https://levelup.gitconnected.com/global-setup-in-playwright-typescript-for-api-testing-ce7abe0173cf
- Retrieved: 2026-08-29
- Exa status: complete

February 23, 2026.

If you need to generate prerequisite test data, such as adding dummy records through an API, before executing your test suite, using Global Setup in Playwright is a clean, maintainable, and scalable approach. It ensures all required data is prepared upfront, keeping test execution structured and reliable.

## What is Global Setup?

The Global Setup in Playwright runs once before the entire test suite begins execution. It is typically used for activities such as creating test data, seeding the database, generating authentication tokens, preparing the test environment, or cleaning up old data. Since it executes only once at the start of the run, it helps ensure efficient and consistent test initialization across the suite.

## How to create a Global Setup in a Playwright TypeScript project?

### Step 1: Create a global-setup.ts file in the root folder of the project

```typescript
import { request } from "@playwright/test";
import orders from './test_data/orders.json' with {type: 'json'};

async function globalSetup() {
    console.log('Running Global Setup...');
    const apiContext = await request.newContext();
    const response = await apiContext.post("http://localhost:3004/addOrder/", {
      data: orders,
    });
    if(response.status() != 201) {
      throw new Error ("Failed to create setup test data!");
    }
    console.log('Test data created successfully');
}

export default globalSetup;
```

The `globalSetup()` function inserts test data into the Restful E-Commerce demo application. The test data for creating new orders is supplied using an orders.json file:

```json
[
  {
    "user_id": "1",
    "product_id": "79",
    "product_name": "5 star 10gm Chocobar",
    "product_amount": 5,
    "qty": 1,
    "tax_amt": 0.5,
    "total_amt": 5.5
  }
]
```

### Step 2: Register globalSetup in `playwright.config.ts`

```typescript
import { defineConfig } from '@playwright/test';
export default defineConfig({
  globalSetup: './global-setup',
});
```

The `globalSetup` property tells Playwright to execute the file once before the entire test suite runs.

tsconfig.json (note: in ESM projects `module`/`moduleResolution: NodeNext` affects how the setup file is resolved):

```json
{
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "resolveJsonModule": true,
    "types": ["node", "@playwright/test"]
  }
}
```

### 69. Level Up Coding / Mohammad Faisal Khatri — Verify Response Data

- Source: https://levelup.gitconnected.com/how-to-verify-response-data-in-playwright-typescript-4b288ad46616
- Retrieved: 2026-08-29
- Exa status: complete

January 10, 2026.

One of the most important parts of API test automation is validating the response body to ensure data integrity. This step plays a key role in functional API testing, as it helps confirm that the API is returning the right data in the expected format.

Response body validation isn't limited to a specific request type; it applies equally to POST, GET, PUT, and PATCH APIs.

### Verify the response structure

Response structure checks ensure that an API consistently returns data in the expected format, protecting the contract between backend services and their consumers. They help catch breaking changes early, such as missing or renamed fields, even when the API still returns a successful status code.

```typescript
test("GET Order details and perform structure check", async ({ request }) => {
  const response = await request.get("http://localhost:3004/getOrder/", {
    params: {
      user_id: "1",
    },
    failOnStatusCode: true,
  });
  const responseBody = await response.json();
  expect(responseBody).toHaveProperty("message");
  expect(responseBody).toHaveProperty("orders");
  expect(responseBody.orders[0]).toHaveProperty("id");
  expect(responseBody.orders[0]).toHaveProperty("product_name");
});
```

### Basic Assertions

The basic assertions validate API success and data presence, making them a good first layer of verification before deeper structure or data-level checks:

```typescript
test("Get order details and perform basic level verification", async ({ request }) => {
  const response = await request.get("http://localhost:3004/getOrder/", {
    params: {
      user_id: 1,
    },
    failOnStatusCode: true,
  });
  const responseBody = await response.json();
  expect(responseBody.message).toBe("Order found!!");
  expect(Array.isArray(responseBody.orders)).toBeTruthy();
  expect(responseBody.orders.length).toBeGreaterThan(0);
});
```

The assertion layers progress from status, to top-level shape, to required properties, types, and values; a schema check protects the contract but does not replace business invariants.

### 70. Level Up Coding / Mohammad Faisal Khatri — Build a Local AI QA Engineer with Playwright MCP

- Source: https://levelup.gitconnected.com/build-your-own-local-ai-qa-engineer-with-docker-ollama-librechat-and-playwright-mcp-1a254fab91d0
- Retrieved: 2026-08-29
- Exa status: complete

July 17, 2026.

Artificial Intelligence is rapidly transforming software testing by enabling QA engineers to generate test cases and test plans, automate browser interactions, analyze and debug failures, and execute complex testing workflows using simple natural-language prompts.

While cloud-based AI assistants offer impressive capabilities, they often require subscriptions and sharing potentially sensitive application data with third-party services.

Running an AI-powered testing assistant locally addresses these concerns by providing better privacy, lower operating costs, and complete control over the testing environment.

This tutorial shows how to build a Local AI QA Engineer using Docker, Ollama, Qwen3:8b, LibreChat, and Playwright MCP — performing browser automation and interacting with web applications using natural language, without cloud-based AI services.

## Understanding the Architecture

Every interaction begins with the user. For example, a user enters a prompt in LibreChat, such as "Open the Playwright website and click the 'Get Started' button."

LibreChat serves as the conversational interface through which users interact with the AI assistant. Rather than processing the request itself, it forwards the prompt to a locally hosted large language model, Qwen3:8b, running via Ollama.

After receiving the prompt, Qwen3:8b interprets the user's intent and generates a step-by-step execution plan. Instead of interacting with the browser directly, the model determines which tools are required and communicates those instructions using the Model Context Protocol (MCP).

These MCP requests are handled by the Playwright MCP Server, which acts as the bridge between the language model and the browser. It translates the AI-generated instructions into executable Playwright commands.

The Playwright MCP Server then launches a Chrome browser and performs the requested actions. Depending on the prompt, it can navigate to websites, click buttons, complete forms, extract text from web pages, capture screenshots, and execute a wide range of browser automation tasks.

Once the browser completes the requested operations, the execution results are returned to Qwen3:8b. The language model analyzes the browser output and transforms the technical details into a clear, human-readable response, which LibreChat presents to the user.

### 71. Level Up Coding / Mohammad Faisal Khatri — Vibe Testing with Playwright MCP

- Source: https://levelup.gitconnected.com/vibe-testing-with-playwright-mcp-testing-ux-with-ai-agents-6b1be03b388a
- Retrieved: 2026-08-29
- Exa status: complete

April 24, 2026.

With the rapid adoption of AI coding agents such as Claude and GitHub Copilot, modern test automation is no longer just about writing scripts; it's about working alongside AI to generate, maintain, and optimize tests faster.

Vibe Testing is becoming important as it goes beyond functional checks to validate the overall user experience. This includes visual consistency, layout alignment, accessibility, performance feel, and even small UI details that impact user perception.

## What is Vibe Testing?

Vibe testing focuses on evaluating the overall user experience beyond functional correctness. When combined with AI agents like Claude, natural language prompts can be used to describe and execute these experience-focused test scenarios.

Instead of just checking whether the app works as expected, vibe testing looks at the overall experience from a user's point of view:

- Is it easy to navigate through pages, buttons, and menus?
- Are there annoying interruptions like pop-ups or sudden page reloads?
- Does the app feel smooth, simple, and pleasant to use?

In short, it's less about "Does it work?" and more about "Is it enjoyable and effortless to use?"

## How does AI enhance vibe testing?

AI enhances vibe testing by generating realistic and diverse test cases that mirror actual user behavior. Through predictive test execution and smart test prioritization, AI focuses on the most critical user journeys and high-risk areas based on usage patterns and historical defects.

In addition, automatic failure analysis speeds up root cause identification. AI-powered visual testing detects small UI issues that can affect how users perceive the application. Accessibility testing ensures the application remains usable for everyone.

Agentic AI can learn from user behavior, adapt test scenarios dynamically, prioritize what matters most, and even identify potential issues.

## What is Playwright MCP

Playwright MCP is a Model Context Protocol server that allows AI agents and large language models (LLMs) to control a browser using Playwright.

### 72. Vitali Haradkou — Angular-Aware Playwright Selectors (canonical Hashnode page)

- Source: https://vitalicset.hashnode.dev/angular-aware-playwright-selectors-query-components-by-input-signals-and-state
- Retrieved: 2026-08-29
- Exa status: complete

Updated April 2, 2026. This is the canonical Hashnode page; the DEV Community copy is listed separately below as a mirror.

Angular gives us a powerful component model: typed `@Input()` properties, reactive `signal()` values, `@Output()` event emitters, and declarative directives. But when we write E2E tests, we typically throw all of that away and fall back to CSS selectors and DOM queries.

What if Playwright could understand Angular's component model natively?

That's the question `@playwright-labs/selectors-angular` answers.

## The Problem with Traditional Angular Testing

Consider this component:

```typescript
@Component({
  selector: "app-button",
  template: `<button [disabled]="disabled" [class]="type">{{ label }}</button>`,
})
export class ButtonComponent {
  @Input() label = "";
  @Input() disabled = false;
  @Input() type: "primary" | "danger" | "secondary" = "primary";
  @Output() clicked = new EventEmitter<void>();
}
```

A traditional Playwright test might look like:

```typescript
// Find the delete button
const deleteBtn = page.locator('button.danger[disabled]');
await expect(deleteBtn).toHaveCount(1);
```

This test is implicitly asserting things about the template implementation: that the `type` input maps to a CSS class called `danger`, that `disabled` renders as a DOM attribute. If the template changes — even without changing behavior — the test breaks.

What we actually want to test is: does a `ButtonComponent` with `type="danger"` and `disabled=true` exist?

```typescript
const deleteBtn = page.locator('angular=app-button[type="danger"][disabled]');
await expect(deleteBtn).toHaveCount(1);
```

## How It Works

The package registers a custom Playwright selector engine called `angular`. When Playwright encounters `angular=...`, it hands the selector string to the engine, which:

1. Parses the selector — extracts the component tag name and attribute conditions using a custom recursive descent parser
2. Walks the DOM — uses `document.createTreeWalker` to visit every element, including shadow DOM roots
3. Queries Angular's DevTools API — calls `window.ng.getComponent(element)` to get the component instance
4. Evaluates conditions — checks each attribute condition against the component instance's properties
5. Returns matching elements — only elements where all conditions pass

The entire evaluation happens inside the browser, so it's fast and works with Angular's live component tree.

## Installation and Setup

```bash
npm install -D @playwright-labs/selectors-angular
```

### Option A: Manual Registration

```typescript
// playwright.config.ts
import { selectors, defineConfig
```

### 73. Vitali Haradkou — Testing Angular Components by Properties with Playwright (DEV mirror)

- Source: https://dev.to/vitalicset/testing-angular-components-by-properties-with-playwright-33jj
- Retrieved: 2026-08-29
- Exa status: complete

Published April 2, 2026. DEV Community mirror/alias of the canonical Hashnode page (entry 71); not counted as a second publication.

Most Angular E2E tests look like this:

```typescript
await page.locator('[data-testid="submit-btn"]').click();
await page.locator('.user-card:nth-child(2) h2').textContent();
```

You end up fighting CSS specificity and fragile DOM structure instead of testing Angular behavior. What if you could do this instead?

```typescript
await page.locator('angular=app-button[label="Submit"]').click();
await page.locator('angular=app-user-card[user.role="admin"]').textContent();
```

That's exactly what `@playwright-labs/selectors-angular` provides.

## What Is It?

A custom Playwright selector engine that queries Angular components by their actual component properties — `@Input()` values, signals, and nested objects — rather than DOM attributes or CSS classes.

It hooks into Angular's DevTools API (`window.ng`) which is available in development builds.

## Installation

```bash
npm install -D @playwright-labs/selectors-angular
```

## Setup

Register the engine in your `playwright.config.ts`:

```typescript
import { defineConfig } from "@playwright/test";
import { AngularEngine } from "@playwright-labs/selectors-angular";

export default defineConfig({
  use: { /* ... */ },
});

// Register once at module level
import { selectors } from "@playwright/test";
selectors.register("angular", AngularEngine);
```

Or use the bundled `test` fixture which handles registration automatically:

```typescript
import { test, expect } from "@playwright-labs/selectors-angular";

test("my angular test", async ({ page, $ng }) => {
  // $ng and engine are auto-registered
});
```

## The `angular=` Selector Syntax

The syntax mirrors CSS attribute selectors:

```text
angular=<component-tag>[property="value"][another="value"]
```

Operators:

| Operator | Meaning | Example |
| --- | --- | --- |
| `[prop]` | truthy check | `[disabled]` |
| `[prop="val"]` | exact match | `[label="Submit"]` |
| `[prop*="val"]` | contains | `[type*="ary"]` |
| `[prop^="val"]` | starts with | `[type^="prim"]` |
| `[prop$="val"]` | ends with | `[type$="ger"]` |
| `[prop~="val"]` | word in list | `[classes~="active"]` |

Regex and case-insensitive matching are also supported:

```typescript
page.locator('angular=app-button[label=/^Sub/i]')
page.locator('angular=app-button[label="submit" i]')
```

Nested properties use dot notation:

```typescript
page.locator('angular=app-user-card[user.role="admin"]')
page.locator('angular=app-card[config.theme.color="dark"]')
```

Multiple conditions (AND) — chain attribute blocks; all must match:

```typescript
page.locator('angular=app-button[type="danger"][disabled]')
```

The `$ng` fixture wraps standard Playwright locators with Angular-aware introspection methods (read `@Input` values, signal values, list inputs, check directives).

### 74. Vitali Haradkou — Blog index (Vercel)

- Source: https://blog-vitaliharadkous-projects.vercel.app/blog
- Retrieved: 2026-08-29
- Exa status: complete

Index page of the author's Vercel-hosted blog. The recheck found no new, unambiguously authored Playwright/TypeScript entry beyond the positions already indexed; Hashnode/DEV aliases are not counted twice. Listing:

- @playwright-labs/reporter-slack: Rich Slack Notifications for Playwright Test Runs (Apr 30, 2026 · 9 min read) — structured, interactive Slack messages with test results, environment context, and auto-masked secrets; setup, three built-in templates, transport options, and custom templates.
- Type-safe SQL in Playwright tests: a compile-time FSM, a branded string, and zero leaked connections (Apr 13, 2026 · 9 min read) — the story behind @playwright-labs/fixture-sql.
- I Built an Angular-Aware Playwright Selector Engine (Apr 2, 2026 · 6 min read) — why @playwright-labs/selectors-angular was built, how it works internally, and what problems it solves for Angular E2E testing.
- I Got Tired of Writing the Same Container Boilerplate, So I Packaged It (Mar 26, 2026 · 6 min read) — how @playwright-labs/fixture-testcontainers eliminates Docker lifecycle boilerplate, and why real containers beat mocks.
- Modern TypeScript Decorators: TC39 Stage 3 - No More Reflect-Metadata! (Feb 20, 2026 · 12 min read) — modern TypeScript decorators with examples from a real Playwright testing framework; context.metadata, type inference, and migration from legacy decorators.

### 75. Vitaliy Potapov — Posts index

- Source: https://vitalets.github.io/posts/
- Retrieved: 2026-08-29
- Exa status: complete

Index page listing all posts on the author's blog. The recheck confirmed that fixtures, workers, fully-parallel and BDD workflow were already described in iteration 6; no new Playwright article was found in this round. Listing:

- Why I Prefer BDD over SDD for Agentic Development (10 Jul, 2026) — a practical workflow for building features with executable Gherkin scenarios instead of markdown specs.
- Playwright in Pictures: How Fixtures Work (25 Jun, 2026) — a visual guide to Playwright fixtures: setup, teardown, auto-mode, dependencies, and test/worker scopes.
- Playwright in Pictures: Why Workers Restart? (updated 1 May, 2026) — why Playwright sometimes throws away a worker and starts a new one, with timelines for failures, hooks, projects, and test.use.
- Playwright in Pictures: Fully Parallel Mode (updated 29 Apr, 2026) — what really changes when you enable fullyParallel in Playwright, and why more parallelism can still make a test run slower.
- Turbopack: A Better Way to Inline SVG in Next.js 16 (19 Nov, 2025).

### 76. Sajith Dilshan — Playwright Auto-Waiting (LinkedIn promo)

- Source: https://www.linkedin.com/posts/sajith-dilshan_playwright-testautomation-qualityassurance-activity-7489870161777152000-hWVW
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-03. Promotes the author's canonical Medium article (listed separately below), which was already in the index; the recheck confirms actionability, web-first assertions and no hard waits, but adds no new content.

Playwright Auto-Waiting – The Secret Behind Stable and Reliable Test Automation

One of the biggest reasons Playwright has become a favorite among QA Engineers is its powerful Auto-Waiting mechanism.

Many automation engineers coming from Selenium are used to dealing with:

❌ Hard waits
❌ Explicit waits everywhere
❌ Flaky tests caused by timing issues

Playwright takes a different approach.

Before performing actions like clicking, filling, or hovering, it automatically checks whether the element is:

✅ Visible
✅ Stable
✅ Enabled
✅ Receiving events
✅ Editable (when required)

This built-in intelligence helps reduce flaky tests, improve execution reliability, and allows QA engineers to focus on validating business functionality instead of fighting synchronization issues.

In the article, the author breaks down: what Auto-Waiting is, actionability checks explained, auto-retrying assertions, common mistakes engineers make, real-world examples from a QA perspective, and best practices for writing stable Playwright tests.

Full article: https://lnkd.in/gFmej5ev

Engagement: 9 reactions.

### 77. Sajith Dilshan — Playwright Auto-Waiting: The Secret Behind Stable and Reliable Test Automation (canonical)

- Source: https://medium.com/@sajith-dilshan/playwright-auto-waiting-the-secret-behind-stable-and-reliable-test-automation-bd3987a3156e
- Retrieved: 2026-08-29
- Exa status: complete

July 31, 2026. Canonical Medium article promoted by the LinkedIn post above.

## Introduction

One of the biggest challenges in test automation is dealing with timing issues.

If you have worked with Selenium or other automation frameworks, you have probably seen tests fail because an element was not ready when the script tried to interact with it. To solve this, teams often add explicit waits, sleep statements, or custom retry mechanisms.

The author remembers writing code like this:

```typescript
await page.waitForTimeout(5000);
await page.click('#submit');
```

It worked sometimes. Other times, it didn't.

This approach makes tests slower, harder to maintain, and more flaky.

This is where Playwright changes the game: Auto-Waiting automatically waits for elements to be ready before performing actions.

## What is Auto-Waiting?

Auto-Waiting is Playwright's ability to automatically wait until an element becomes ready for interaction.

Instead of immediately clicking an element, Playwright first checks whether the element is:

- Present in the DOM
- Visible
- Stable
- Able to receive events
- Enabled

Only after these conditions are satisfied will Playwright perform the action.

For example:

```typescript
await page.locator('#login-button').click();
```

At first glance, this looks like a simple click. However, before clicking, Playwright automatically performs multiple validations to ensure the action will succeed.

## Why Auto-Waiting Matters

Imagine a real-world scenario:

1. User fills the registration form.
2. Application sends data to the server.
3. Submit button remains disabled while validation occurs.
4. Button becomes enabled after validation completes.

Without Auto-Waiting, `await page.click('#submit');` may fail because the button is still disabled. With Playwright, `await page.locator('#submit').click();` waits until the button becomes actionable before clicking it.

The result: fewer flaky tests, less waiting code, faster execution, better maintainability.

## Understanding Actionability Checks

Before performing an action, Playwright validates the target element using Actionability Checks — safety checks before interacting with an element.

#### 1. Element Must Be Visible

Playwright verifies that the element can actually be seen by the user. Not-visible examples:

```html
<button style="display:none">Submit</button>
<button style="visibility:hidden">Submit</button>
```

Interesting fact: the following element is considered visible:

```html
<button style="opacity:0">Submit</button>
```

Although invisible to the eye, it still occupies space on the page, so Playwright considers it visible.

#### 2. Element Must Be Stable

Playwright waits for animations or movements to finish. If a button is still sliding into view, Playwright waits until its position becomes stable. This prevents clicks from happening at the wrong coordinates.

#### 3. Element Must Receive Events

An element may be visible but still blocked by another component:

```html
<div class="loading-overlay"></div>
<button>Save</button>
```

The button exists and is visible, but a loading overlay covers it. If a real user clicks, the overlay receives the click — Playwright enforces the same rule.

### 78. Sajith Dilshan — TypeScript Module Systems Explained (LinkedIn promo)

- Source: https://www.linkedin.com/posts/sajith-dilshan_typescript-module-systems-explained-commonjs-activity-7474649871942307840-HVje
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-06-22. Promotes the already-indexed article about CommonJS/ESM and `NodeNext`.

🧩 TypeScript Module Systems Explained: CommonJS vs ES Modules

Confused about imports in TypeScript? 😅
This beginner-friendly guide explains the difference between CommonJS and ES Modules (NodeNext), when to use each, and their benefits and drawbacks. Perfect for building Playwright frameworks or any TypeScript project!

Key takeaways:

- CommonJS → simple, stable, no `.js` needed
- ES Modules → modern, strict, requires `.js` imports
- ts-node is only for standalone scripts, not needed for Playwright

Read the full article: https://lnkd.in/gV4_9hxf

Engagement: 5 reactions.

### 79. Sajith Dilshan — Scalable Authentication beyond `globalSetup` (LinkedIn promo)

- Source: https://www.linkedin.com/posts/sajith-dilshan_playwright-testautomation-qaengineering-activity-7484796747332931584-bgzX
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-07-20. Promotes the already-indexed article about setup projects + `storageState`; the canonical Medium article is not duplicated here.

🚀 Scalable Authentication in Playwright: Moving Beyond globalSetup

As QA engineers, globalSetup often feels like the easiest way to handle authentication in Playwright.

But as frameworks scale, it starts to show cracks — especially with CI/CD, parallel execution, and multi-role applications.

In the article, the author breaks down:

✅ Why globalSetup breaks down at scale
✅ Playwright test runner lifecycle (and why it matters)
✅ Common issues with debugging, retries, and CI
✅ A better approach using setup projects + storageState
✅ Scalable authentication for multi-role applications

The guide is based on real-world experience building and maintaining Playwright frameworks in production environments.

Full article: https://lnkd.in/gscJh9Yp

Engagement: 8 reactions.

### 80. Artem Bondar — Course refresh announcement

- Source: https://www.linkedin.com/posts/artem-bondar_hey-guys-just-fyi-i-recently-refreshed-activity-7490476134573412352-rkQH
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-04.

Hey guys, just FYI, I recently refreshed my Playwright course, brought it up to date, and fixed minor mistakes and inconsistencies that drifted over time.

The original course was recorded in 2023, and it was time for an update.

P.S. The "Advanced" section is not updated yet; planning to complete it by the end of August.

Engagement: 141 reactions; commenters ask for a release-note-style list of changed sections.

### 81. Artem Bondar / Bondar Academy — Blog index

- Source: https://bondaracademy.com/blog
- Retrieved: 2026-08-29
- Exa status: complete

Index page confirming the Bondar Academy blog listing; the recheck found no new, unduplicated entry beyond the earlier positions about projects, fixtures, API, POM, locators, assertions, data-driven testing, storage state and CI. Listing (newest first):

- How to Mock API Responses in Playwright (8 min, July 13, 2026)
- Playwright Projects Configuration Guide: Beyond Cross-Browser Testing (9 min, July 7, 2026)
- How to Test PDF in Playwright (6 min, June 29, 2026)
- How to Read Excel Files in Playwright (5 min, June 22, 2026)
- Playwright Storage State: Stop Logging In Before Every Test (6 min, June 15, 2026)
- Cypress cy.prompt AI Code Generation: Honest Review (8 min, June 9, 2026)
- Data-Driven Testing in Playwright: Stop Repeating Your Tests (7 min, June 1, 2026)
- Playwright AI Agents Review: Planner, Generator, and Healer Tested (7 min, May 18, 2026)
- Playwright Codegen Test Recorder: Generate Tests in Minutes (11 min, May 12, 2026)
- How to Install Playwright in VS Code (6 min, May 4, 2026)
- Playwright GitHub Actions Setup from Scratch (8 min, April 27, 2026)
- How to Fix Playwright Flaky Tests: 3 Common Causes (11 min, April 20, 2026)

### 82. Joseph Ward — Blog index

- Source: https://josephward.tech/blog/
- Retrieved: 2026-08-29
- Exa status: complete

Index page of Joseph Ward's blog ("Polyglot Testing Technologist"). The recheck confirms the relevant Playwright entries and that no new post appeared since iteration 6:

- Looking Behind Playwright's Magic (07 Jul 2026) — "A Playwright click looks simple:"
- Why Simple UI Tests Become Slow (30 Jun 2026) — "Sometimes, UI tests take longer than the code suggests they should."
- Testing the Data, Not Just the Migration (12 Aug 2026, described in iteration 6)
- Harmonising Selenium with Playwright and Cypress: A Journey Through Network Event Handling (21 Jan 2024)
- Designing a Sub-1kb Homepage (02 May 2023), Green Testing (07 Apr 2023), and older testing essays from 2019–2020.

### 83. Yevhen Laichenkov — Posts index

- Source: https://elaichenkov.github.io/posts/
- Retrieved: 2026-08-29
- Exa status: complete

Index page listing all posts on the author's blog; still two relevant Playwright articles, no new Playwright/TypeScript entry:

- TIL: Playwright step decorator for better test reporting (16 Feb, 2026) — a TypeScript decorator that wraps page object methods with Playwright test steps, giving clear, readable trace reports with parameter interpolation.
- TIL: git worktree lets you work on multiple branches at once (16 Feb, 2026)
- 17 Playwright Testing Mistakes You Should Avoid (14 Feb, 2026) — a practical guide to 17 Playwright mistakes that cause flaky, slow, and hard-to-maintain tests.

### 84. Yevhen Laichenkov — 17 Playwright Testing Mistakes You Should Avoid

- Source: https://elaichenkov.github.io/posts/17-playwright-testing-mistakes-you-should-avoid
- Retrieved: 2026-08-29
- Exa status: complete

February 14, 2026.

I keep seeing the same pattern: tests start flaking and the blame goes to data, CI, browsers, or infrastructure. Then the test gets "fixed" with sleeps, forced actions that skip actionability checks, and custom retry or wait helpers that reimplement what Playwright already provides. Sometimes deprecated APIs even make it into fresh code, which guarantees maintenance trouble later. In this article, I go through the most common mistakes I see in projects.

TL;DR

1. Don't make tests depend on each other with `test.describe.serial`
2. Keep page-object actions simple. Avoid returning new page objects from every action
3. Add eslint-plugin-playwright and catch bad patterns before they make it into your codebase
4. Prefer positive assertions `toBeHidden` over negative ones `not.toBeVisible` when possible
5. Use `waitForFunction` only for truly custom conditions
6. Use `expect.poll` for specific polling scenarios, not basic DOM checks
7. Make locators strict with `{ exact: true }`
8. Stop using deprecated APIs and options `waitForNavigation`, `waitForSelector`
9. When using `toPass`, keep inner assertion timeouts short
10. Don't write manual retry loops. Use `toPass` or `expect.poll`
11. For `waitForResponse`: listen first, trigger request second, await third
12. Don't try to solve UI issues with `{ force: true }`
13. Don't pre-wait before actions that already auto-wait `click`, `fill`, `check`
14. Stop treating `networkidle` as "page is ready."
15. Stop using sleeps like `waitForTimeout`.
16. Use web-first assertions instead of one-shot checks `isVisible`, `textContent`, `toBe`
17. Every test needs assertions. If there are no checks, it's just a script.

## 1. Forgetting assertions in tests

```typescript
// ❌ Bad
test('should open the page', async ({ page }) => {
  await page.goto('/dashboard');
  // assuming that if the goto method doesn't throw, the page is open,
  // but we don't actually check anything
});
```

```typescript
// ✅ Better
test('should open dashboard page and verify text', async ({ page }) => {
  await page.goto('/dashboard');
  await expect(page.getByText('Dashboard')).toBeVisible();
});
```

A test without assertions is not really a test, it's just a script that performs actions without verifying any outcomes.

## 2. Using one-shot checks instead of web-first assertions

```typescript
// ❌ Bad
expect(await page.getByTestId('status').isVisible()).toBeTruthy();
expect(await page.getByTestId('name').textContent()).toBe('Alice');
expect(page.url()).toMatch(/\/dashboard$/);
expect(await page.locator('li').count()).toBe(5);
expect(await page.getByRole('button', { name: 'Submit' }).isEnabled()).toBe(true);
```

```typescript
// ✅ Better
await expect(page.getByTestId('status')).toBeVisible();
await expect(page.getByTestId('name')).toHaveText('Alice');
await expect(page).toHaveURL(/\/dashboard$/);
await expect(page.locator('li')).toHaveCount(5);
await expect(page.getByRole('button', { name: 'Submit' })).toBeEnabled();
```

Web-first assertions retry until timeout. One-shot checks pass/fail based on timing luck. The article continues through the remaining 15 mistakes with the same ❌/✅ pattern.

### 85. Yevhen Laichenkov — TIL: Playwright step decorator for better test reporting

- Source: https://elaichenkov.github.io/posts/til-playwright-step-decorator
- Retrieved: 2026-08-29
- Exa status: complete

February 16, 2026.

If you use page objects in Playwright, your trace reports can quickly become a wall of low-level actions. You can fix this by wrapping methods with `test.step()`, but doing it manually everywhere is tedious.

Here's a `@step()` decorator that does it automatically:

```typescript
import { test } from "@playwright/test";

type Method<This, Args extends unknown[], Return> = (
  this: This,
  ...args: Args
) => Promise<Return>;

type MethodDecoratorContext<
  This,
  Args extends unknown[],
  Return,
> = ClassMethodDecoratorContext<This, Method<This, Args, Return>>;

function extractParams(fn: Function): string[] {
  const fnStr = fn.toString();
  const argsMatch = fnStr.match(/\(([^)]*)\)/);

  if (!argsMatch?.[1]) return [];

  return argsMatch[1]
    .split(",")
    .map(param => param.trim())
    .filter(Boolean)
    .map(param => param.replace(/=.*$/, "").trim())
    .map(param => param.replace(/^\.\.\./, "").trim());
}

function interpolateParams<Args extends unknown[]>(
  message: string,
  fn: Function,
  args: Args
): string {
  const paramNames = extractParams(fn);

  return message.replace(/\{\{(\w+)\}\}/g, (_, paramName) => {
    const index = paramNames.indexOf(paramName);
    if (index === -1 || index >= args.length) return `{{${paramName}}}`;

    const value = args[index];
    return typeof value === "object" ? JSON.stringify(value) : String(value);
  });
}

export function step<
  This extends { constructor: { name: string } },
  Args extends unknown[],
  Return,
>(message?: string) {
  return (
    value: Method<This, Args, Return>,
    context: MethodDecoratorContext<This, Args, Return>
  ) => {
    const target = value;
    const name = context.name ?? "unknown";

    function replacementMethod(
      this: This,
      ...args: Args
    ): Promise<Return> {
      const defaultName = `${this.constructor.name}.${String(name)}`;
      const stepName = message
        ? interpolateParams(message, target, args)
        : defaultName;

      return test.step(stepName, async () => {
        return await target.call(this, ...args);
      });
    }

    return replacementMethod as Method<This, Args, Return>;
  };
}
```

Now every method decorated with `@step()` shows up as a named step in Playwright traces and reports:

```typescript
class LoginPage {
  @step("Log in with {{username}}")
  async logIn(username: string, password: string) {
    // ...
  }
}
```

The `{{paramName}}` syntax uses the parameter names from the function signature, so `{{username}}` resolves to the actual value passed at runtime — instead of a generic step name, you see `"Log in with john.doe"`.

### 86. Butch Mayhew — Do not stop reading agent-written code

- Source: https://www.linkedin.com/posts/butchmayhew_softwaretesting-aiinqa-qa-activity-7490115981323935744-S_1o
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-08-03.

In the AI in QA newsletter this week I share my thoughts on the recent trend "not reading code written by agents".

Where this idea thrives is on personal projects, internal tools, prototypes, simple software, or other projects which have a low footprint, risk factor, and can be easily verified through automated checks. Think about that tool that does 1 or 2 things and that is it. Shipping Enterprise level software in this fashion just seems irresponsible.

Full take plus 18 other articles/links: https://lnkd.in/gGKEs6gp

Engagement: 4 reactions.

### 87. Butch Mayhew — Playwright and AI in QA: Beyond Test Generation

- Source: https://www.linkedin.com/posts/butchmayhew_orchestrating-ai-native-testing-with-playwright-activity-7478073686026207234-DD7I
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-07-01.

I've been seeing the AI in QA conversation move past the first obvious question. Yes, AI can generate Playwright tests. The more useful question is whether those tests are giving the team real signal once they land in a suite with CI, shared fixtures, test data, locator drift, retries, and flaky failures.

That is where Playwright gets interesting. A generated test that passes once is not the same thing as a test that belongs in the framework. It still has to fit how the team sets up state, chooses locators, writes assertions, debugs traces, and decides whether a failure is the app, the test, or the data. That is the part AI needs context for. Otherwise, you are mostly getting faster test file creation.

That is why the author is looking forward to "Orchestrating AI-Native Testing with Playwright" with Debbie O'Brien and Ivan Davidov — focused on using Playwright and AI together in a workflow that can actually run, fail, debug, and improve inside real testing work.

Commenters report AI growing tests 300% after strict locator and assertion rules were set, with humans still owning framework decisions like state setup and failure triage.

### 88. Butch Mayhew — Manual testing is still valuable

- Source: https://www.linkedin.com/posts/butchmayhew_playwright-testautomation-qaengineering-activity-7473717480301977600-X6nL
- Retrieved: 2026-08-29
- Exa status: complete

LinkedIn post, 2026-06-19.

Someone asked me: "Is manual testing still valuable?"

My answer is yes. But manual testing alone is no longer enough if you want to grow into modern QA, SDET, or automation roles.

The next career jump is learning how to build automation systems that teams can actually scale. That is why Playwright has become such an important skill for QA engineers.

Companies are now looking for people who can work across TypeScript, UI and API testing, CI/CD pipelines, fixtures, page objects, test data, and AI-assisted workflows.

And with AI entering testing, the skill set is changing again.

It is not enough to ask AI to generate tests faster. You need to know how to give AI the right structure and context so it can work inside your Playwright workflow without creating brittle scripts.

That is exactly what the "Orchestrating AI-Native Testing with Playwright" hands-on workshop focuses on: building Playwright workflows where AI supports the testing system, instead of creating automation the team has to clean up later.

Engagement: a commenter notes that with AI entering the mix, "context engineering" is becoming as vital as a clean Page Object design pattern — without anchoring AI to a strict structural baseline, you are automating technical debt at scale.

### 89. Playwright — Best Practices (official docs)

- Source: https://playwright.dev/docs/best-practices
- Retrieved: 2026-08-29
- Exa status: complete

## Testing philosophy

### Test user-visible behavior

Automated tests should verify that the application code works for the end users, and avoid relying on implementation details such as things which users will not typically use, see, or even know about such as the name of a function, whether something is an array, or the CSS class of some element. The end user will see or interact with what is rendered on the page, so your test should typically only see/interact with the same rendered output.

### Make tests as isolated as possible

Each test should be completely isolated from another test and should run independently with its own local storage, session storage, data, cookies etc. Test isolation improves reproducibility, makes debugging easier and prevents cascading test failures.

In order to avoid repetition for a particular part of your test you can use before and after hooks. Within your test file add a before hook to run a part of your test before each test such as going to a particular URL or logging in to a part of your app. This keeps your tests isolated as no test relies on another. However it is also ok to have a little duplication when tests are simple enough, especially if it keeps your tests clearer and easier to read and maintain.

```javascript
import { test } from '@playwright/test';

test.beforeEach(async ({ page }) => {
  // Runs before each test and signs in each page.
  await page.goto('https://github.com/login');
  await page.getByLabel('Username or email address').fill('username');
  await page.getByLabel('Password').fill('password');
  await page.getByRole('button', { name: 'Sign in' }).click();
});

test('first', async ({ page }) => {
  // page is signed in.
});

test('second', async ({ page }) => {
  // page is signed in.
});
```

You can also reuse the signed-in state in the tests with a setup project. That way you can log in only once and then skip the log in step for all of the tests.

### Avoid testing third-party dependencies

Only test what you control. Don't try to test links to external sites or third-party servers that you do not control. Not only is it time consuming and can slow down your tests, but also you cannot control the content of the page you are linking to, or if there are cookie banners or overlay pages or anything else that might cause your test to fail.

### 90. Playwright — Fixtures (official docs)

- Source: https://playwright.dev/docs/test-fixtures
- Retrieved: 2026-08-29
- Exa status: complete

## Introduction

Playwright Test is based on the concept of test fixtures. Test fixtures are used to establish the environment for each test, giving the test everything it needs and nothing else. Test fixtures are isolated between tests. With fixtures, you can group tests based on their meaning, instead of their common setup.

### Built-in fixtures

You have already used test fixtures in your first test:

```javascript
import { test, expect } from '@playwright/test';

test('basic test', async ({ page }) => {
  await page.goto('https://playwright.dev/');
  await expect(page).toHaveTitle(/Playwright/);
});
```

The `{ page }` argument tells Playwright Test to set up the `page` fixture and provide it to your test function.

Pre-defined fixtures:

| Fixture | Type | Description |
| --- | --- | --- |
| page | Page | Isolated page for this test run. |
| context | BrowserContext | Isolated context for this test run. The `page` fixture belongs to this context as well. |
| browser | Browser | Browsers are shared across tests to optimize resources. |
| browserName | string | The name of the browser currently running the test. Either `chromium`, `firefox` or `webkit`. |
| request | APIRequestContext | Isolated APIRequestContext instance for this test run. |

### With fixtures

Fixtures have a number of advantages over before/after hooks:

- Fixtures encapsulate setup and teardown in the same place so it is easier to write. If you have an after hook that tears down what was created in a before hook, consider turning them into a fixture.
- Fixtures are reusable between test files — define them once and use them in all your tests.
- Fixtures are on-demand — Playwright Test sets up only the ones needed by a test and nothing else.
- Fixtures are composable — they can depend on each other to provide more complex behavior.

### 91. Playwright — Assertions (official docs)

- Source: https://playwright.dev/docs/test-assertions
- Retrieved: 2026-08-29
- Exa status: complete

## Introduction

Playwright includes test assertions in the form of the `expect` function. To make an assertion, call `expect(value)` and choose a matcher that reflects the expectation. There are many generic matchers like `toEqual`, `toContain`, `toBeTruthy` that can be used to assert any conditions.

```javascript
expect(success).toBeTruthy();
```

Playwright also includes web-specific async matchers that will wait until the expected condition is met:

```javascript
await expect(page.getByTestId('status')).toHaveText('Submitted');
```

Playwright will re-test the element with the test id of `status` until the fetched element has the `"Submitted"` text, until the condition is met or until the timeout is reached. The timeout can be configured once via the `testConfig.expect` value in the test config. By default, the timeout for assertions is set to 5 seconds.

## Auto-retrying assertions

The following assertions will retry until the assertion passes, or the assertion timeout is reached. Retrying assertions are async, so you must `await` them.

| Assertion | Description |
| --- | --- |
| `await expect(locator).toBeAttached()` | Element is attached |
| `await expect(locator).toBeChecked()` | Checkbox is checked |
| `await expect(locator).toBeDisabled()` | Element is disabled |
| `await expect(locator).toBeEditable()` | Element is editable |
| `await expect(locator).toBeEmpty()` | Container is empty |
| `await expect(locator).toBeEnabled()` | Element is enabled |
| `await expect(locator).toBeFocused()` | Element is focused |
| `await expect(locator).toBeHidden()` | Element is not visible |
| `await expect(locator).toBeInViewport()` | Element intersects viewport |
| `await expect(locator).toBeVisible()` | Element is visible |
| `await expect(locator).toContainText()` | Element contains text |
| `await expect(locator).toContainClass()` | Element has specified CSS classes |
| `await expect(locator).toHaveAccessibleDescription()` | Element has a matching accessible description |
| `await expect(locator).toHaveAccessibleName()` | Element has a matching accessible name |
| `await expect(locator).toHaveAttribute()` | Element has a DOM attribute |
| `await expect(locator).toHaveClass()` | Element has specified CSS class property |
| `await expect(locator).toHaveCount()` | List has exact number of children |
| `await expect(locator).toHaveCSS()` | Element has CSS property |
| `await expect(locator).toHaveId()` | Element has an ID |
| `await expect(locator).toHaveJSProperty()` | Element has a JavaScript property |
| `await expect(locator).toHaveRole()` | Element has a specific ARIA role |
| `await expect(locator).toHaveScreenshot()` | Element has a screenshot |
| `await expect(locator).toHaveText()` | Element matches text |
| `await expect(locator).toHaveValue()` | Input has a value |
| `await expect(locator).toHaveValues()` | Select has options selected |
| `await expect(locator).toMatchAriaSnapshot()` | Element matches the Aria snapshot |
| `await expect(page).toMatchAriaSnapshot()` | Page matches the Aria snapshot |
| `await expect(page).toHaveScreenshot()` | Page has a screenshot |
| `await expect(page).toHaveTitle()` | Page has a title |
| `await expect(page).toHaveURL()` | Page has a URL |
| `await expect(response).toBeOK()` | Response has an OK status |

## Non-retrying assertions

These assertions allow testing any conditions, but do not auto-retry. Most of the time, web pages show information asynchronously, and using non-retrying assertions can lead to a flaky test. Prefer auto-retrying assertions whenever possible. For more complex assertions that need to be retried, use `expect.poll` or `expect.toPass`.

### 92. Playwright — Configuration (official docs)

- Source: https://playwright.dev/docs/test-configuration
- Retrieved: 2026-08-29
- Exa status: complete

## Introduction

Playwright has many options to configure how your tests are run. You can specify these options in the configuration file. Note that test runner options are top-level; do not put them into the `use` section.

## Basic Configuration

```javascript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  // Look for test files in the "tests" directory, relative to this configuration file.
  testDir: 'tests',
  // Run all tests in parallel.
  fullyParallel: true,
  // Fail the build on CI if you accidentally left test.only in the source code.
  forbidOnly: !!process.env.CI,
  // Retry on CI only.
  retries: process.env.CI ? 2 : 0,
  // Opt out of parallel tests on CI.
  workers: process.env.CI ? 1 : undefined,
  // Reporter to use
  reporter: 'html',
  use: {
    // Base URL to use in actions like `await page.goto('/')`.
    baseURL: 'http://localhost:3000',
    // Collect trace when retrying the failed test.
    trace: 'on-first-retry',
  },
  // Configure projects for major browsers.
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  // Run your local dev server before starting the tests.
  webServer: {
    command: 'npm run start',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});
```

| Option | Description |
| --- | --- |
| `testConfig.forbidOnly` | Whether to exit with an error if any tests are marked as `test.only`. Useful on CI. |
| `testConfig.fullyParallel` | Have all tests in all files run in parallel. |
| `testConfig.projects` | Run tests in multiple configurations or on multiple browsers |
| `testConfig.reporter` | Reporter to use. |
| `testConfig.retries` | The maximum number of retry attempts per test. |
| `testConfig.testDir` | Directory with the test files. |
| `testConfig.use` | Options with `use{}` |
| `testConfig.webServer` | To launch a server during the tests, use the `webServer` option |
| `testConfig.workers` | The maximum number of concurrent worker processes. Can also be set as percentage of logical CPU cores, e.g. `'50%'`. |

## Filtering Tests

Filter tests by glob patterns or regular expressions with `testIgnore` and `testMatch`.

## Advanced Configuration

```javascript
import { defineConfig } from '@playwright/test';

export default defineConfig({
  // Folder for test artifacts such as screenshots, videos, traces, etc.
  outputDir: 'test-results',
  // Path to the global setup file, run before all the tests.
  globalSetup: require.resolve('./global-setup'),
  // Path to the global teardown file, run after all the tests.
  globalTeardown: require.resolve('./global-teardown'),
  // Each test is given 30 seconds.
  timeout: 30000,
});
```

### 93. Playwright — Test API: `test` (incl. `test.step`)

- Source: https://playwright.dev/docs/api/class-test#test-step
- Retrieved: 2026-08-29
- Exa status: complete

Playwright Test provides a `test` function to declare tests and `expect` function to write assertions.

```javascript
import { test, expect } from '@playwright/test';

test('basic test', async ({ page }) => {
  await page.goto('https://playwright.dev/');
  const name = await page.innerText('.navbar__title');
  expect(name).toBe('Playwright');
});
```

## Methods

### test

Added in: v1.10. Declares a test. Signatures: `test(title, body)` and `test(title, details, body)`.

Tags: you can tag tests by providing additional test details, or include tags in the test title. Each tag must start with the `@` symbol.

```javascript
import { test, expect } from '@playwright/test';

test('basic test', {
  tag: '@smoke',
}, async ({ page }) => {
  await page.goto('https://playwright.dev/');
  // ...
});
```

Test tags are displayed in the test report and are available to a custom reporter via `TestCase.tags`. You can also filter tests by their tags during test execution: in the command line, or in the config with `testConfig.grep` and `testProject.grep`.

Annotations: you can annotate tests by providing additional test details:

```javascript
import { test, expect } from '@playwright/test';

test('basic test', {
  annotation: {
    type: 'issue',
    description: 'https://github.com/microsoft/playwright/issues/23180',
  },
}, async ({ page }) => {
  await page.goto('https://playwright.dev/');
  // ...
});
```

Test annotations are displayed in the test report and are available via `TestCase.annotations`; annotations can also be added during runtime via `testInfo.annotations`.

### test.abort

Added in: v1.60. Aborts the currently running test by throwing an error. The test is immediately marked as failed and execution stops. Useful from inside a fixture or a route handler when an unrecoverable misuse has been detected:

```javascript
import { test, expect } from '@playwright/test';

test('does not publish to shared page', async ({ page }) => {
  await page.route('**/publish', route => {
    test.abort('Tests must not publish to the shared page. Use the `clone` option.');
    return route.abort();
  });
  // ...
});
```

### test.afterAll

Added in: v1.10. Declares an `afterAll` hook executed once per worker after all tests. When called in the scope of a test file, runs after all tests in the file; inside a `test.describe()` group, runs after all tests in the group.

```javascript
test.afterAll(async () => {
  console.log('Done with tests');
  // ...
});
```

When multiple `afterAll` hooks are added, they run in the order of their registration. The page also documents `test.beforeAll`, `test.beforeEach`, `test.afterEach`, `test.describe`, `test.step` (reportable steps for readable reports), `test.setTimeout`, `test.skip`, and the fixtures API.

### 94. Playwright — Authentication (official docs)

- Source: https://playwright.dev/docs/auth
- Retrieved: 2026-08-29
- Exa status: complete

## Introduction

Playwright executes tests in isolated environments called browser contexts. This isolation model improves reproducibility and prevents cascading test failures. Tests can load existing authenticated state. This eliminates the need to authenticate in every test and speeds up test execution.

## Core concepts

Regardless of the authentication strategy you choose, you are likely to store authenticated browser state on the file system.

We recommend creating a `playwright/.auth` directory and adding it to your `.gitignore`. The authentication routine will produce authenticated browser state and save it to a file in this directory. Later on, tests will reuse this state and start already authenticated.

> **Danger:** the browser state file may contain sensitive cookies and headers that could be used to impersonate you or your test account. Checking them into private or public repositories is strongly discouraged.

```bash
mkdir -p playwright/.auth
echo $'\nplaywright/.auth' >> .gitignore
```

## Basic: shared account in all tests

This is the recommended approach for tests without server-side state. Authenticate once in the setup project, save the authentication state, and then reuse it to bootstrap each test already authenticated.

When to use: when you can imagine all your tests running at the same time with the same account, without affecting each other.

When not to use: your tests modify server-side state (e.g. one test checks the rendering of the settings page while the other is changing the setting, in parallel — in that case tests must use different accounts); or your authentication is browser-specific.

Create `tests/auth.setup.ts` that will prepare authenticated browser state for all other tests:

```javascript
// tests/auth.setup.ts
import { test as setup, expect } from '@playwright/test';
import path from 'path';

const authFile = path.join(__dirname, '../playwright/.auth/user.json');

setup('authenticate', async ({ page }) => {
  // Perform authentication steps. Replace these actions with your own.
  await page.goto('https://github.com/login');
  await page.getByLabel('Username or email address').fill('username');
  await page.getByLabel('Password').fill('password');
  await page.getByRole('button', { name: 'Sign in' }).click();
  // Wait until the page receives the cookies.
  //
  // Sometimes login flow sets cookies in the process of several redirects.
  // Wait for the final URL to ensure that the cookies are actually set.
  await page.waitForURL('https://github.com/');
  // Alternatively, wait until the page reaches a state where all cookies are set.
  await expect(page.getByRole('button', { name: 'View profile and more' })).toBeVisible();
  // End of authentication steps.
  await page.context().storageState({ path: authFile });
});
```

Create a new `setup` project in the config and declare it as a dependency for all testing projects. This project will always run and authenticate before all the tests. All testing projects should use the authenticated state as `storageState`:

```javascript
// playwright.config.ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  projects: [
    // Setup project
    { name: 'setup', testMatch: /.*\.setup\.ts/ },
    {
      name: 'chromium',
      use: {
        ...devices['Desktop Chrome'],
        // Use prepared auth state.
        storageState: 'playwright/.auth/user.json',
      },
      dependencies: ['setup'],
    },
    {
      name: 'firefox',
      use: {
        ...devices['Desktop Firefox'],
        // Use prepared auth state.
        storageState: 'playwright/.auth/user.json',
      },
      dependencies: ['setup'],
    },
  ],
});
```

Tests start already authenticated because `storageState` is specified in the config.

```javascript
// tests/example.spec.ts
import { test } from '@playwright/test';

test('test', async ({ page }) => {
  // page is authenticated
});
```


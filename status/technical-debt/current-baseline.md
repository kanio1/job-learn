---
name: current-baseline
last_updated: 2026-07-13
audited_branch: 001-project-foundation
audited_commit: 95e35c97d74608bdc3d7925a4f6bb0b46c99a79b
---

# Technical Debt — Current Baseline

Only findings with direct evidence from this session's fresh validation run (`status/evidence/latest-validation.md`) or independently corroborated by two or more sources are listed. General improvement ideas without evidence are excluded per the audit's own rule.

| ID | Problem | Source | Affected Kiro tasks | Evidence | Severity | Scope | Recommended action |
|----|---------|--------|----------------------|----------|----------|-------|---------------------|
| TD-1 | ~~`AuditEventPersistenceTest.migrationAndJpaMappingPersistOnlyExplicitAuditFields` fails~~ **RESOLVED 2026-07-12** — see `status/roadmaps/audit-export-closure.md`. Root cause: the test's exhaustive field-list assertion predated the legitimate `beforeState`/`afterState` addition (Flyway `V11`, Playwright Phase 3C-5 "F-D7 audit diff drawer"); fixed by updating the assertion and adding a dedicated JSONB round-trip test, not by reverting the feature. | Fresh `./mvnw -Dsurefire.excludes='**/restkit/**,**/paymentsupport/**' verify` run, prior session | `audit-log-dashboard` | `apps/backend/src/test/java/lab/paymentquality/audit/AuditEventPersistenceTest.java` (fixed, commit-pending), `status/roadmaps/audit-export-closure.md` (full investigation + decision record) | RESOLVED (was HIGH) | `apps/backend/.../AuditEventPersistenceTest.java` | Closed. Targeted audit suite: 32/32 green. Full filtered `./mvnw verify` Surefire stage: 463/463 green, 0 failures, 5 skipped. See TD-5 below for a newly-exposed, unrelated Failsafe regression this fix uncovered. |
| TD-5 | ~~`SeedProfileStartupIT`/`TestEndpointsEnabledIT` hardcoded merchant count = 3~~ **RESOLVED 2026-07-12** — see `status/roadmaps/audit-export-closure.md`'s sibling record in `status/specs/deterministic-seed-and-test-isolation.md` §3 (tasks 3.2/3.3). Root cause confirmed via `git log`/`git show 8861f84`: `Fixtures.merchants()` was deliberately extended to 4 merchants (`MERCHANT_SUSPENDED_DEMO`, commit `8861f84` "system functinalities extend", 2026-06-29 — the MVP Phase 1 roadmap's `SEED-MVP-001` task) **after** `SeedProfileStartupIT`/`TestEndpointsEnabledIT` were written (commit `1ade297` "wave 3R finished", predates `8861f84`). `FixturesTest` was correctly updated in the same commit (`merchantsContainsExactlyFourWithExpectedUuids`, `hasSize(4)`) but the two Failsafe ITs were never touched. Fixed by updating the 4 stale `isEqualTo(3)`→`isEqualTo(4)` merchant-count assertions and strengthening `SeedProfileStartupIT.deterministicMerchantUuidsArePresent` to also assert the 4th merchant's deterministic UUID exists. The 104-payment-order-count assertions were verified unaffected (the 4th merchant owns zero payment orders per `Fixtures.paymentOrders()`) and were left unchanged. | Fresh `./mvnw -Dsurefire.excludes='**/restkit/**,**/paymentsupport/**' verify` run, prior session (Failsafe stage, first time it ran to completion since TD-1 always blocked it earlier) | `deterministic-seed-and-test-isolation` (tasks 3.2, 3.3 — now `DONE_VERIFIED` again with fresh evidence) | `apps/backend/src/test/java/lab/paymentquality/testing/{SeedProfileStartupIT,TestEndpointsEnabledIT}.java` (fixed, commit-pending) | RESOLVED (was HIGH) | `apps/backend/src/test/java/lab/paymentquality/testing/{SeedProfileStartupIT,TestEndpointsEnabledIT}.java` | Closed. Targeted Failsafe: `SeedProfileStartupIT` 8/8, `TestEndpointsEnabledIT` 9/9, `TestEndpointsDisabledIT` 4/4, `TestEndpointsProdSafetyIT` 4/4 (25/25 total). Targeted unit: `FixturesTest` 25/25, `DeterministicDatasetTest` 3/3, `TestingModuleTest` 4/4, `TestOperationResponseTest` 6/6, `TestEndpointSecurityChainTest` 5/5 (43/43 total). Full filtered `./mvnw verify`: **BUILD SUCCESS** — Surefire 463/463 (0 failures, 5 skipped), Failsafe 46/46 (0 failures). No new regression found. |
| TD-2 | **DONE_VERIFIED 2026-07-13** — TD-2E-1/TD-2E, TD-2F, TD-2D, and TD-2G all completed. | Continuation validation: targeted suites, repeat-each=10, two closure Chromium runs, independent final frontend run, and filtered backend verify | None directly (Playwright specs are POST_KIRO_WORK) | RESOLVED (was HIGH) | `apps/frontend/tests/e2e/**`, `apps/frontend/app/**`, duplicate-error BFF/backend path | Closed: all three full Chromium runs are 82 passed / 0 failed; filtered backend verify is green. |
| TD-3 | **DONE_VERIFIED 2026-07-13** — stale current-status headers in six `docs/specs-analysis/**/README.md` files contradicted their canonical `status/specs/*.md` ledgers. | TD-3 continuation audit: docs `02`–`07` compared against seven status ledgers and the latest validation evidence | None — documentation only | RESOLVED (was LOW) | `docs/specs-analysis/{02-iam-roles-and-keycloak-login,03-payment-operations-dashboard,04-tenant-model-and-isolation,05-user-management,06-audit-log-dashboard,07-deterministic-seed-and-test-isolation}/README.md` | Closed: headers now reference the current canonical status ledger; historical narrative evidence remains unchanged. |
| TD-4 | **DONE_WITH_DEVIATION 2026-07-13** — immutable Kiro `specId` collision: `audit-log-dashboard`, `deterministic-seed-and-test-isolation`, `iam-roles-and-keycloak-login`, and `user-management` share `ddbff980-460a-4eec-ae6b-f004d743fac8`, which has no single canonical owner. | TD-4 identifier inventory; `.codex/status-hygiene-audit.md` historical report | None — documentation/data hygiene only | LOW — no execution behavior affected | `.kiro/specs/{audit-log-dashboard,deterministic-seed-and-test-isolation,iam-roles-and-keycloak-login,user-management}/.config.kiro` | **Deviation:** immutable `.kiro/**` retains the collision. `status/index.md` now declares each unique slug/path the canonical current identity and forbids new references to the shared UUID; all current references are unambiguous. |
| VAL-API-01A | **DONE_VERIFIED 2026-07-13** — standalone merchant DTO did not include current `riskFlagged`. | Fresh `apps/api-tests` Testcontainers baseline | REST Assured black-box framework | RESOLVED (was HIGH validation gap) | `apps/api-tests/.../MerchantResponse.java` | Focused merchant/tenant specs green; full Surefire 79/79 and Failsafe 72/72. |
| PW-AUTH-01 / PW-DATA-01 / PW-IDEM-01 / PW-304-01 | **DONE_VERIFIED 2026-07-13** — `ASSURANCE-CLOSURE-W1-VALIDATION-02` complete live run passed 7/7 after focused validation. It includes real Keycloak roles/tenants, same-route authorization and foreign-tenant denial, two-worker owner-key allocation, BFF replay persistence proof, and BFF conditional GET/304 headers/body proof. | Complete live run plus focused projects and 82/82 Chromium regression baseline | Post-Kiro Playwright assurance | RESOLVED (was HIGH validation gap) | `apps/frontend/playwright.live.config.ts`, `apps/frontend/tests/live/**`, BFF payment-order routes, merchant/payment scope code | Closed. Worker data remains owner-tagged and retained in a session-owned ephemeral database; no global reset or foreign-worker delete was introduced. |
| PW-HEAD-01 | **SUPERSEDED_BY_VERIFIED_SOLUTION 2026-07-13** — a dedicated Playwright/APIRequestContext HEAD test is duplicate coverage without a distinct BFF HEAD route. | Fresh `apps/api-tests` Failsafe 72/72 | Post-Kiro Playwright assurance | RESOLVED (was LOW validation gap) | `HttpMethodSemanticsContractSpec.head_existing_payment_order_returns_200_with_etag_and_no_body` | The actual backend resource HEAD contract is verified for 200, ETag, Vary, Cache-Control, correlation ID, and empty body. |
| QA-HARDEN-01 | **DONE_VERIFIED 2026-07-13** — all 11 system-hardening/frontend-polish requirements now have focused acceptance-level proof. Ten implementations were already correct. One confirmed product defect was fixed: the `Field Errors` term in `ProblemDetailsCard` lacked the shared `w-28` label width. Two initial test assumptions were corrected as test-design defects without production workarounds. | Assurance Closure Wave 2A focused suites plus full frontend validation | Post-Kiro hardening/polish roadmap | RESOLVED (was MEDIUM validation gap) | Five new focused frontend acceptance-test files; `ProblemDetailsCard.vue`; `status/roadmaps/system-hardening-and-frontend-polish.md` | Closed: focused packages 01A–01E green; typecheck green; units 58 files / 594 tests; standard Chromium 82/82. Required verified completeness is 296/296. |
| SEED-PROP-01 | **DONE_VERIFIED 2026-07-13** — optional deterministic-seed task 5.1 / Property 6 now exhaustively cross-checks five actual per-role Keycloak realm users against deterministic tenants, merchants, and ownership. | Two focused jqwik runs; related IAM/tenant/seed/security tests; filtered backend verify | `deterministic-seed-and-test-isolation` task 5.1 | RESOLVED (was LOW optional coverage gap) | `RealmAlignmentPropertyTest.java`; `status/specs/deterministic-seed-and-test-isolation.md` | Closed: both focused runs 4/4; property generation exhaustive 5/5; compile/test-compile green; Surefire 469/464/5 and Failsafe 46/46. Optional verified/deviation coverage increases 43/73 → 44/73; the other 29 optional tasks remain explicitly skipped. |

The current assurance queue contains no unresolved required item. `REST-ADVANCED` is a separately gated Wave 2B design, not a Wave 2A defect: multipart is the first recommended executable capability because an evidence-upload domain already exists; proxy/SSL, redirects, and OpenAPI drift retain explicit prerequisite stop conditions documented in `status/index.md` and the Wave 2A final evidence.

## TD-2 detail: Playwright chromium failure breakdown

### Historical note on the prior session's hypothesis rejection

The previous session's "Rejected hypothesis" conclusion below (PENDING-vs-DRAFT) was made **before** the actual masking causes (TD-2A: default timeout, missing session roles) were identified and removed. Once those were fixed, the PENDING/DRAFT contract mismatch became directly observable and is now **CONFIRMED** (see TD-2B). This is left in place, struck through in spirit but not in text, as a record of how the investigation actually unfolded — a lesson that "element not found" symptoms can have multiple stacked causes, and rejecting a hypothesis based on symptom text alone (without removing confounding causes first) can be premature.

### TD-2A — RESOLVED 2026-07-12 (10 of 21 tests fixed, 0 regressions)

Two distinct, now-confirmed root causes, fixed together because both are pure test-infrastructure/test-mock defects discovered via the same investigation and both needed for the affected spec files to reach a stable state:

**TD-2A-1 (8 tests): default 5000ms Playwright assertion timeout too tight for this project's `nuxt dev` webServer.**
Measured first-render latency for a fresh `/admin/merchants*` navigation (session fetch → middleware → Suspense resolution → component mount) is consistently ~4.1–4.6s in isolation (confirmed via a temporary diagnostic probe spec, removed after use), and regularly exceeds 5s under this machine's default worker parallelism (16 workers sharing one dev server, confirmed via `nproc`=32). Proof: rerunning the failing 8 files serially (`--workers=1`) dropped 21→13 failures with zero code changes — pure contention removal. `ui/command-palette.spec.ts` already worked around this per-assertion with an explicit `{ timeout: 15000 }` on the same "Merchants" heading other tests check with the default. **Fix**: `apps/frontend/playwright.config.ts` — added project-wide `expect: { timeout: 15_000 }` (see inline comment for full justification). Fixed: `foundation.spec.ts` (1), `payment-status-polling.spec.ts` (2, one is borderline-flaky under full-82-test contention — passes reliably in isolation/smaller batches), `rbac/merchant-risk-flag.spec.ts` (2), `auth-deny.spec.ts` "authenticated identity..." (1), `payment-orders-panel.spec.ts` "renders payment summary..." + "renders forbidden state..." (2).

**TD-2A-2 (2 tests): `mockAuthenticatedSession()` test helper omits the `roles` array, so `useAuthorization()`'s fail-closed empty-roles default hides every capability-gated button.**
`infra/keycloak/realms/payment-quality-realm.json` confirms the real `platform.operator` user (the helper's default username) is assigned realm role `PLATFORM_ADMIN`, which per `app/utils/rbacMatrix.ts` grants `canCreateMerchant`/`canUpdateMerchantStatus`/etc. The mock never replicated this — `{ loggedIn: true, user: { username } }` has no `roles`, so `useAuthorization()` (`app/composables/useAuthorization.ts`) computes an empty roles array and every capability is `false`. This hid the "Create merchant" and "Activate/Suspend" buttons entirely for any test using the bare helper. **Fix**: `apps/frontend/tests/e2e/merchant-support.ts` — `mockAuthenticatedSession(page, username, roles = ['PLATFORM_ADMIN'])`; the one call site that deliberately wants a no-authority session (`auth-deny.spec.ts`'s "authenticated identity without merchant authority" test, username `'merchant.denied'`) was updated to pass `roles: []` explicitly to preserve its intended denial scenario. Fixed: `merchant-create.spec.ts` "create-merchant-form data-testid..." + "activate-merchant-button data-testid... for a pending merchant" (2).

Targeted validation: the exact 8 originally-failing spec files rerun with both fixes → **11 failed / 18 passed** (down from 21/61). Full 101-test chromium baseline rerun → **12 failed / 70 passed** (1 extra — `payment-status-polling.spec.ts:52` reappeared under full-suite 16-worker contention; confirmed flaky, not a new defect: reruns green in isolation, `3 passed` in 19.6s). Frontend `typecheck` and `test:unit` (532/532) both green after the fix, confirming no regression. **Zero new failures introduced anywhere in the 101-test suite.**

### TD-2B — DONE_VERIFIED 2026-07-12: merchant status contract mismatch, `'PENDING'` vs real backend `'DRAFT'`

Codex CLI completed this package after an interrupted Claude Code CLI handoff. The backend domain values are `DRAFT`, `ACTIVE`, `SUSPENDED`; `MerchantMapper.toResponse()` uses `merchant.getStatus().name()`, confirming serialized `"DRAFT"`. No OpenAPI specification or active legacy caller supports `PENDING`.

- Strict frontend boundary: `merchantStatusSchema` now accepts exactly `DRAFT`, `ACTIVE`, `SUSPENDED`, is exported for direct contract tests, and rejects `PENDING` and unknown values. `MerchantResponse` remains inferred from that schema and `MerchantTable` derives its row type from it.
- Application and presentation: filters use `DRAFT`; table action gating activates a `DRAFT` merchant; `BusinessStatusBadge` maps `DRAFT` to the user label `Draft`; all merchant mocks and visual fixtures use `DRAFT`. Unrelated audit-history `PENDING` remains untouched.
- Validation: focused schema/badge tests 40/40 green; typecheck green; full units 46 files / 546 tests green; targeted changed-spec Chromium run 16 passed / 5 failed, with all TD-2B assertions green; full Chromium 72 passed / 10 failed. The tracked visual snapshot was replaced from `merchant-badge-pending-...png` to `merchant-badge-draft-...png`.
- The 5 targeted failures are not TD-2B: four stale `Create` accessible-name expectations and one unsupported `SUSPENDED` re-activation expectation. No compatibility alias, permissive Zod fallback, timeout change, or lifecycle-rule change was introduced.

### Historical TD-2F diagnosis — superseded by DONE_VERIFIED closure (2026-07-13)

`merchant-lifecycle.spec.ts:39` expects `SUSPENDED -> ACTIVE`, but the backend state machine allows only `DRAFT -> ACTIVE` and `ACTIVE -> SUSPENDED`; the Playwright mock correctly returns 409. This was exposed once the DRAFT activation path became valid. It is not part of TD-2B and requires a product/lifecycle decision before changing code or the test.

### TD-2C — DONE_VERIFIED 2026-07-12: stale merchant loading/error and payment empty-state expectations

All three failures were stale test expectations; no merchant or payment API contract defect was found.

- `merchant-feedback.spec.ts:4`: a controlled pending GET now verifies `LoadingState` with `role=status`, name `Loading merchants…`, no premature empty heading, then the post-response empty heading. The arbitrary `setTimeout` mock delay and stale table expectation were removed.
- `merchant-feedback.spec.ts:32`: a structured `503 application/problem+json` response now verifies `role=alert`, `Request failed`, problem title/detail, `Retry`, recovery to the empty merchant state, and two hydration-time initial requests plus the third retry request. `ErrorState` now exposes the alert semantics.
- `payment-orders-panel.spec.ts:41`: a 200 success with empty list/summary verifies the payment empty state, current `No currency data.` copy, creation action, and absence of loading/error state. It no longer asserts obsolete currency/status copy.

Targeted affected specs: 6/6 green. Full Chromium: 76 passed / 6 failed, exactly 3 fewer failures than TD-2B's 72/10 baseline. No snapshot was regenerated.

### Historical TD-2E partial result — superseded by DONE_VERIFIED closure (2026-07-13)

Fresh reproduction confirmed the four test titles at lines 4, 23, 52, and 81 each stopped before form submission on `getByRole('button', { name: 'Create', exact: true })`. The rendered element is role `button`, visible text `Create`, accessible name `Create merchant`, supplied intentionally by `CreateMerchantForm.vue`'s enabled-state `aria-label`; it is not a product or shared-component defect. Six exact role locators were updated to `Create merchant`, preserving both semantic selection and exactness. Three test titles now pass (successful creation and both validation-gating paths), preserving their downstream assertions and the `DRAFT` contract. The duplicate-feedback title now reaches its original assertion and exposes a separate root cause: the mock returns legacy `{ error, message }`, while the current client expects Problem Details and correctly displays the generic safe fallback. This is tracked as TD-2E-1; no production code, helper, snapshot, timeout, retry, or unrelated cluster was changed.

### Historical TD-2E-1 diagnosis — superseded by DONE_VERIFIED closure (2026-07-13)

`merchant-create.spec.ts` test `shows create validation and duplicate feedback` now reaches its post-submit assertion but its mock `409` body is `{ error: 'duplicate_merchant_reference', message: 'Duplicate merchant reference' }`. The current `useApiClient` maps non-Problem-Details responses to the intentional fallback `Failed to create merchant. Please try again.`, so the existing assertion for `A merchant with this reference already exists` fails. This is a stable test-fixture contract mismatch, distinct from TD-2E accessible naming. Next package: inspect the BFF/error mapping and existing error tests, then make the smallest contract-aligned fixture/assertion correction without weakening duplicate-feedback coverage.

### TD-2 continuation closure — DONE_VERIFIED 2026-07-13

- **TD-2E-1 / TD-2E:** verified `409 application/problem+json` response with `type`, `title`, `status`, `detail`, `error`, and conditional `correlationId`; exact duplicate E2E and all six merchant-create Chromium cases pass; `MerchantRestAssuredTest` is 5/5 green.
- **TD-2F:** verified transition graph `DRAFT -> ACTIVE`, `ACTIVE -> SUSPENDED`, terminal `SUSPENDED`; domain tests are 17/17 and lifecycle E2E passes.
- **TD-2D:** the browser normalizes `%2F` in the displayed URL, so the test now checks pathname and `redirectTo` value rather than transport encoding; auth-deny E2E passes with protected data absent.
- **TD-2G:** 6/10 failures reproduced first. The cause was `includes(detailPath)`, which also matched `/history` and `/evidence`; exact pathname plus GET matching made repeat-each=10 green (20 polling cases). No retry, sleep, timeout, or worker workaround was introduced.
- **Closure:** full Chromium run 1 = 82/82, run 2 = 82/82, independent final frontend run = 82/82. TD-2 has no stable or flaky residual failure.

### Historical TD-2D diagnosis — superseded by DONE_VERIFIED closure (2026-07-13)

- `auth-deny.spec.ts:4` "unauthenticated access starts Keycloak redirect and hides merchant data" — mocks `**/auth/keycloak` to return `<h1>Keycloak login</h1>` and expects that heading to become visible after `page.goto('/admin/merchants')` while unauthenticated. Never fires — confirms the prior session's H3 hypothesis. Needs a direct read of `app/middleware/auth.global.ts`'s actual current unauthenticated redirect behavior (it redirects to `/login`, a real app page — see the middleware's own doc comment — not to `/auth/keycloak` directly) to write the corrected assertion; not yet done this session.

### Historical hypothesis ledger (superseded where noted)

| Hypothesis (audit brief §16 / this session's §11) | Prior-session verdict | This-session verdict |
|---|---|---|
| PENDING vs DRAFT merchant-status mismatch | REJECTED (no direct evidence found) | **CONFIRMED** (TD-2B) — was masked by TD-2A |
| `merchant-feedback.spec.ts` stale assertions | CONFIRMED (attributed to a pre-existing, accepted Phase 3B-8 regression) | Still CONFIRMED stale, but the specific mechanism is TD-2C (loading/error-state copy), not the Phase 3B-8 note — both may be true simultaneously |
| `auth-deny.spec.ts` stale redirect assumption | CONFIRMED | DONE_VERIFIED (TD-2D, 2026-07-13) |
| Missing/unstable `data-testid` (`payment-status-polling`, `merchant-risk-flag`) | CONFIRMED independent | **SUPERSEDED** — both were actually TD-2A-1 (timeout), not a testid problem; re-ran green with no testid changes |

**Not independently re-verified with PostgreSQL/Testcontainers this session:** the audit brief's hypothesis that "new audit diff and payment expiration [features] may not have been verified with PostgreSQL/Testcontainers" — the corresponding Playwright specs (`audit-diff-drawer.spec.ts`, `payment-expiration.spec.ts`) both **passed** in every chromium run this session, and the backend `AuditEventPersistenceTest` (the one PostgreSQL/Testcontainers-backed test touching audit before/after-state) was **fixed in a prior session** (TD-1) — so the hypothesis is now moot for both halves.
